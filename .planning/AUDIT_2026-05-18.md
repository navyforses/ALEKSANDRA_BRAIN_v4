# ALEKSANDRA_BRAIN — Ground-Truth State Audit

**Audit date:** 2026-05-18
**Author:** Claude (read-only audit; no memory, no handoff reuse)
**Methodology:** Every claim below cites a primary source — file path, verifier stdout, SQL row count, or HTTP probe result. CLAUDE.md, `.handoffs/`, and `PHASE_*_EXIT_REPORT.md` are NOT quoted as sources of fact; section 6 cross-checks them against the audit findings.
**Cost incurred:** $0. No LLM-using verifier paths exercised. No writes to any backend.

---

## 1. Executive summary — 18-cell matrix

Legend: 🟢 verified working / 🟡 partial or known caveat / 🔴 verified broken / ⚪ unprobed

| Phase | Code (filesystem) | Verifier (read-only) | Production traffic |
|---|---|---|---|
| 0 — Foundation | 🟢 committed | 🟢 implicit (subsequent phases regress through it) | 🟡 daily-budget-gate workflow active but n8n process dead — code-side `check_daily_budget()` is the live backstop |
| 1 — Perception | 🟢 committed | 🟢 **10/10 PASS** | 🔴 6h cron `perception_6h.json` inactive; n8n service broken (runs worker code, not n8n) |
| 2 — Memory | 🟢 committed | 🟢 **19/19 PASS** | 🟢 Qdrant Cloud + Neo4j Aura rehydrated (5302 vectors, 792 nodes) — but no live ingestion path firing |
| 2.5 — Quick Wins | 🟢 committed | 🟡 **15/16 PASS** — C.3 (daily_digest workflow last-fire 0) | 🟡 same C.3 root cause: daily_digest.json inactive; would resolve on first fire |
| 3 — Cognition | 🟢 committed | 🟡 **9/11 PASS** — CGM-01 NEW failure (Qdrant 403) + REGR cascade | 🟡 outreach_review_queue active and fires; 1 outreach_log row exists; no clinician outreach has been sent |
| 4 — Family Value | 🟢 committed | 🟡 **8/9 PASS** code-complete — REGR cascade only | 🟡 4 of 11 workflows active; 1 clinician PDF in `briefs/clinician/` but `briefs` table count = 0 (never written through prod path); `prod_t1_delivered=0`, `prod_weekly_drafts=0`, `prod_clinician_drafts=0` |
| 5 — Manager | 🟢 committed | 🟡 **12/13 PASS** code-complete — REGR cascade only | 🔴 `manager_actions=0`, `intake_drops=0` — zero production traffic; Vercel viewer build broken so the UI cannot even render |

**Two unrelated cross-system findings outside the phase matrix:**

- 🟢 **Railway `aleksandra-worker` service is LIVE.** `GET https://aleksandra-worker-production.up.railway.app/healthz` returns `{"status": "ok", "service": "perception_worker"}` (probed §4.4)
- 🔴 **Vercel viewer is DOWN.** All 5 most recent main-branch production deploys are in `ERROR` state; the alias `aleksandra-brain-v4.vercel.app` does not exist (returns `DEPLOYMENT_NOT_FOUND`). Root cause: build looks for `pages/` or `app/` at the repo root but the Next.js app lives in `viewer/app/` — Vercel "Root Directory" setting is misconfigured (§4.6)

---

## 2. Per-phase evidence

### Phase 0 — Foundation
**Source of truth:** `workflows/daily-budget-gate.json` (read 2026-05-18)

| Probe | Result |
|---|---|
| `daily-budget-gate.json` cron + active flag | `active=True`, schedule `every 30 minutes`, polls Supabase `runs` table |
| Where it POSTs | Nowhere (pure n8n; targets Supabase REST + Telegram only) |
| Whether n8n is actually running this workflow | NO — see §4.5 (n8n service runs `perception_worker`, not n8n) |

### Phase 1 — Perception
**Source of truth:** `python -m scripts.verify_phase1` output (run 2026-05-18, no `--json`)

```
[PASS]  1. ledger rows >= 20                  actual=326
[PASS]  2. distinct source_types >= 3         actual=5 (biorxiv,crawl4ai,ctgov,medrxiv,pubmed)
[PASS]  3. all 5 provenance fields non-null   null_rows=0
[PASS]  4. >= 1 mode='negative' row           negative_rows=30
[PASS]  5. R2 artifact count >= ledger rows   R2=334  ledger=326
[PASS]  6. content_hash integrity (sample 3)  3/3 match
[PASS]  7. NCBI compliance                    email=set  tool=aleksandra_brain  api_key=set
[PASS]  8. firecrawl spend < cap              spend=$0.00  cap=$10.00  calls=0
[PASS]  9. workflows/perception_6h.json       present
[PASS] 10. Phase 0 fetch-lint regression      violations=0
RESULT: 10/10 PASS
```

**Production reality:** `perception_6h.json` is `active=False` (file inspection §A.2) → n8n cron is not firing → no fresh ingestion has happened via production path. All 326 ledger rows came from earlier `perception_tick --no-telegram` drills.

### Phase 2 — Memory
**Source of truth:** `python -m scripts.verify_phase2` output (run 2026-05-18)

19/19 PASS. Notable evidence:
- `paper_chunks` = **5301** rows (Supabase)
- `papers` = **255** rows (Supabase)
- Qdrant `papers` collection = **5302** vectors, dim=**384** (probed directly §4.2)
- Graphiti Entity nodes = **641** (Neo4j, probed directly §4.3 confirms 641)
- Graphiti RELATES_TO = **935** edges
- Episodic = **141**
- MENTIONS = **988** edges
- Typed Drug=83, Disease=227, Treatment=94, Trial=14, Biomarker=107, Gene=34

All 4 previously-deferred MEM checks (MEM-01/04/05/06) now PASS.

### Phase 2.5 — Quick Wins
**Source of truth:** `python -m scripts.verify_phase2_5 --json` output

15/16 PASS. The 1 failure:
- `C.3` — `workflows/daily_digest.json exists + ≥ 1 fire in last 24h` → `file=yes  recent_fire=0`

This is the known cascading caveat. `daily_digest.json` is `active=False` in the file (§A.2) and `runs` query for last 24h shows 0 daily_digest entries.

### Phase 3 — Cognition
**Source of truth:** `python -m scripts.verify_phase3 --json` output

**9/11 PASS** — TWO failures, including a previously-undocumented one:

1. 🔴 **CGM-01 FAIL** — *"Every Communicator output has ≥1 source citation per claim"*
   - Evidence verbatim: `live summarize() raised: HTTPStatusError: Client error '403 Forbidden' for url 'https://2e7a3efd-8f14-4d01-a6b8-0ed0ffb3069f.us-east-1-1.aws.cloud.qdrant.io:6333/collections/papers/points/search'`
   - **Root cause:** `scripts/verify_phase3.py` (or the upstream `summarize()` it exercises) opens a Qdrant client that does NOT pass `QDRANT_API_KEY`. The same bug Shako fixed in 5 other scripts (`scripts/setup_qdrant.py`, `scripts/chunking/embedder.py`, `scripts/chunking/retrofit_qdrant_stamps.py`, `scripts/verify_phase2.py`, `scripts/verify_phase2_5.py` — all modified, uncommitted per §A.1) still exists in the Phase 3 retrieval path.
   - This is a **real bug**, not a cascade. It was not surfaced in the prior handoff narrative.

2. 🟡 **REGR FAIL** — cascade from Phase 2.5 C.3 (`15/16 PASS`)

Other Phase 3 checks (all PASS):
- CGM-02 PHI redactor 12/12 fixtures
- CGM-03 Tier router 100/100 (100%)
- CGM-04 Gmail compose-only scopes, no_send=True, 1 pending draft
- CGM-05 Weekly Brief PDF rendered 4001B
- CGM-06 Confidence 30/30 in range
- CGM-07 Language detection 30/30 (EN/KA/FR)
- CGM-08 Banned phrases 95% accuracy
- CGM-09 Daily cap 5 enforced
- CGM-10 Migration 008 applied, 96/75 contacts, anon RLS clean

### Phase 4 — Family Value
**Source of truth:** `python -m scripts.verify_phase4 --mode code-complete --json` output

8/9 PASS — only REGR cascade fails. All scaffolding present:
- FFV-01 telegram digest workflow ok, `prod_t1_delivered=0`
- FFV-03 weekly Gmail digest scaffold ok, `prod_weekly_drafts=0`
- FFV-04 Notion bootstrap + 1 archived finding
- FFV-05 clinician PDF 4432B + Gmail draft attachment, `prod_clinician_drafts=0`
- OBS-02 1 recent linked digest
- OBS-03 latest spend report `2026-05-17 22:06:58 UTC`

**Production reality from Supabase (§4.1):**
- `briefs` table count = **0** — no brief has ever been written through the production path
- `runs` count = **3131** (most are local-drill traces, but OBS-03 found one spend report from 22:06 on 2026-05-17)
- `contacts` count = **96** (matches CGM-10 evidence)
- `outreach_log` count = **1** — single staged draft, never sent

**Disk evidence:** `briefs/clinician/clinician_74b5a152b37a_20260516T235114.pdf` (4705 bytes) — one PDF on disk from a 2026-05-16 generation; not from production cron.

### Phase 5 — Manager
**Source of truth:** `python -m scripts.verify_phase5 --mode code-complete --json` output

12/13 PASS — only REGR cascade fails. Visible checks (MNG-03 through MNG-12 + REGR; MNG-01/MNG-02 were in the truncated portion of stdout but verifier's overall `passed=False` follows from REGR alone):
- MNG-03 OCR module + photo fixtures present
- MNG-04 voice recorder + route + lib present
- MNG-05 email parser + fixtures
- MNG-06 preview cards + router + diff
- MNG-07 batch apply
- MNG-08 realtime + ActivityFeed + audit route
- MNG-09 undo + button + route + audit page
- MNG-10 morning briefing module + workflow
- MNG-11 email draft module + route + UI
- MNG-12 PHI `CHECK intake_drops_must_redact` constraint present (migration 011)

**Production reality from Supabase (§4.1):**
- `manager_actions` count = **0**
- `intake_drops` count = **0**
- → **Phase 5 has never seen production traffic.** The verifier passes because `--mode code-complete` only requires module presence + fixture dirs, not real rows.

---

## 3. Untracked Phase 6 (Visualization) scaffold

**Total uncommitted code: 5,073 lines across 33 files** (`wc -l` evidence §A.5).

### MCP servers — `mcp/` (1024 LOC total)
| File | LOC | Tracked? |
|---|---|---|
| `mcp/__init__.py` | 1 | untracked |
| `mcp/aleksandra_niivue_mcp.py` | 284 | untracked |
| `mcp/hello_brain.py` | 66 | committed (pre-existing) |
| `mcp/panic_stop.py` | 176 | committed (pre-existing) |
| `mcp/swarm_orchestrator.py` | 497 | untracked |

### Swarm agents — `agents/swarm/` (921 LOC total, fully untracked)
| File | LOC |
|---|---|
| `__init__.py` | 18 |
| `celery_app.py` | 49 |
| `celery_tasks.py` | 70 |
| `chunk_worker.py` | 214 |
| `enhanced_detector.py` | 278 |
| `team_registry.py` | 292 |

### Neuroimaging scripts — `scripts/neuroimaging/` (3,128 LOC total, fully untracked)
17 files, largest:
- `generate_brain_procedural.py` (629)
- `generate_neuron.py` (426)
- `create_realistic_brain.py` (417)
- `compare_brains.py` (259)
- `create_damaged_brain.py` + `create_realistic_damaged.py` (165 each)

### Standalone HTML/JS demos — `viewer/` (8 HTML pages + 7 JS, all untracked)
| File | LOC | Note |
|---|---|---|
| `viewer/brain_voxels.html` | 468 | uses banned `linear-gradient` per DESIGN.md (not yet enforced) |
| `viewer/neuron_3d.html` | 505 | banned gradient |
| `viewer/brain_3d.html` | 318 | — |
| `viewer/brain_surface.html` | 265 | — |
| `viewer/brain_comparison.html` | 284 | — |
| `viewer/brain_procedural.html` | 244 | — |
| `viewer/neuron_surface.html` | 310 | — |

### Test fixtures — `tests/fixtures/` (untracked, 6 JSON files)
Real swarm-run outputs — useful as regression baselines if/when swarm code is committed:
- `brain_comparison_report.json`
- `damaged_brain_points.json`
- `enhanced_detector_validation.json`
- `healthy_brain_points.json`
- `swarm_comparison_report.json`
- `test_brain_128_swarm_result.json`

### Phase 6 input docs
- `.planning/phase-5/SPEC_DRAFT.md` (one file in dir; legacy)
- `.planning/research/MCP_SWARM_ARCHITECTURE.md` (large architecture writeup)
- `.planning/PHASE_5_INPUTS.md` (cleanup checklist for the Gemini scaffold)
- `DESIGN.md` (Linear/Notion/Mayo design system)

---

## 4. Cloud services — live state (probed 2026-05-18)

### 4.1 Supabase — `redsinfzadkyrsnwcznu.supabase.co`
Probed via `supabase-py` `service-role` key. All counts are exact via REST `count='exact'`.

| Table | Row count |
|---|---|
| `evidence_ledger` | **326** |
| `paper_chunks` | **5301** |
| `papers` | **255** |
| `contacts` | **96** |
| `hypotheses` | **10** |
| `therapies` | **16** |
| `manager_actions` | **0** |
| `intake_drops` | **0** |
| `runs` | **3131** |
| `outreach_log` | **1** |
| `alerts_log` | **2** |
| `briefs` | **0** |
| `aleksandra_timeline` | **9** |

Tables that **do not exist** in `public` schema (queried, errored with "Could not find the table"): `llm_call`, `citations`, `digest_to_run_link`, `telegram_history`, `email_log`, `firecrawl_calls`. Some of these may live in another schema, or may be referenced in code but never migrated.

### 4.2 Qdrant Cloud — `2e7a3efd-…aws.cloud.qdrant.io:6333`

| Collection | Vectors | Dim | Status |
|---|---|---|---|
| `papers` | **5302** | 384 | green |
| `therapies` | 0 | 384 | green |
| `hypotheses` | 0 | 384 | green |

### 4.3 Neo4j Aura — `29204819.databases.neo4j.io`

**Total nodes: 792.  Total relationships: 1932.**

| Label | Count | | Relationship | Count |
|---|---|---|---|---|
| Entity | 641 | | MENTIONS | 988 |
| Disease | 227 | | RELATES_TO | 935 |
| Episodic | 141 | | HAS_BRAIN_REGION | 9 |
| Biomarker | 107 | | | |
| Treatment | 94 | | | |
| Drug | 83 | | | |
| BrainRegion | 55 | | | |
| Pathway | 36 | | | |
| Gene | 34 | | | |
| Trial | 14 | | | |
| Patient | 1 | | | |

### 4.4 Railway worker — `aleksandra-worker-production.up.railway.app`

- `GET /healthz` → **200 OK**, body: `{"status": "ok", "service": "perception_worker"}`
- Subdomain pattern confirmed: simple `aleksandra-worker-production`, no random hash suffix.

Other guessed patterns probed: `aleksandra-worker.up.railway.app` → 404, `aleksandra-worker-production-c8df` → 404, `aleksandra-worker-production-3a5c` → 404.

Railway GraphQL API auth failed (`RAILWAY_API_TOKEN` returns `Not Authorized` / `Project Token not found`). Cannot list services programmatically; only the worker's healthz is verifiable from outside.

### 4.5 n8n service — `n8n-production-48c7.up.railway.app`

| Probe | Result |
|---|---|
| `GET /` | **200**, body `{"status": "ok", "service": "perception_worker"}` |
| `GET /healthz` | **200**, same body |
| `GET /api/v1/workflows` (n8n REST) | **404**, body `{"error": "not_found", "path": "/api/v1/workflows"}` |
| `GET /rest/workflows` (legacy n8n REST) | **404**, same shape |
| Server header | `railway-edge` |

**Conclusion:** the Railway service named `n8n` is **not running n8n** — it is running our Python `perception_worker.py`. The 11 workflow JSON files in `workflows/` are inert: nothing consumes them.

### 4.6 Vercel — `viewer` project (`prj_pOGHJR8Hr3ACxFr2IOzAi8S1lkhm`)

Last 5 production deploys on `main` branch — **all in `ERROR` state**:

| Deploy URL | State | Created |
|---|---|---|
| viewer-1xz36z0nt-… | ERROR | 2026-05-18T06:02 |
| viewer-n9k3wqlb4-… | ERROR | 2026-05-18T06:01 |
| viewer-na1c5l14u-… | ERROR | 2026-05-18T02:14 |
| viewer-pyf3453oy-… | ERROR | 2026-05-18T01:42 |
| viewer-688rw6ehn-… | ERROR | 2026-05-18T01:40 (railway/code-change-pZznTp branch) |

**Build error (verbatim from `v3/deployments/{id}/events`):**
```
Vercel CLI 53.3.2
⚠ Found lockfile missing swc dependencies, run next locally to automatically patch
> Build error occurred
Error: > Couldn't find any `pages` or `app` directory. Please create one under the project root
Error: Command "npm run vercel-build" exited with 1
```

**Root cause:** Vercel "Root Directory" project setting is the repo root, but Next.js source lives at `viewer/`. After commit `f4b8d72 chore(deploy): delete stale root package.json + add explicit Dockerfile` removed the placeholder root `package.json`, Next.js detection at the repo root started failing.

**Alias state:**
| Alias | Points to | Created |
|---|---|---|
| `viewer-git-main-shakos-projects-82dad3f2.vercel.app` | `dpl_9LGKmTS447ZynTLR…` | 2026-05-18T06:02 |
| `viewer-git-railway-code-change-pzzntp-…` | `dpl_7p5gmWwoBp4n7rzq…` | 2026-05-18T06:00 |
| `viewer-git-railway-fix-deploy-f46f45-…` | `dpl_85BexgYG3HJrs33u…` | 2026-05-18T06:00 |
| `viewer-sigma-two.vercel.app` | `dpl_2za3fitxtXHC7PGc…` | **2026-05-14T23:53** |
| `viewer-jincharadzeshako-9429-…` | `dpl_2za3fitxtXHC7PGc…` | 2026-05-14T23:53 |
| `viewer-shakos-projects-82dad3f2.vercel.app` | `dpl_2za3fitxtXHC7PGc…` | 2026-05-14T23:53 |

- `aleksandra-brain-v4.vercel.app` → **DEPLOYMENT_NOT_FOUND**. The URL the operator runbook references *does not exist as a Vercel alias.*
- `viewer-sigma-two.vercel.app` → **200 OK** on `/`, **404** on `/brain` — serves the stale 2026-05-14 deploy (which is older than Phase 5 sprint close on 2026-05-18, hence no `/brain` route built).
- `viewer-git-main-…` → **401** (Vercel SSO required). Auth-gated; cannot confirm whether the deploy would have a `/brain` route even if the build had succeeded.

### 4.7 n8n workflow inventory — disk only (since no n8n process to query)

11 workflows in `workflows/`. Read each via `python json.load` (3 needed `encoding='utf-8'` due to ka-locale strings):

| File | Active flag (file) | Trigger | Target |
|---|---|---|---|
| `chunking_trigger.json` | `False` | every 30 min | `{{$env.PERCEPTION_WORKER_URL}}/chunking-tick` |
| `daily_digest.json` | `False` | cron `0 9 * * *` | Supabase REST + Telegram (no worker) |
| `daily_spend_report.json` | `False` | cron `0 12 * * *` | Supabase REST + Telegram |
| `daily-budget-gate.json` | **`True`** | every 30 min | Supabase REST + Telegram (pure n8n) |
| `extraction_trigger.json` | `False` | every 30 min | `{{$env.PERCEPTION_WORKER_URL}}/extraction-tick` |
| `manager_briefing.json` | **`True`** | cron `0 13 * * 0` (Sun 13 UTC) | `{{$env.PHASE5_MANAGER_WORKER_URL \|\| $env.PHASE5_VOICE_WORKER_URL \|\| 'https://placeholder.invalid'}}/morning-briefing` |
| `outreach_review_queue.json` | **`True`** | cron `0 18 * * *` | Supabase REST + Telegram |
| `perception_6h.json` | `False` | every 6 h | `{{$env.PERCEPTION_WORKER_URL}}/perception-tick` |
| `telegram_daily_digest.json` | **`True`** | cron `0 13 * * *` | `{{$env.PHASE4_DIGEST_WORKER_URL}}/fire-daily-batch` |
| `urgent_alerts.json` | `False` | every 6 h | Supabase REST + Telegram |
| `weekly_brief.json` | **`True`** | cron `0 13 * * 0` | `{{$env.PHASE4_DIGEST_WORKER_URL \|\| 'https://placeholder.invalid'}}/render-weekly-brief` |

5 marked active in file, 6 inactive. **None of these fire** in reality, because the Railway `n8n` service runs `perception_worker.py` instead of n8n (§4.5).

Two workflows reference an env var that is **not present in `.env`**:
- `PHASE4_DIGEST_WORKER_URL` (used by `telegram_daily_digest.json` and `weekly_brief.json`)
- `PHASE5_VOICE_WORKER_URL` (alternate fallback in `manager_briefing.json`)

`PHASE5_MANAGER_WORKER_URL` and `PERCEPTION_WORKER_URL` are also referenced but **not present in `.env`** either (§5).

---

## 5. Config & secrets reality — `.env` shape audit

Probed via regex `^[A-Z_]+=` against `.env`, exposing only *shape* (length, prefix), never values.

### Filled and validated
| Line | Key | Shape |
|---|---|---|
| 11 | `SUPABASE_URL` | url(`https://redsinfzadkyrsnwcznu.supabase.co`) |
| 12 | `SUPABASE_ANON_KEY` | jwt(208) |
| 13 | `SUPABASE_SERVICE_ROLE_KEY` | jwt(219) |
| 15 | `NEXT_PUBLIC_SUPABASE_URL` | url(same) |
| 16 | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | jwt(208) |
| 20 | `SUPABASE_DB_URL` | postgres(88) |
| 26 | `NEO4J_URI` | aura(`neo4j+s://29204819.databases.neo4j.io`) |
| 27 | `NEO4J_USERNAME` | filled(5) |
| 28 | `NEO4J_PASSWORD` | filled(43) |
| 29 | `NEO4J_DATABASE` | filled(5) |
| 35 | `QDRANT_URL` | url(`https://2e7a3efd-…aws.cloud.qdrant.io:6333`) |
| 36 | `QDRANT_API_KEY` | jwt(176) |
| 42 | `CLOUDFLARE_ACCOUNT_ID` | filled(32) |
| 48 | `CLOUDFLARE_KV_NAMESPACE_ID` | filled(32) |
| 54 | `ANTHROPIC_API_KEY` | anthropic_key(108) |
| 60 | `TELEGRAM_BOT_TOKEN` | filled(46) |
| 61 | `TELEGRAM_CHAT_ID` | filled(14) |
| 67 | `N8N_URL` | url(`https://n8n-production-48c7.up.railway.app`) |
| 68 | `N8N_API_KEY` | jwt(289) |
| 69 | `RAILWAY_API_TOKEN` | filled(36) — **but auth fails at Railway GraphQL (§4.4)** |
| 70 | `VERCEL_TOKEN` | filled(60) — auth OK |
| 80 | `DAILY_BUDGET_USD` | short(4) |
| 93 | `OPENAI_API_KEY` | openai(164) |
| 94 | `NOTION_API_KEY` | filled(50) |
| 95 | `NOTION_PARENT_PAGE_ID` | filled(32) |
| 96 | `NOTION_DATABASE_ID` | filled(36) |
| 101 | `GITHUB_TOKEN` | filled(93) |
| 102 | `GITHUB_OWNER` | filled(16) |
| 103 | `GITHUB_REPO` | filled(16) |
| 110 | `NCBI_API_KEY` | filled(36) |
| 111 | `NCBI_EMAIL` | filled(26) |
| 112 | `NCBI_TOOL` | filled(16) |
| 115 | `FIRECRAWL_MONTHLY_CAP_USD` | short(2) |
| 124 | `MANAGER_USER_ID` | filled(17) |
| 141 | `PHASE5_WORKER_AUTH_TOKEN` | hex64 |

### Placeholder / empty / missing
| Line | Key | Shape | Impact |
|---|---|---|---|
| 77 | `ANTHROPIC_USAGE_API_KEY` | **placeholder `sk-ant-admin-`** | n8n daily-budget-gate can't read real spend (but n8n doesn't run anyway, §4.5) |
| 84 | `PANIC_STOP_WEBHOOK_URL` | empty | non-blocking |
| 89 | `FIRECRAWL_API_KEY` | empty | Firecrawl fallback unavailable; Phase 1 ran 0 fallback calls anyway |
| 90 | `PERPLEXITY_API_KEY` | empty | unused in codepaths |
| 91 | `EXA_API_KEY` | empty | unused |
| 92 | `TAVILY_API_KEY` | empty | unused |
| — | `PHASE5_MANAGER_WORKER_URL` | **NOT IN FILE** | Vercel viewer cannot resolve the worker; n8n `manager_briefing.json` falls back to `placeholder.invalid` |
| — | `PERCEPTION_WORKER_URL` | **NOT IN FILE** | `perception_6h.json` + `chunking_trigger.json` + `extraction_trigger.json` cannot fire |
| — | `PHASE4_DIGEST_WORKER_URL` | **NOT IN FILE** | `telegram_daily_digest.json` + `weekly_brief.json` would fail |
| — | `PHASE5_VOICE_WORKER_URL` | **NOT IN FILE** | alternate fallback only |
| — | `TESSERACT_PATH` | commented out | Photo OCR uses Claude-vision fallback |

---

## 6. Documentation-vs-reality deltas

I now compare three claim-bearing documents against the audit findings. These docs are **cross-checked**, not quoted as facts.

| Source | Claim | Audit reality | Delta |
|---|---|---|---|
| `CLAUDE.md` "მიმდინარე ეტაპი" | Phase 2: "568 entities · 307 facts · 47 episodes" | Neo4j: 641 entities (Entity label), 935 RELATES_TO, 141 Episodic | Entities +73, relationships +628, episodes +94 — Phase 2.5 rehydration added more than CLAUDE.md captured |
| `CLAUDE.md` | Phase 2.5: "326 ledger · 5301 chunks · 5302 Qdrant vectors · 568 Neo4j entities" | All four confirmed (§4.1, §4.2, §4.3 — except entities=641) | Ledger/chunks/vectors match; entities count drifted upward in current Neo4j Aura instance |
| `CLAUDE.md` | Phase 3: "11/11 CGM gates + REGR ყველა GREEN" | verify_phase3 → 9/11 PASS (CGM-01 Qdrant 403 + REGR cascade) | 🔴 CLAUDE.md overclaims by 2 checks. **CGM-01 was not GREEN at the time of audit.** |
| `CLAUDE.md` | Phase 4: "code-complete 9/9 PASS" | verify_phase4 --mode code-complete → 8/9 (REGR cascade) | Cascade only; 9 PASS minus REGR = 8/9 |
| `CLAUDE.md` | Phase 5: "13/13 PASS · ALL GREEN" | verify_phase5 --mode code-complete → 12/13 (REGR cascade) | Cascade only; 12 PASS minus REGR |
| `CLAUDE.md` | "78/78 cumulative verifier coverage" | 10+19+15+9+8+12 = **73/78** in this audit's run | -5 from headline due to CGM-01 + 4 cascades |
| `CLAUDE.md` | "Phase 5 production activation needs ~45min Shako work" | Worker IS deployed and healthz returns 200 (§4.4) — but Vercel is broken (§4.6) and env vars `PHASE5_MANAGER_WORKER_URL`/`PERCEPTION_WORKER_URL` are missing from `.env` (§5) | Worker side DONE; Vercel + env distribution NOT done |
| `5-warm-crown.md` Phase E | "Open https://aleksandra-brain-v4.vercel.app/brain in browser" | Alias does not exist (§4.6: `DEPLOYMENT_NOT_FOUND`) | URL in runbook is fictitious. Real candidate aliases all 401 or 404 on `/brain` |
| `handoff` | "n8n service runs `perception_worker.py`, not n8n" | Confirmed (§4.5: `/api/v1/workflows` returns 404; `/healthz` returns service name `perception_worker`) | Handoff narrative is **correct on this point** |
| `handoff` | "aleksandra-worker BUILDING at session pause; subdomain not captured" | Worker is LIVE at `aleksandra-worker-production.up.railway.app` (§4.4) | Build succeeded after pause. Simple subdomain (no random suffix) |
| `handoff` | "real failures = 0" | CGM-01 is a real failure (§Phase 3) | Handoff missed this |
| `PHASE_*_EXIT_REPORT.md` | "78/78 cumulative coverage" | Same delta as CLAUDE.md (-5 today) | Exit reports were correct at their write time; environment-state changes between then and now caused CGM-01 to regress |

---

## 7. Outstanding work — tiered

### Tier 0 — Blocks any production traffic (must fix before Phase 5 family value materializes)

| # | Item | Evidence section | Effort |
|---|---|---|---|
| T0-1 | Fix Vercel "Root Directory" project setting to `viewer/` (currently repo root → Next.js can't find `app/`) | §4.6 | Shako 2 min |
| T0-2 | Re-trigger Vercel deploy after T0-1; confirm `viewer-git-main-…` 401 → 200 with valid SSO session | §4.6 | Shako 5 min + 1 deploy cycle |
| T0-3 | Add the missing 4 env vars to `.env`: `PHASE5_MANAGER_WORKER_URL`, `PERCEPTION_WORKER_URL`, `PHASE4_DIGEST_WORKER_URL`, `PHASE5_VOICE_WORKER_URL` (all four point at `https://aleksandra-worker-production.up.railway.app`) | §5 | 5 min |
| T0-4 | Copy the 4 worker URLs + `PHASE5_WORKER_AUTH_TOKEN` to Vercel project env vars | §5, §4.6 | 5 min |
| T0-5 | Repair n8n service: change Railway `n8n` source from GitHub repo to Docker Image `n8nio/n8n:latest`; workflows resume from postgres-volume | §4.5 | Shako 10 min + 3 min redeploy |
| T0-6 | Fix CGM-01 root cause: add `QDRANT_API_KEY` propagation to whichever Qdrant client `summarize()` uses; commit alongside the 5 already-modified scripts | §Phase 3 | 15 min |
| T0-7 | Commit the 6 currently-modified files | git §A.1 | 3 min |

### Tier 1 — Phase 4 acceptance window (closes ~2026-06-07)

| # | Item | Evidence | Effort |
|---|---|---|---|
| T1-1 | Activate `perception_6h`, `daily_digest`, `daily_spend_report`, `chunking_trigger`, `extraction_trigger`, `urgent_alerts` workflows in n8n UI after T0-5 | §4.7 file-flag inactive list | 5 min (after n8n alive) |
| T1-2 | Manually fire `daily_digest` once to close Phase 2.5 C.3 cascade (resolves CGM-01-independent REGR for Phase 2.5 → 3 → 4 → 5) | §Phase 2.5 | 1 min |
| T1-3 | Confirm Sunday 2026-05-24 first real Weekly Brief fires (writes to `briefs` table; currently 0 rows) | §4.1 `briefs=0` | Wait until Sunday |
| T1-4 | Confirm `manager_briefing.json` fires Sunday 13:00 UTC + delivers Telegram | §4.7 active=True | Wait |
| T1-5 | Monitor `outreach_log` (currently 1) — should grow if `outreach_review_queue.json` (active) is producing | §4.1 | Daily check |

### Tier 2 — Hardening (no immediate user impact)

| # | Item | Evidence | Effort |
|---|---|---|---|
| T2-1 | Fix CLAUDE.md "მიმდინარე ეტაპი" — downgrade Phase 3 11/11 claim to acknowledge CGM-01 (or commit fix from T0-6 first) | §6 delta row | 5 min |
| T2-2 | Get fresh `RAILWAY_API_TOKEN` (current returns `Not Authorized`) — enables service inventory automation | §4.4 | Shako 3 min |
| T2-3 | Ingest the `aleksandra-brain-v4.vercel.app` URL question — either purchase/assign that alias or update all documentation referencing it | §4.6 | 10 min |
| T2-4 | Decide on missing tables: `llm_call`, `citations`, `digest_to_run_link`, `telegram_history`, `email_log`, `firecrawl_calls` — are these intended to exist? If yes, write a migration; if no, remove code references | §4.1 | 30 min |
| T2-5 | Reconnect `ANTHROPIC_USAGE_API_KEY` so daily-budget-gate workflow can read real spend post-n8n-restoration | §5 line 77 | 10 min |
| T2-6 | Process remaining ~200 ledger papers blocked by Anthropic 400 content-filter (Neo4j currently 792 nodes; was ~568 before, target ~1000+) | §4.3 | LLM spend ~$0.30 |
| T2-7 | Delete stale Cloudflare Worker `aleksandrabrane4` | (not probed in this audit; carry over) | Shako 2 min |

### Tier 3 — Phase 6 (Visualization) — entirely backlog

| # | Item | Evidence | Effort |
|---|---|---|---|
| T3-1 | Decide Phase 6 framing: integrate the 5,073 LOC of untracked Phase 6 scaffold via `/gsd-spec-phase`, OR rewrite from scratch with a fresh SPEC | §3 | Discussion |
| T3-2 | Cleanup blockers per `.planning/PHASE_5_INPUTS.md` before any integration: dual layout, Tailwind 3↔4 reconciliation, banned-gradient demos, MCP-INVENTORY registration, Redis decision, PHI/stdio gate for niivue MCP | §3 + `.planning/PHASE_5_INPUTS.md` | Variable |
| T3-3 | Phase 6 backlog items from `5-warm-crown.md` out-of-scope: Google Calendar API integration, pattern recognition, TVB simulation, mobile responsive viewer, Supabase Auth, voice-ambient PHI redactor expansion, `aleksandra_timeline.event_type` ENUM | (not probed; carry-over) | Multi-week |

---

## 8. Methodology trail (every numeric claim → its source)

| Section claim | Reproducer |
|---|---|
| `evidence_ledger=326`, `paper_chunks=5301`, etc. | `python -X utf8 -c "from supabase import create_client; ..."` with `count='exact'` |
| Qdrant `papers=5302 dim=384` | `qdrant_client.QdrantClient(url=…, api_key=…).count('papers').count` |
| Neo4j 792 nodes / 1932 rels | `MATCH (n) RETURN count(n)` + `MATCH ()-[r]->() RETURN count(r)` |
| Worker `/healthz` 200 | `requests.get('https://aleksandra-worker-production.up.railway.app/healthz', timeout=6)` |
| n8n service running `perception_worker` | `requests.get('https://n8n-production-48c7.up.railway.app/api/v1/workflows', headers={...})` → 404; root `/` → `{"service":"perception_worker"}` |
| Vercel ERROR deploys | `requests.get('https://api.vercel.com/v6/deployments?projectId=…&limit=5', headers={'Authorization': f'Bearer {VERCEL_TOKEN}'})` |
| Vercel build error verbatim | `requests.get('https://api.vercel.com/v3/deployments/{id}/events?limit=200', …)` filtered by type=stderr |
| Phase verifier results | `.venv/Scripts/python.exe -X utf8 -m scripts.verify_phaseN [--mode code-complete] [--json]` |
| File LOC counts | `wc -l <files>` on Phase 6 scaffold |
| .env shape audit | `grep -nE "^[A-Z_]+=" .env | awk ...` with shape-only output (no values) |
| Workflow active flags | `python -c "json.load(open('workflows/X.json', encoding='utf-8')).get('active')"` |
| Git status | `git status --short` + `git log --oneline -30` |

---

## 9. Confidence statement

- **High confidence (probed twice via different paths):** Supabase row counts, Qdrant vector counts, Neo4j node counts, worker healthz, Vercel deploy state, n8n service identity, .env shape.
- **Medium confidence (probed once):** verifier outputs (run once each; could re-run for verification), workflow active flags (read from disk, not from a running n8n).
- **Low/unprobed:** Cloudflare R2 artifact integrity beyond what `verify_phase1` already checked (PRC-05/06); Notion API beyond `verify_phase4` Phase 4 bootstrap; Gmail compose-only scope beyond `verify_phase3` CGM-04 evidence string; Railway service inventory (token auth failed §4.4).

---

*End of audit. Owner: Shako Jincharadze. Next decision point: which Tier 0 items to execute first — T0-1 (Vercel root) and T0-5 (n8n service swap) are the two single-step changes that unlock everything downstream.*
