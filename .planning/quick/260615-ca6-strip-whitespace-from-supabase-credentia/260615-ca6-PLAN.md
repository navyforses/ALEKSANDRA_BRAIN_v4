---
phase: quick-260615-ca6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/ledger.py]
autonomous: true
requirements: [QUICK-CA6]

must_haves:
  truths:
    - "A Supabase credential value with a trailing newline/whitespace produces no header value containing a newline"
    - "_supabase_creds() returns a stripped key and a stripped, trailing-slash-free url"
    - "_supabase_headers() strips the key it receives before building the header dict"
    - "Local .env-based runs continue to work unchanged"
  artifacts:
    - path: "scripts/ledger.py"
      provides: "Whitespace-stripped Supabase credentials and headers"
      contains: ".strip()"
  key_links:
    - from: "scripts/ledger.py:_supabase_creds"
      to: "os.environ SUPABASE_SERVICE_ROLE_KEY"
      via: "os.environ.get(...).strip()"
      pattern: "SUPABASE_SERVICE_ROLE_KEY.*strip"
    - from: "scripts/ledger.py:_supabase_headers"
      to: "header dict apikey/Authorization"
      via: "key = key.strip() before dict build"
      pattern: "key = key.strip"
---

<objective>
Strip whitespace from Supabase credentials in `scripts/ledger.py` so the
"Perception fallback (worker-independent)" GitHub Actions job stops failing on
its first HTTP call with `httpx.LocalProtocolError: Illegal header value b'***'`.

Purpose: The GitHub secret `SUPABASE_SERVICE_ROLE_KEY` arrives as an env var with
a trailing newline. `_supabase_creds()` reads it un-stripped, `_supabase_headers()`
interpolates it into `apikey` and `Authorization: Bearer <key>`, and httpx rejects
any header value containing `\n`. `load_env()` only strips values it reads from
`.env` for keys NOT already in `os.environ`, so the GitHub-secret env-var path
bypasses all stripping — which is why local `.env` runs pass but CI fails.

Output: A two-site defensive `.strip()` change (plus explanatory comments) in
`scripts/ledger.py`, restoring the Core Value perception-fallback pipeline in CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md

<interfaces>
<!-- Current code in scripts/ledger.py — modify exactly these two functions. -->
<!-- Do NOT touch load_env(), the workflow YAML, or any HTTP caller. -->

From scripts/ledger.py (current, BEFORE fix):
```python
def _supabase_creds() -> tuple[str, str]:
    load_env()
    url = os.environ.get("SUPABASE_URL", "").rstrip("/")
    key = os.environ.get("SUPABASE_SERVICE_ROLE_KEY", "")
    if not url or not key:
        raise RuntimeError("SUPABASE_URL or SUPABASE_SERVICE_ROLE_KEY missing in .env")
    return url, key


def _supabase_headers(
    key: str, *, prefer: str = "return=representation"
) -> dict[str, str]:
    return {
        "apikey": key,
        "Authorization": f"Bearer {key}",
        "Content-Type": "application/json",
        "Prefer": prefer,
    }
```

Callers (do NOT modify — they rely on the existing signatures):
- scripts/perception_tick.py:_budget_locked → first HTTP call; builds
  `headers=_supabase_headers(key, prefer="count=none")`. This is where CI dies.
- scripts/perception_tick.py:_write_run → `**_supabase_headers(key)`.
- scripts/ledger.py:is_known_source and other ledger functions call `_supabase_creds()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip whitespace from Supabase credentials and headers</name>
  <files>scripts/ledger.py</files>
  <action>
    Apply a defense-in-depth whitespace strip at two sites in scripts/ledger.py
    (per QUICK-CA6). Do NOT change function signatures, return shapes, or any
    other code. Do NOT touch load_env(), perception_tick.py, or the workflow YAML.

    Site 1 — `_supabase_creds()` (~line 172):
      - Change the url line to also strip leading/trailing whitespace before the
        existing trailing-slash strip: read `SUPABASE_URL`, apply `.strip()`, then
        `.rstrip("/")`.
      - Change the key line to apply `.strip()`: read `SUPABASE_SERVICE_ROLE_KEY`,
        then `.strip()`.
      - Keep the existing `if not url or not key` guard and the existing
        RuntimeError message unchanged (an all-whitespace value strips to "" and
        will still trip the guard, which is correct).

    Site 2 — `_supabase_headers()` (~line 181):
      - Add `key = key.strip()` as the FIRST statement in the function body, before
        the dict is constructed, so any caller passing a raw (un-stripped) key is
        also protected.

    Comments — add a short English comment at EACH site explaining the reason, e.g.:
      "Strip whitespace: a stray newline in a GitHub secret makes httpx raise
      'Illegal header value' when the key is interpolated into apikey/Authorization."

    Style: match the file's existing English-comment + type-hint conventions
    (PEP 8, 4-space indent). No new imports.
  </action>
  <verify>
    <automated>cd "c:/Users/jinch/OneDrive/სამუშაო დაფა/aleksandra brane" && (.venv/Scripts/python.exe scripts/ledger.py >/dev/null 2>&1 || true) && python -c "import sys; sys.argv=['x']; from scripts.ledger import _supabase_headers, _supabase_creds; import os; h=_supabase_headers('secretkey\n'); assert all('\n' not in v for v in h.values()), 'newline leaked into header value'; assert h['apikey']=='secretkey' and h['Authorization']=='Bearer secretkey', h; os.environ['SUPABASE_URL']='https://x.supabase.co/\n'; os.environ['SUPABASE_SERVICE_ROLE_KEY']=' k \n'; u,k=_supabase_creds(); assert u=='https://x.supabase.co' and k=='k', (u,k); print('PASS: no newline in headers; creds stripped')"</automated>
    <notes>If `.venv/Scripts/python.exe` is absent, the bare `python` invocation alone is the real check. Run from the repo root so `scripts` is importable. The test imports `_supabase_headers` with a key ending in `\n` and asserts no header value contains `\n`, then sets whitespace-laden env vars and asserts `_supabase_creds()` strips both.</notes>
  </verify>
  <done>
    `_supabase_creds()` strips both SUPABASE_URL (then rstrip "/") and
    SUPABASE_SERVICE_ROLE_KEY; `_supabase_headers()` strips its key argument first;
    both sites carry an explanatory English comment; the verify command prints
    PASS; no other code or files changed.
  </done>
</task>

</tasks>

<verification>
- `_supabase_headers("secretkey\n")` returns a dict whose every value is newline-free,
  with `apikey == "secretkey"` and `Authorization == "Bearer secretkey"`.
- `_supabase_creds()` with whitespace-laden env vars returns a stripped url
  (no trailing slash, no surrounding whitespace) and a stripped key.
- `git diff --stat` shows only `scripts/ledger.py` changed.
- No changes to load_env(), perception_tick.py, or .github/workflows/perception-fallback.yml.
</verification>

<success_criteria>
- A GitHub secret value with a trailing newline no longer produces a header value
  containing `\n` (the root cause of `httpx.LocalProtocolError: Illegal header value`).
- The fix is confined to `scripts/ledger.py`; the workflow YAML and secrets are untouched.
- Local `.env`-based runs behave identically (stripping a clean value is a no-op).
- The verify command exits 0 and prints PASS.
</success_criteria>

<output>
Create `.planning/quick/260615-ca6-strip-whitespace-from-supabase-credentia/260615-ca6-SUMMARY.md` when done
</output>
