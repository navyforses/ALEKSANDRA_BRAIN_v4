---
phase: 06.1-bilingual-polish-i18n-gap-closure
plan: 02
type: summary
status: complete
completed: 2026-05-22
requirements-completed:
  - I18N-13
files-modified:
  - viewer/messages/en.json
  - viewer/messages/ka.json
  - viewer/app/[locale]/timeline/page.tsx
  - viewer/app/[locale]/hypotheses/page.tsx
  - viewer/app/[locale]/hypotheses/[id]/page.tsx
commits:
  - c62f224 feat(06.1-02) add 4 enum namespaces
  - f6c0dab feat(06.1-02) wire enum namespaces with t.has() fallback
---

# Plan 06.1-02 SUMMARY — ENUM Label Translation

## Outcome

I18N-13 closed. 4 new enum namespaces in `viewer/messages/{en,ka}.json` + 3 server-component pages now render Georgian enum labels on `/ka/timeline`, `/ka/hypotheses`, `/ka/hypotheses/[id]`. Unmapped values fall back to the raw DB string (D-03 rule), so no page crashes on a value not in the namespace.

## Namespace inventory (en == ka leaf-set)

| Namespace | Leaves | Source |
|---|---|---|
| ConfidenceLevel | 4 | schema.sql:281+ (high/moderate/low/very_low) |
| HypothesisStatus | 8 | schema.sql:318-321 CHECK (new/under_review/promising/pursuing/tested/confirmed/rejected/archived) |
| HypothesisType | 10 | schema.sql:281 CHECK (drug_repurposing/pathway_target/combination_therapy/timing_optimization/cross_disease_inference/plasticity_mechanism/biomarker_discovery/technology_application/rehabilitation_innovation/other) |
| TimelineEventType | 11 | CONTEXT D-03 (8 canonical UPPER + 3 lowercase variants seen in code: appointment, medication_change, observation) |
| **Total** | **33** | |

Dictionary leaf count: en.json/ka.json each gained 33 leaves. Phase-6 dictionary parity (verify_phase6 check_i18n_03) still holds — overall `en_leaves == ka_leaves`.

## Render sites wired

All use the `t.has(v) ? t(v) : v` raw-value fallback pattern.

- **timeline/page.tsx**: count chip text + per-article event_type tag (2 sites).
- **hypotheses/page.tsx** (list): status badge + hypothesis_type label + confidence_level cell (3 sites).
- **hypotheses/[id]/page.tsx** (detail): same 3 sites as list view — Rule 3 anticipatory extension; planned scope was list-only but the detail page renders the same fields from the same JSONB rows, so translating one without the other would leave `/ka/hypotheses/[id]` in English. Cheap consistency fix.

Unchanged on purpose (raw enums needed for DB writes + visual styling):
- `tone(status)` switch in both hypotheses pages — classNames keyed on raw enum.
- `reviewHypothesis` form button `value=` attributes — server action writes raw enum to DB.

## Deviations

- **Rule 3 — Scope extension.** Translated `hypotheses/[id]/page.tsx` enum displays in addition to the list view (3 extra wrapping points + 3 new `getTranslations` calls). Justification above.

No HIGH concerns from plan-checker remained; the M1 pre-execute fix (HypothesisStatus 6→8 to match schema.sql:318-321) was applied during planning. M2 (BRAIN dormant components) was 06.1-01 scope.

## Verification

- `cd viewer && npm run build` → exits 0 (21 static pages generated; `/en/*` + `/ka/*` family routes preserved).
- `npx tsc --noEmit` → exits 0 (after Task 2 wiring).
- Dictionary parity check (node):
  ```
  ConfidenceLevel:    en=4  ka=4  symdiff=∅
  HypothesisStatus:   en=8  ka=8  symdiff=∅
  HypothesisType:     en=10 ka=10 symdiff=∅
  TimelineEventType:  en=11 ka=11 symdiff=∅
  ```
- `python -X utf8 -m scripts.verify_phase6 --mode code-complete` → **11/11 PASS · ALL GREEN**
- `python -X utf8 -m scripts.verify_phase4 --mode code-complete` → **9/9 PASS · ALL GREEN**
- `python -X utf8 -m scripts.verify_phase5 --mode code-complete` → **13/13 PASS · ALL GREEN**

89/89 cumulative project verifier coverage preserved.

## Self-Check

- [x] 4 enum namespaces present in both dictionaries, key-sets equal, Georgian distinct
- [x] timeline tags + count chips use t.has() fallback
- [x] hypotheses list view: status/type/confidence use t.has() fallback
- [x] hypotheses detail view: same 3 sites use t.has() fallback (Rule 3 extension)
- [x] tone() and form button values unchanged (DB writes still use raw enums)
- [x] cd viewer && npm run build exits 0
- [x] verify_phase6 11/11 + verify_phase4 9/9 + verify_phase5 13/13 GREEN
- [x] SUMMARY + STATE + ROADMAP updated; commits c62f224 + f6c0dab landed

## Handoff to 06.1-03

Wave 1 closed. 06.1-03 (DB content bilingual backfill — Shako-supervised production run via `compose_bilingual`) is the final plan. With 06.1-01 + 06.1-02 live, the only remaining `/ka` gap is the data content itself (rows currently mirror `ka = en` from migration 012). 06.1-03's backfill closes it.
