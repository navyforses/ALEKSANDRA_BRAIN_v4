---
phase: 06.1-bilingual-polish-i18n-gap-closure
plan: 02
type: execute
wave: 1
depends_on:
  - 06.1-01
files_modified:
  - viewer/messages/en.json
  - viewer/messages/ka.json
  - viewer/app/[locale]/timeline/page.tsx
  - viewer/app/[locale]/hypotheses/page.tsx
requirements:
  - I18N-13
autonomous: true
must_haves:
  truths:
    - "On /ka the timeline event_type tags and count chips render Georgian labels for known enum values"
    - "On /ka the hypotheses status badge, hypothesis_type label, and confidence_level render Georgian for known enum values"
    - "An UNKNOWN enum value (not present in the namespace) renders the raw DB string and never crashes the page (D-03 fallback rule)"
    - "Every t('Namespace.value') call resolves in BOTH en.json and ka.json for the enumerated values; the fallback path uses t.has() before t()"
    - "verify_phase6 check_i18n_03 leaf-count parity holds with the 4 new namespaces"
  artifacts:
    - path: viewer/messages/en.json
      provides: "TimelineEventType / HypothesisStatus / HypothesisType / ConfidenceLevel namespaces (English)"
      contains: "\"TimelineEventType\""
    - path: viewer/messages/ka.json
      provides: "same 4 namespaces, Georgian, key-sets equal to en.json"
      contains: "\"TimelineEventType\""
    - path: viewer/app/[locale]/timeline/page.tsx
      provides: "event_type tag + count chips via getTranslations('TimelineEventType') with t.has() raw-value fallback"
      contains: "TimelineEventType"
    - path: viewer/app/[locale]/hypotheses/page.tsx
      provides: "status/type/confidence via 3 namespaces with t.has() raw-value fallback"
      contains: "HypothesisStatus"
  key_links:
    - from: viewer/app/[locale]/timeline/page.tsx
      to: viewer/messages/{en,ka}.json
      via: "getTranslations('TimelineEventType') + t.has(v) ? t(v) : v"
      pattern: "getTranslations\\(['\\\"]TimelineEventType['\\\"]\\)"
    - from: viewer/app/[locale]/hypotheses/page.tsx
      to: viewer/messages/{en,ka}.json
      via: "getTranslations('HypothesisStatus'|'HypothesisType'|'ConfidenceLevel')"
      pattern: "getTranslations\\(['\\\"]Hypothesis"
---

<objective>
Close I18N-13 (CONTEXT.md D-03): translate the DB enum labels surfaced on the timeline and hypotheses pages. Add 4 dedicated namespaces to en.json + ka.json (TimelineEventType, HypothesisStatus, HypothesisType, ConfidenceLevel), then wire the render sites with a `t.has(value) ? t(value) : rawValue` fallback so an unmapped enum value renders the raw DB string and NEVER crashes (D-03 fallback rule). Both pages are server components → getTranslations.

depends_on: [06.1-01] only to serialize the en.json/ka.json writes (distinct namespaces — clean merge). No code overlap with 06.1-01.

Purpose: On /ka, the event_type tags, count chips, hypothesis status/type/confidence labels read Georgian instead of raw machine enums (SYSTEM_MILESTONE, drug_repurposing, etc.).
Output: 4 new namespaces × 2 dictionaries + 2 wired pages; verify_phase6 stays GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-bilingual-polish-i18n-gap-closure/06.1-CONTEXT.md
@viewer/messages/en.json
@viewer/messages/ka.json
@viewer/app/[locale]/timeline/page.tsx
@viewer/app/[locale]/hypotheses/page.tsx
@scripts/schema.sql
@viewer/AGENTS.md

<interfaces>
<!-- Both pages are async server components; setRequestLocale(locale) already called; add getTranslations per namespace. -->
const tEvt = await getTranslations('TimelineEventType');
const label = tEvt.has(event.event_type) ? tEvt(event.event_type) : event.event_type;  // D-03 raw-value fallback

<!-- ENUM SOURCES (verified):
  hypotheses.hypothesis_type CHECK (schema.sql:281): drug_repurposing, pathway_target, combination_therapy,
    timing_optimization, cross_disease_inference, plasticity_mechanism, biomarker_discovery,
    technology_application, rehabilitation_innovation, other
  hypotheses.confidence_level CHECK (schema.sql): high, moderate, low, very_low
  hypotheses.status (from hypotheses/page.tsx tone()/statusNew): confirmed, promising, pursuing, rejected, new
    (also under_review used as a button value — include it)
  aleksandra_timeline.event_type: free-form text. CONTEXT D-03 lists 8 canonical values:
    SYSTEM_MILESTONE, PROGRAM_ENROLLMENT, MEDICATION_CHANGE, APPOINTMENT, RELOCATION, MRI_SCAN, DIAGNOSIS, BIRTH
    NOTE: code paths also emit lowercase (appointment, medication_change, observation). Add BOTH the 8 canonical
    AND the lowercase variants seen in code (appointment, medication_change, observation) so live rows resolve;
    anything unmapped falls back to raw via t.has(). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 4 enum namespaces to en.json + ka.json (key-sets equal, Georgian distinct)</name>
  <files>viewer/messages/en.json, viewer/messages/ka.json</files>
  <read_first>
    - viewer/messages/en.json (do not collide with the 11 existing namespaces or with Manager from 06.1-01; add 4 new top-level keys)
    - viewer/messages/ka.json
    - scripts/schema.sql (lines ~281 hypothesis_type CHECK, confidence_level CHECK — authoritative value lists)
    - .planning/phases/06.1-bilingual-polish-i18n-gap-closure/06.1-CONTEXT.md (D-03 value lists + fallback rule)
  </read_first>
  <action>
    Add 4 NEW top-level namespaces to BOTH dictionaries. Key-set identical between en.json and ka.json (check_i18n_03 parity). The JSON KEY is the raw enum value; the VALUE is the display label.

    TimelineEventType (canonical 8 + lowercase variants seen in code):
      SYSTEM_MILESTONE   en "System milestone"     ka "სისტემური ეტაპი"
      PROGRAM_ENROLLMENT en "Program enrollment"    ka "პროგრამაში ჩართვა"
      MEDICATION_CHANGE  en "Medication change"     ka "მედიკამენტის ცვლილება"
      APPOINTMENT        en "Appointment"           ka "ვიზიტი"
      RELOCATION         en "Relocation"            ka "გადასვლა"
      MRI_SCAN           en "MRI scan"              ka "MRI სკანი"
      DIAGNOSIS          en "Diagnosis"             ka "დიაგნოზი"
      BIRTH              en "Birth"                 ka "დაბადება"
      appointment        en "Appointment"           ka "ვიზიტი"
      medication_change  en "Medication change"     ka "მედიკამენტის ცვლილება"
      observation        en "Observation"           ka "დაკვირვება"

    HypothesisStatus (schema.sql:318-321 CHECK — all 8):
      new          en "New"           ka "ახალი"
      under_review en "Under review"  ka "განხილვაში"
      promising    en "Promising"     ka "პერსპექტიული"
      pursuing     en "Pursuing"      ka "მიმდინარე"
      tested       en "Tested"        ka "ტესტირებული"
      confirmed    en "Confirmed"     ka "დადასტურებული"
      rejected     en "Rejected"      ka "უარყოფილი"
      archived     en "Archived"      ka "დაარქივებული"

    HypothesisType (schema.sql CHECK — all 10):
      drug_repurposing         en "Drug repurposing"          ka "წამლის გადანიშვნა"
      pathway_target           en "Pathway target"            ka "სამიზნე გზა"
      combination_therapy      en "Combination therapy"       ka "კომბინირებული თერაპია"
      timing_optimization      en "Timing optimization"       ka "დროის ოპტიმიზაცია"
      cross_disease_inference  en "Cross-disease inference"    ka "დაავადებათაშორისი დასკვნა"
      plasticity_mechanism     en "Plasticity mechanism"      ka "პლასტიკურობის მექანიზმი"
      biomarker_discovery      en "Biomarker discovery"       ka "ბიომარკერის აღმოჩენა"
      technology_application   en "Technology application"    ka "ტექნოლოგიის გამოყენება"
      rehabilitation_innovation en "Rehabilitation innovation" ka "სარეაბილიტაციო ინოვაცია"
      other                    en "Other"                     ka "სხვა"

    ConfidenceLevel (schema.sql CHECK — all 4):
      high      en "High"      ka "მაღალი"
      moderate  en "Moderate"  ka "ზომიერი"
      low       en "Low"       ka "დაბალი"
      very_low  en "Very low"  ka "ძალიან დაბალი"

    Keep valid UTF-8 JSON, no trailing commas. Touch only these 4 new keys (and leave Manager / the 11 existing namespaces untouched — minimizes merge surface vs 06.1-01).
  </action>
  <acceptance_criteria>
    - en.json and ka.json parse as valid JSON.
    - Both contain TimelineEventType, HypothesisStatus, HypothesisType, ConfidenceLevel.
    - Each of the 4 namespaces has equal leaf-count between en.json and ka.json.
    - ka values are Georgian, distinct from en.
  </acceptance_criteria>
  <verify>
    <automated>cd viewer && node -e "const en=require('./messages/en.json'),ka=require('./messages/ka.json');const ns=['TimelineEventType','HypothesisStatus','HypothesisType','ConfidenceLevel'];for(const n of ns){if(!en[n]||!ka[n])throw new Error('missing '+n);const a=Object.keys(en[n]).length,b=Object.keys(ka[n]).length;if(a!==b)throw new Error(n+' mismatch '+a+'/'+b);if(JSON.stringify(en[n])===JSON.stringify(ka[n]))throw new Error(n+' not translated')}console.log('OK 4 enum namespaces')"</automated>
  </verify>
  <done>4 enum namespaces present in both dictionaries, key-sets equal, Georgian distinct.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire timeline + hypotheses render sites with t.has() raw-value fallback</name>
  <files>viewer/app/[locale]/timeline/page.tsx, viewer/app/[locale]/hypotheses/page.tsx</files>
  <read_first>
    - viewer/app/[locale]/timeline/page.tsx (event_type rendered at the per-article tag line ~103 AND the count chips at lines ~83-85)
    - viewer/app/[locale]/hypotheses/page.tsx (status badge ~122, hypothesis_type ~125, confidence_level ~143)
    - .planning/phases/06.1-bilingual-polish-i18n-gap-closure/06.1-CONTEXT.md (D-03 — both server components; raw-value fallback mandatory)
    - viewer/AGENTS.md → node_modules/next/dist/docs/ (next-intl t.has API on this version)
  </read_first>
  <action>
    timeline/page.tsx:
      (a) After existing getTranslations calls, add `const tEvt = await getTranslations('TimelineEventType');`.
      (b) Count chips (the `{type}` rendered in the chips section): replace `{type}` with `{tEvt.has(type) ? tEvt(type) : type}`.
      (c) Per-article event_type line: replace `{event.event_type}` with `{tEvt.has(event.event_type) ? tEvt(event.event_type) : event.event_type}`.
      (Both display sites must use the fallback; the count-chips KEY stays `type` for React keys — only the displayed text is translated.)

    hypotheses/page.tsx:
      (a) Add `const tStatus = await getTranslations('HypothesisStatus');` `const tType = await getTranslations('HypothesisType');` `const tConf = await getTranslations('ConfidenceLevel');`.
      (b) Status badge: `{hypothesis.status || t("statusNew")}` → if status present: `{hypothesis.status ? (tStatus.has(hypothesis.status) ? tStatus(hypothesis.status) : hypothesis.status) : t("statusNew")}`.
      (c) Type label: `{hypothesis.hypothesis_type || t("typeOther")}` → `{hypothesis.hypothesis_type ? (tType.has(hypothesis.hypothesis_type) ? tType(hypothesis.hypothesis_type) : hypothesis.hypothesis_type) : t("typeOther")}`.
      (d) Confidence: `{hypothesis.confidence_level || tShared("na")}` → `{hypothesis.confidence_level ? (tConf.has(hypothesis.confidence_level) ? tConf(hypothesis.confidence_level) : hypothesis.confidence_level) : tShared("na")}`.

    The `tone(status)` switch and DB column values stay untouched (logic uses raw enum keys — only DISPLAY is translated). Do NOT change the `reviewHypothesis` form button `value=` attributes (those are DB writes).

    After edits: `cd viewer && npx tsc --noEmit` exits 0. No new fetch (FND-02).
  </action>
  <acceptance_criteria>
    - timeline/page.tsx contains getTranslations('TimelineEventType') and a `.has(` fallback at both event_type display sites.
    - hypotheses/page.tsx contains getTranslations for all 3 namespaces and `.has(` fallback at status/type/confidence.
    - tone() switch and form button values unchanged.
    - `cd viewer && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd viewer && grep -q "getTranslations('TimelineEventType')" "app/[locale]/timeline/page.tsx" && grep -c "\.has(" "app/[locale]/timeline/page.tsx" | awk '{exit ($1>=2?0:1)}' && grep -q "getTranslations('HypothesisStatus')" "app/[locale]/hypotheses/page.tsx" && grep -q "getTranslations('HypothesisType')" "app/[locale]/hypotheses/page.tsx" && grep -q "getTranslations('ConfidenceLevel')" "app/[locale]/hypotheses/page.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>Both pages translate known enum values and fall back to raw string for unknown ones; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build + missing-key sweep + Phase 6/5/4 regression</name>
  <files>(no source files modified — verification only)</files>
  <read_first>
    - scripts/verify_phase6.py (check_i18n_03)
    - viewer/messages/en.json + ka.json
  </read_first>
  <action>
    (a) `cd viewer && npm run build` MUST exit 0. (Unknown-enum fallback means no runtime crash even if a value is unmapped.)
    (b) Phase 6 invariant (D-06): `python -m scripts.verify_phase6 --mode code-complete` 11/11 PASS (leaf-parity holds with 4 new namespaces).
    (c) Regression (D-06): verify_phase4 9/9 + verify_phase5 13/13 stay GREEN.
    (d) Manual sanity (record in SUMMARY, not gating): load /ka/timeline + /ka/hypotheses locally, confirm Georgian enum labels render and that injecting a fabricated unknown event_type still renders (raw string, no crash).
  </action>
  <acceptance_criteria>
    - `cd viewer && npm run build` exits 0.
    - `python -m scripts.verify_phase6 --mode code-complete` exits 0, 11/11 PASS.
    - verify_phase4 9/9 + verify_phase5 13/13 GREEN.
  </acceptance_criteria>
  <verify>
    <automated>cd viewer && npm run build 2>&1 | grep -qE "[Cc]ompiled successfully" && cd .. && python -m scripts.verify_phase6 --mode code-complete && python -m scripts.verify_phase5 --mode code-complete && python -m scripts.verify_phase4 --mode code-complete</automated>
  </verify>
  <done>Build clean; Phase 6 11/11 + Phase 5 13/13 + Phase 4 9/9 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer build → messages/{locale}.json | static enum labels bundled at build time |
| DB enum value → JSX render | untrusted free-form event_type may not match a namespace key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-02 | Denial of Service (low) | page crash on unmapped enum value | mitigate | D-03 fallback rule — `t.has(v) ? t(v) : v` guarantees an unmapped value renders the raw string instead of throwing. Verified by Task 2 grep + Task 3 manual sanity. |
| T-06.1-02b | Information Disclosure (low) | enum namespace keys missing in one dictionary | mitigate | Task 1 parity assertion + check_i18n_03 leaf-count gate. |
</threat_model>

<verification>
- `cd viewer && npm run build` exits 0
- `python -m scripts.verify_phase6 --mode code-complete` 11/11 PASS
- verify_phase4 9/9 + verify_phase5 13/13 GREEN
</verification>

<success_criteria>
- 4 enum namespaces present in both dictionaries, key-sets equal
- timeline + hypotheses pages translate known enum values, fall back to raw for unknown
- No page crash on an unmapped enum value
- Phase 6 11/11 + Phase 5 13/13 + Phase 4 9/9 stay GREEN
</success_criteria>

<output>
Create `.planning/phases/06.1-bilingual-polish-i18n-gap-closure/06.1-02-SUMMARY.md` when done. Record:
- Final leaf-count per namespace (en == ka)
- Which enum values were observed live vs added speculatively (canonical vs lowercase)
- Manual /ka render sanity result + unknown-value fallback confirmation
- verify_phase6 / verify_phase5 / verify_phase4 result lines
</output>
