---
phase: 06.1-bilingual-polish-i18n-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - viewer/messages/en.json
  - viewer/messages/ka.json
  - viewer/components/layout/BrainPanel.tsx
  - viewer/components/BrainPanel/ActivityFeed.tsx
  - viewer/components/BrainPanel/EmailIntent.tsx
  - viewer/components/BrainPanel/InputBar.tsx
  - viewer/components/BrainPanel/VoiceRecorder.tsx
requirements:
  - I18N-12
autonomous: true
must_haves:
  truths:
    - "On /ka the BRAIN panel header reads ტვინი (not BRAIN) and the status pill reads აქტიური (not Active)"
    - "On /ka the activity feed summary lines, relative-time strings, and EmailIntent states render Georgian (these are the mounted components). InputBar/VoiceRecorder are dormant (not mounted in BrainPanel.tsx) — their code is translated but not rendered, so verification is code-level (tsc + grep), not live-render"
    - "Every t('Manager.key') / t('key', {args}) call across the 5 BRAIN components resolves in BOTH en.json and ka.json (verify_phase6 check_i18n_03 leaf-count parity holds)"
    - "summarize()/relativeTime() interpolation uses ICU args ({id},{title},{date},{name},{description},{n}) — no string concatenation of translated fragments"
    - "No new remote fetch to non-self origins introduced (FND-02 trust boundary lint stays green)"
  artifacts:
    - path: viewer/messages/en.json
      provides: "Manager namespace (~28 keys) — English source strings"
      contains: "\"Manager\""
    - path: viewer/messages/ka.json
      provides: "Manager namespace (~28 keys) — idiomatic Georgian, key-set equal to en.json"
      contains: "\"Manager\""
    - path: viewer/components/layout/BrainPanel.tsx
      provides: "async server component using await getTranslations('Manager')"
      contains: "getTranslations('Manager')"
    - path: viewer/components/BrainPanel/ActivityFeed.tsx
      provides: "client component using useTranslations('Manager') for summarize()/relativeTime()"
      contains: "useTranslations('Manager')"
  key_links:
    - from: viewer/components/BrainPanel/ActivityFeed.tsx
      to: viewer/messages/{en,ka}.json
      via: "useTranslations('Manager') + t('summarize.*', {args})"
      pattern: "useTranslations\\(['\\\"]Manager['\\\"]\\)"
    - from: viewer/components/layout/BrainPanel.tsx
      to: viewer/messages/{en,ka}.json
      via: "await getTranslations('Manager')"
      pattern: "getTranslations\\(['\\\"]Manager['\\\"]\\)"
---

<objective>
Close I18N-12 (CONTEXT.md D-02): translate the BRAIN panel into Georgian via a new `Manager` namespace. Add ~28 keys to both viewer/messages/en.json and ka.json (key-set equal), then wire all 5 BRAIN components — BrainPanel.tsx becomes an async server component using `await getTranslations('Manager')`; ActivityFeed/EmailIntent/InputBar/VoiceRecorder use `useTranslations('Manager')`. summarize() and relativeTime() use ICU message args for interpolation (D-02). InputBar/VoiceRecorder are dormant but translated anyway (D-02 "cheap and may be re-mounted").

Purpose: On /ka, the entire BRAIN panel reads in Georgian — the last large block of hardcoded English chrome surfaced by live /ka inspection.
Output: 1 new namespace × 2 dictionaries + 5 wired components; verify_phase6 check_i18n_03 stays GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-bilingual-polish-i18n-gap-closure/06.1-CONTEXT.md
@viewer/messages/en.json
@viewer/messages/ka.json
@viewer/components/layout/BrainPanel.tsx
@viewer/components/BrainPanel/ActivityFeed.tsx
@viewer/components/BrainPanel/EmailIntent.tsx
@viewer/components/BrainPanel/InputBar.tsx
@viewer/components/BrainPanel/VoiceRecorder.tsx
@viewer/components/LanguageSwitcher.tsx
@viewer/AGENTS.md

<interfaces>
<!-- next-intl 4 — server vs client translation hooks (THIS Next.js is non-standard; read node_modules/next/dist/docs/ first per viewer/AGENTS.md) -->
import {getTranslations} from 'next-intl/server';   // server components — await it
const t = await getTranslations('Manager');
import {useTranslations} from 'next-intl';           // client components ('use client')
const t = useTranslations('Manager');
<!-- ICU interpolation: t('addedAppointment', {title: 'X', date: '2026-...'}) reads "Added appointment '{title}' ({date})" -->
<!-- NextIntlClientProvider is ALREADY mounted in viewer/app/[locale]/layout.tsx (06-03b) so client hooks have context -->

<!-- ActivityFeed.tsx current English literals to translate (from source):
  panel title "BRAIN", status "Active", section "Activity Log"
  summarize(): "Undid action {id}…", "Added appointment '{title}' ({date})",
    "Logged '{title}'", "New therapy candidate: {name}", "Appended note to therapy",
    "Dismissed an intake drop", "Pattern: {description}", "{action_type} on {target_table}" (fallback)
  relativeTime(): "just now", "{n}m ago", "{n}h ago"
  feed states: "Activity feed unreachable: {error}", "No recent activity. Drop a file or speak a note to start."
  EmailIntent: placeholder "Write to … about …", "Drafting…", "Draft email",
    "✓ Gmail draft staged for {contact}", "Open Gmail → Drafts to review and send. Never auto-sent.",
    "Draft blocked: {reason}", contact fallback "contact"
  InputBar: placeholder "Ask BRAIN or drop file...", "Attach"
  VoiceRecorder: "Recording…", "Transcribing…", "Retry", "Hold to talk"
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the Manager namespace (~28 keys) to en.json AND ka.json with idiomatic Georgian, key-sets equal</name>
  <files>viewer/messages/en.json, viewer/messages/ka.json</files>
  <read_first>
    - viewer/messages/en.json (existing 11 namespaces — Manager must be a NEW top-level key, alphabetically between "Knowledge" and "Navigation"; do not collide)
    - viewer/messages/ka.json (mirror structure — confirm same 11 namespaces present)
    - viewer/components/BrainPanel/ActivityFeed.tsx + EmailIntent.tsx + InputBar.tsx + VoiceRecorder.tsx + layout/BrainPanel.tsx (exact source strings — already enumerated in <interfaces>)
    - .planning/phases/06.1-bilingual-polish-i18n-gap-closure/06.1-CONTEXT.md (D-02 — ICU args required; D-05 banned imperative verbs do NOT apply to UI chrome but keep Georgian natural)
  </read_first>
  <action>
    Add a new top-level "Manager" namespace to BOTH viewer/messages/en.json and viewer/messages/ka.json. The key-set MUST be byte-for-byte identical between the two files (verify_phase6 check_i18n_03 enforces leaf-count parity — any asymmetry fails the gate).

    Required keys (~28), grouped (use flat or nested consistently in BOTH files; nested shown for readability — if nested, both files nest identically):

    Panel chrome:
      title            en "BRAIN"            ka "ტვინი"
      active           en "Active"           ka "აქტიური"
      activityLog      en "Activity Log"     ka "აქტივობის ჟურნალი"
      activityUnreachable en "Activity feed unreachable: {error}" ka "აქტივობის არხი მიუწვდომელია: {error}"
      noActivity       en "No recent activity. Drop a file or speak a note to start."
                       ka "ბოლოდროინდელი აქტივობა არ არის. დასაწყებად ატვირთეთ ფაილი ან ჩაწერეთ ხმოვანი შენიშვნა."

    summarize() templates (ICU args — D-02):
      summarize.undidAction      en "Undid action {id}…"                       ka "მოქმედება გაუქმდა {id}…"
      summarize.addedAppointment en "Added appointment '{title}' ({date})"     ka "დაემატა ვიზიტი „{title}“ ({date})"
      summarize.logged           en "Logged '{title}'"                          ka "დაფიქსირდა „{title}“"
      summarize.newTherapy       en "New therapy candidate: {name}"             ka "ახალი თერაპიის კანდიდატი: {name}"
      summarize.appendedNote     en "Appended note to therapy"                  ka "თერაპიას დაემატა შენიშვნა"
      summarize.dismissed        en "Dismissed an intake drop"                  ka "მიღების ჩანაწერი უარყოფილია"
      summarize.pattern          en "Pattern: {description}"                    ka "შაბლონი: {description}"
      summarize.fallback         en "{action} on {table}"                       ka "{action} — {table}"

    relativeTime() (ICU plural-friendly — use {n}):
      relativeTime.justNow       en "just now"        ka "ახლახ"
      relativeTime.minutesAgo    en "{n}m ago"        ka "{n} წთ წინ"
      relativeTime.hoursAgo      en "{n}h ago"        ka "{n} სთ წინ"

    EmailIntent:
      email.writeToPlaceholder   en "Write to … about …"                       ka "მისწერე … შესახებ …"
      email.drafting             en "Drafting…"                                 ka "მზადდება…"
      email.draftEmail           en "Draft email"                               ka "წერილის მონახაზი"
      email.draftStaged          en "✓ Gmail draft staged for {contact}"        ka "✓ Gmail-ის მონახაზი მზადაა {contact}-თვის"
      email.openGmailNote        en "Open Gmail → Drafts to review and send. Never auto-sent."
                                 ka "გახსენით Gmail → Drafts გადასახედად და გასაგზავნად. ავტომატურად არასოდეს იგზავნება."
      email.draftBlocked         en "Draft blocked: {reason}"                   ka "მონახაზი დაიბლოკა: {reason}"
      email.contactFallback      en "contact"                                   ka "კონტაქტი"

    InputBar:
      input.askPlaceholder       en "Ask BRAIN or drop file..."                 ka "ჰკითხეთ ტვინს ან ატვირთეთ ფაილი..."
      input.attach               en "Attach"                                    ka "მიმაგრება"

    VoiceRecorder:
      voice.recording            en "Recording…"                               ka "მიმდინარეობს ჩაწერა…"
      voice.transcribing         en "Transcribing…"                            ka "ტრანსკრიფცია…"
      voice.retry                en "Retry"                                     ka "ხელახლა"
      voice.holdToTalk           en "Hold to talk"                             ka "დააჭირეთ სალაპარაკოდ"

    Keep valid JSON (UTF-8, no trailing commas). After editing, run a parity assertion (Task verify) to prove leaf-counts match.

    NOTE: this plan and 06.1-02 both edit these two files. 06.1-02 declares depends_on:[06.1-01] so execute-phase serializes the writes. Touch ONLY the new "Manager" key — do not reorder or reformat existing namespaces (minimizes the merge surface).
  </action>
  <acceptance_criteria>
    - en.json and ka.json each parse as valid JSON.
    - Both contain a top-level "Manager" namespace.
    - Manager leaf-key count is identical between en.json and ka.json (~28 each).
    - ka.json Manager values are Georgian (Mkhedruli), not copies of the English.
  </acceptance_criteria>
  <verify>
    <automated>cd viewer && node -e "const en=require('./messages/en.json'),ka=require('./messages/ka.json');const leaves=o=>Object.values(o).flatMap(v=>typeof v==='object'?leaves(v):[1]).length;if(!en.Manager||!ka.Manager)throw new Error('Manager namespace missing');const a=leaves(en.Manager),b=leaves(ka.Manager);if(a!==b)throw new Error('Manager leaf mismatch en='+a+' ka='+b);if(JSON.stringify(en.Manager)===JSON.stringify(ka.Manager))throw new Error('ka identical to en — not translated');console.log('OK Manager parity',a)"</automated>
  </verify>
  <done>Manager namespace added to both dictionaries, key-sets equal, Georgian distinct from English.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire the 5 BRAIN components to the Manager namespace (server async + 4 client hooks, ICU args)</name>
  <files>viewer/components/layout/BrainPanel.tsx, viewer/components/BrainPanel/ActivityFeed.tsx, viewer/components/BrainPanel/EmailIntent.tsx, viewer/components/BrainPanel/InputBar.tsx, viewer/components/BrainPanel/VoiceRecorder.tsx</files>
  <read_first>
    - viewer/components/layout/BrainPanel.tsx (currently a sync default-export server component — convert to async)
    - viewer/components/BrainPanel/ActivityFeed.tsx (summarize()/relativeTime() return raw strings — convert to t() with ICU args; both functions must move inside the component or take t as a param so they can call the hook output)
    - viewer/components/BrainPanel/{EmailIntent,InputBar,VoiceRecorder}.tsx (all already 'use client')
    - viewer/components/LanguageSwitcher.tsx (canonical 'use client' + next-intl import pattern)
    - viewer/AGENTS.md → read node_modules/next/dist/docs/ before writing (this Next.js has breaking changes)
  </read_first>
  <action>
    (a) BrainPanel.tsx (server component): add `import {getTranslations} from 'next-intl/server';`, make the default export `async function`, add `const t = await getTranslations('Manager');`. Replace `BRAIN`→`{t('title')}`, `Active`→`{t('active')}`, `Activity Log`→`{t('activityLog')}`.

    (b) ActivityFeed.tsx ('use client'): add `import {useTranslations} from 'next-intl';`, inside the component add `const t = useTranslations('Manager');`. Refactor `summarize(row)` and `relativeTime(iso)` so they receive `t` (pass as arg, e.g. `summarize(row, t)`, or inline them into the component body). Map each branch to an ICU call:
        reverse        → t('summarize.undidAction', {id: String(row.target_record_id ?? '').slice(0,8)})
        add_event      → t('summarize.addedAppointment', {title: String(ap?.title ?? 'event'), date: String(ap?.event_date ?? '?')})
        add_milestone  → t('summarize.logged', {title: String(ap?.title ?? 'observation')})
        create+therapies → t('summarize.newTherapy', {name: String(ap?.name ?? '?')})
        update+therapies → t('summarize.appendedNote')
        dismiss        → t('summarize.dismissed')
        log_pattern    → t('summarize.pattern', {description: String(ap?.description ?? '(no description)')})
        default        → t('summarize.fallback', {action: row.action_type, table: row.target_table})
      relativeTime: <60s → t('relativeTime.justNow'); <1h → t('relativeTime.minutesAgo', {n: Math.floor(ms/60000)}); <24h → t('relativeTime.hoursAgo', {n: Math.floor(ms/3600000)}); else keep toLocaleDateString().
      Feed states: error → t('activityUnreachable', {error}); empty → t('noActivity').

    (c) EmailIntent.tsx: add `useTranslations('Manager')`. placeholder→t('email.writeToPlaceholder'); button "Drafting…"/"Draft email"→t('email.drafting')/t('email.draftEmail'); staged header→t('email.draftStaged', {contact: result.contact_name ?? t('email.contactFallback')}); openGmailNote→t('email.openGmailNote'); blocked→t('email.draftBlocked', {reason: result.block_reason ?? 'unknown'}).

    (d) InputBar.tsx: add `useTranslations('Manager')`. placeholder→t('input.askPlaceholder'); "Attach"→t('input.attach').

    (e) VoiceRecorder.tsx: add `useTranslations('Manager')`. The `label` ternary → t('voice.recording')/t('voice.transcribing')/t('voice.retry')/t('voice.holdToTalk').

    After all edits: `cd viewer && npx tsc --noEmit` MUST exit 0. Remove any now-unused imports. Do NOT introduce any new fetch/axios call (FND-02).
  </action>
  <acceptance_criteria>
    - BrainPanel.tsx is async and contains getTranslations('Manager').
    - ActivityFeed/EmailIntent/InputBar/VoiceRecorder each contain useTranslations('Manager').
    - No hardcoded English literal from the <interfaces> list remains in JSX render output.
    - summarize()/relativeTime() use t('...', {args}) — no `+` concatenation of translated fragments.
    - `cd viewer && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd viewer && grep -q "getTranslations('Manager')" components/layout/BrainPanel.tsx && for f in ActivityFeed EmailIntent InputBar VoiceRecorder; do grep -q "useTranslations('Manager')" "components/BrainPanel/$f.tsx" || { echo "MISS $f"; exit 1; }; done && npx tsc --noEmit</automated>
  </verify>
  <done>All 5 BRAIN components consume the Manager namespace; tsc clean; ICU interpolation in place.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build + missing-key sweep + Phase 6/5/4 regression</name>
  <files>(no source files modified — verification only)</files>
  <read_first>
    - scripts/verify_phase6.py (check_i18n_03 leaf-count parity)
    - viewer/messages/en.json + ka.json
  </read_first>
  <action>
    (a) Missing-key sweep over the BRAIN components: extract every t('...') / useTranslations('Manager') reference and assert each resolves in BOTH en.json and ka.json under Manager. (Reuse the inline Python verifier pattern from 06-05b Task 3, scoped to viewer/components/BrainPanel/ + viewer/components/layout/BrainPanel.tsx.)

    (b) `cd viewer && npm run build` MUST exit 0.

    (c) Phase 6 invariant (D-06): `python -m scripts.verify_phase6 --mode code-complete` MUST stay 11/11 PASS (check_i18n_03 leaf-parity holds with the new Manager namespace).

    (d) Regression (D-06): `python -m scripts.verify_phase4 --mode code-complete` 9/9 + `python -m scripts.verify_phase5 --mode code-complete` 13/13 stay GREEN (this plan touches only viewer/ — no backend regression expected, but confirm).
  </action>
  <acceptance_criteria>
    - Inline missing-key verifier prints OK (all Manager refs resolve in both dictionaries).
    - `cd viewer && npm run build` exits 0.
    - `python -m scripts.verify_phase6 --mode code-complete` exits 0, 11/11 PASS.
    - verify_phase4 9/9 + verify_phase5 13/13 stay GREEN.
  </acceptance_criteria>
  <verify>
    <automated>cd viewer && npm run build 2>&1 | grep -qE "[Cc]ompiled successfully" && cd .. && python -m scripts.verify_phase6 --mode code-complete && python -m scripts.verify_phase5 --mode code-complete && python -m scripts.verify_phase4 --mode code-complete</automated>
  </verify>
  <done>Build clean; Manager keys all resolve; Phase 6 11/11 + Phase 5 13/13 + Phase 4 9/9 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer build → messages/{locale}.json | static strings bundled at build time |
| viewer/ → external origins | FND-02 lint — this plan touches JSX + i18n only; no new fetch sites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-01 | Information Disclosure (low) | BRAIN components emitting unresolvable Manager keys | mitigate | Task 3 missing-key sweep asserts every t('Manager.*') resolves in both dictionaries before merge. |
| T-06.1-FND-02 (carry-over) | Information Disclosure | viewer/ remote-origin fetches | mitigate | Edits are JSX text + hook imports; no new fetch call site. FND-02 lint regression continues to pass. |
</threat_model>

<verification>
- `cd viewer && npm run build` exits 0
- Inline Manager missing-key verifier prints OK
- `python -m scripts.verify_phase6 --mode code-complete` 11/11 PASS
- verify_phase4 9/9 + verify_phase5 13/13 GREEN
</verification>

<success_criteria>
- Manager namespace (~28 keys) present in both dictionaries, key-sets equal
- All 5 BRAIN components consume Manager via getTranslations / useTranslations
- /ka BRAIN panel fully Georgian; ICU args used for interpolation
- Phase 6 11/11 + Phase 5 13/13 + Phase 4 9/9 stay GREEN
</success_criteria>

<output>
Create `.planning/phases/06.1-bilingual-polish-i18n-gap-closure/06.1-01-SUMMARY.md` when done. Record:
- Final Manager leaf-key count (en == ka)
- Whether summarize()/relativeTime() were inlined or passed t as arg (and why)
- Missing-key verifier output
- verify_phase6 / verify_phase5 / verify_phase4 result lines
</output>
