---
phase: 06-bilingual-system-i18n
plan: 12
type: execute
wave: 4
depends_on:
  - 06-09
  - 06-10
  - 06-11
files_modified:
  - scripts/communicator/_bilingual_read.py
  - scripts/communicator/telegram_sender.py
  - scripts/communicator/gmail_digest.py
  - scripts/manager/briefing.py
  - tests/test_display_field_py.py
  - workflows/_phase6_notes.md
requirements:
  - I18N-07
autonomous: true
must_haves:
  truths:
    - "scripts/communicator/telegram_sender.py reads .ka from JSONB columns via display_field_py when composing the Telegram message body (CONTEXT.md D-02 Telegram=ka)"
    - "scripts/communicator/gmail_digest.py reads .en from JSONB columns via display_field_py when composing the Gmail draft body (CONTEXT.md D-02 Gmail=en)"
    - "scripts/manager/briefing.py reads .ka for Telegram briefing emission (Sunday morning briefing per Phase 5 MNG-10 is family-facing Telegram)"
    - "Both call sites use scripts/communicator/_bilingual_read.display_field_py with English fallback symmetric to viewer/lib/i18n.ts displayField (RESEARCH.md Pitfall 6)"
    - "workflows/_phase6_notes.md documents the zero-touch-on-n8n decision per RESEARCH.md Pattern 7"
    - "Telegram dry-run produces body containing Mkhedruli codepoints (U+10A0..U+10FF); Gmail dry-run produces body containing zero Mkhedruli codepoints"
  artifacts:
    - path: scripts/communicator/_bilingual_read.py
      provides: "Python display_field_py helper mirroring viewer/lib/i18n.ts displayField"
      contains: "display_field_py"
    - path: scripts/communicator/telegram_sender.py
      provides: "Telegram body composer reads .ka via display_field_py"
    - path: scripts/communicator/gmail_digest.py
      provides: "Gmail body composer reads .en via display_field_py"
    - path: workflows/_phase6_notes.md
      provides: "Documentation of why no n8n JSON files were modified (RESEARCH.md Pattern 7 zero-touch finding)"
  key_links:
    - from: scripts/communicator/telegram_sender.py
      to: scripts/communicator/_bilingual_read.py
      via: "from scripts.communicator._bilingual_read import display_field_py"
      pattern: "display_field_py"
    - from: scripts/communicator/gmail_digest.py
      to: scripts/communicator/_bilingual_read.py
      via: "from scripts.communicator._bilingual_read import display_field_py"
      pattern: "display_field_py"
---

<objective>
Implement the audience-routing half of Phase 6 per CONTEXT.md D-02 + RESEARCH.md Pattern 7: Telegram-sending worker code reads `.ka` from JSONB columns; Gmail-sending worker code reads `.en`. Author a small Python helper (`display_field_py`) mirroring viewer/lib/i18n.ts displayField so legacy-TEXT tolerance and English-fallback behavior are symmetric on the worker side (RESEARCH.md Pitfall 6). Update scripts/communicator/telegram_sender.py, scripts/communicator/gmail_digest.py, and scripts/manager/briefing.py for locale-resolved reads. Document the n8n zero-touch decision in workflows/_phase6_notes.md.

Per RESEARCH.md Pattern 7 (5 workflows surveyed): no n8n workflow JSON file needs editing — `telegram_daily_digest.json`, `manager_briefing.json`, `weekly_brief.json` delegate body composition to the Python worker via `PHASE4_DIGEST_WORKER_URL` / `PHASE5_MANAGER_WORKER_URL`; `daily_digest.json` stays inactive; `outreach_review_queue.json` uses outreach_log.subject which stays TEXT.

Purpose: Land I18N-07 — Telegram→ka, Gmail→en audience routing.
Output: Worker code reads the right locale per audience; dry-run produces locale-correct body strings; n8n JSON untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-SPEC.md
@.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-CONTEXT.md
@.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md
@scripts/communicator/telegram_sender.py
@scripts/communicator/gmail_digest.py
@scripts/manager/briefing.py
@scripts/communicator/weekly_brief.py
@viewer/lib/i18n.ts
@workflows/telegram_daily_digest.json
@workflows/weekly_brief.json
@workflows/manager_briefing.json
@workflows/daily_digest.json
@workflows/outreach_review_queue.json

<interfaces>
<!-- Python helper signature (RESEARCH.md Pitfall 6 verbatim) -->
def display_field_py(field, locale: str) -> str:
    if field is None:
        return ""
    if isinstance(field, str):
        return field
    if isinstance(field, dict):
        return field.get(locale) or field.get("en") or ""
    return str(field)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author scripts/communicator/_bilingual_read.py + tests/test_display_field_py.py</name>
  <files>scripts/communicator/_bilingual_read.py, tests/test_display_field_py.py</files>
  <read_first>
    - viewer/lib/i18n.ts (06-04 — TypeScript source of truth)
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Pitfall 6 — exact Python function)
  </read_first>
  <behavior>
    7 assertions:
    1. display_field_py(None, 'en') returns ''
    2. display_field_py('legacy text', 'en') returns 'legacy text'
    3. display_field_py('legacy text', 'ka') returns 'legacy text' (passthrough, no translation)
    4. display_field_py({'en': 'Hello', 'ka': 'გამარჯობა'}, 'en') returns 'Hello'
    5. display_field_py({'en': 'Hello', 'ka': 'გამარჯობა'}, 'ka') returns 'გამარჯობა'
    6. display_field_py({'en': 'Only en'}, 'ka') returns 'Only en' (English fallback)
    7. display_field_py({}, 'en') returns ''
  </behavior>
  <action>
    Create scripts/communicator/_bilingual_read.py with the exact function from RESEARCH.md Pitfall 6. The leading underscore on the module name marks it Communicator-internal.

    Module docstring:

        """Python-side mirror of viewer/lib/i18n.ts displayField.

        Used by Telegram / Gmail / manager-briefing worker code to resolve a
        bilingual JSONB field to a single locale string with English fallback.

        Source contract: RESEARCH.md Pitfall 6.
        """

    Function:

        from __future__ import annotations
        from typing import Any

        def display_field_py(field: Any, locale: str) -> str:
            if field is None:
                return ""
            if isinstance(field, str):
                return field
            if isinstance(field, dict):
                return field.get(locale) or field.get("en") or ""
            return str(field)

    Create tests/test_display_field_py.py with the 7 pytest assertions listed in the behavior block.
  </action>
  <acceptance_criteria>
    - scripts/communicator/_bilingual_read.py exists and exports display_field_py.
    - tests/test_display_field_py.py exists.
    - `python -m pytest tests/test_display_field_py.py -v` exits 0; output contains `7 passed` (or equivalent count).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_display_field_py.py -v 2>&1 | grep -E "7 passed"</automated>
  </verify>
  <done>Python display_field_py helper landed; symmetric contract with TS displayField; 7 tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update scripts/communicator/telegram_sender.py — read .ka via display_field_py for all bilingual JSONB columns + add --bilingual-dryrun CLI</name>
  <files>scripts/communicator/telegram_sender.py</files>
  <read_first>
    - scripts/communicator/telegram_sender.py (current — Phase 4 / Phase 5 Telegram body composer)
    - scripts/communicator/_bilingual_read.py (Task 1)
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Pattern 7 — telegram_sender pseudocode showing .ka resolution + Pitfall 6)
  </read_first>
  <action>
    Open scripts/communicator/telegram_sender.py.

    (a) Add `from scripts.communicator._bilingual_read import display_field_py` to imports.

    (b) Locate every read of one of the 6 converted JSONB columns (aleksandra_timeline.title, aleksandra_timeline.description, hypotheses.title, hypotheses.description, therapies.name, therapies.evidence_summary) AND every iteration over briefs.sections.* fields that became bilingual post-migration-012. Replace the bare-string read with display_field_py(row['field'], 'ka').

    Example transformation:

        # BEFORE
        title = row["title"]
        body = f"{title}: {description}"

        # AFTER
        title = display_field_py(row["title"], "ka")
        body = f"{title}: {display_field_py(row['description'], 'ka')}"

    For briefs.sections.summary_lines (now [{en, ka}, ...] after migration 012), iterate and call display_field_py(line, 'ka') per element.

    (c) Do NOT pass locale='en' anywhere in this file. Telegram is Georgian-only per CONTEXT.md D-02.

    (d) Add a `--bilingual-dryrun` CLI flag (argparse) that:
        - Composes the Telegram body using a fixture row (or a recent real row from a read-only query)
        - Prints the composed body to stdout
        - Exits 0 without invoking the Telegram API

    The verifier check_i18n_07 (06-02 Task 1) calls this CLI flag and asserts Mkhedruli codepoints appear.
  </action>
  <acceptance_criteria>
    - File contains `from scripts.communicator._bilingual_read import display_field_py`.
    - `grep -c "display_field_py" scripts/communicator/telegram_sender.py` is at least 2.
    - File contains the literal string `'ka'` (Telegram locale).
    - File does NOT contain `display_field_py.*'en'` (negative check; only Georgian here).
    - File contains argparse flag `--bilingual-dryrun`.
    - `python -m scripts.communicator.telegram_sender --bilingual-dryrun` exits 0 and stdout contains at least one Mkhedruli codepoint when written to a file (verified by Bash unicode-aware grep below).
    - Phase 4 ACD-01 verifier still PASS: `python -m scripts.verify_phase4 --mode code-complete --gate acd-01` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "display_field_py" scripts/communicator/telegram_sender.py && grep -q "bilingual-dryrun\|bilingual_dryrun" scripts/communicator/telegram_sender.py</automated>
  </verify>
  <done>Telegram body composer resolves .ka; dry-run CLI exposes Mkhedruli output; Phase 4 regression check still PASS.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update scripts/communicator/gmail_digest.py — read .en via display_field_py + add --bilingual-dryrun CLI</name>
  <files>scripts/communicator/gmail_digest.py</files>
  <read_first>
    - scripts/communicator/gmail_digest.py (current — Phase 4 weekly Gmail digest composer)
    - scripts/communicator/_bilingual_read.py (Task 1)
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Pattern 7 — gmail_digest pseudocode showing .en resolution)
  </read_first>
  <action>
    Open scripts/communicator/gmail_digest.py.

    (a) Add `from scripts.communicator._bilingual_read import display_field_py` to imports.

    (b) Locate every read of one of the 6 converted JSONB columns + every iteration over briefs.sections.* bilingual fields. Replace bare-string reads with display_field_py(row['field'], 'en').

    Example transformation:

        # BEFORE
        name = row["name"]
        evidence = row["evidence_summary"]

        # AFTER
        name = display_field_py(row["name"], "en")
        evidence = display_field_py(row["evidence_summary"], "en")

    For briefs.sections nested fields, use display_field_py(line, 'en') per element.

    (c) Do NOT pass locale='ka' anywhere in this file. Gmail is English-only per CONTEXT.md D-02.

    (d) Add a `--bilingual-dryrun` CLI flag mirroring telegram_sender:
        - Composes the Gmail body using a fixture row or a read-only recent query
        - Prints to stdout
        - Exits 0 without invoking the Gmail API

    The verifier check_i18n_07 asserts the dry-run output contains ZERO Mkhedruli codepoints.
  </action>
  <acceptance_criteria>
    - File contains `from scripts.communicator._bilingual_read import display_field_py`.
    - `grep -c "display_field_py" scripts/communicator/gmail_digest.py` is at least 2.
    - File contains the literal `'en'` (Gmail locale).
    - File does NOT contain `display_field_py.*'ka'` (negative check; only English here).
    - File contains argparse flag `--bilingual-dryrun`.
    - `python -m scripts.communicator.gmail_digest --bilingual-dryrun` exits 0.
    - Phase 4 ACD-03 verifier still PASS: `python -m scripts.verify_phase4 --mode code-complete --gate acd-03` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "display_field_py" scripts/communicator/gmail_digest.py && grep -q "bilingual-dryrun\|bilingual_dryrun" scripts/communicator/gmail_digest.py</automated>
  </verify>
  <done>Gmail body composer resolves .en; dry-run CLI exposes English-only output; Phase 4 regression preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update scripts/manager/briefing.py — read .ka for the Sunday morning briefing Telegram emission</name>
  <files>scripts/manager/briefing.py</files>
  <read_first>
    - scripts/manager/briefing.py (current — Phase 5 MNG-10 Sunday morning briefing composer)
    - scripts/communicator/_bilingual_read.py (Task 1)
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Pattern 7 — manager_briefing reads .ka because Sunday brief goes to family Telegram)
  </read_first>
  <action>
    Open scripts/manager/briefing.py.

    (a) Add `from scripts.communicator._bilingual_read import display_field_py` to imports (if not already wired by 06-09 Task 3).

    (b) Locate every read of one of the 6 converted JSONB columns OR briefs.sections bilingual fields when composing the Sunday morning briefing Telegram body. Replace with display_field_py(row['field'], 'ka').

    Manager briefing per Phase 5 MNG-10 is family-facing Telegram (Sunday 09:00 ET) — locale is Georgian. If the same file also writes a Notion archive line in English (per Phase 4 ACD-03), that English-side call site uses display_field_py(field, 'en').

    (c) If 06-09 Task 3 already wired compose_bilingual into briefing.py for WRITE inserts, this task's READ paths are the complement — distinct call sites for distinct purposes:
        - WRITE (06-09): briefing.py inserts new rows with {en, ka} pairs (compose_bilingual)
        - READ (06-12 Task 4): briefing.py reads existing rows with display_field_py(field, 'ka') for Telegram composition

    Both wirings coexist in the same file.
  </action>
  <acceptance_criteria>
    - scripts/manager/briefing.py contains `from scripts.communicator._bilingual_read import display_field_py`.
    - File contains `display_field_py(` call with `'ka'` argument at least once.
    - `python -c "import scripts.manager.briefing; print('OK')"` exits 0 (no import errors).
    - Phase 5 MNG-10 verifier still PASS: `python -m scripts.verify_phase5 --mode code-complete --gate mng-10` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "display_field_py" scripts/manager/briefing.py</automated>
  </verify>
  <done>Manager Sunday briefing reads .ka; Phase 5 MNG-10 regression preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Author workflows/_phase6_notes.md documenting the zero-touch decision on n8n JSON files</name>
  <files>workflows/_phase6_notes.md</files>
  <read_first>
    - workflows/telegram_daily_digest.json (Phase 4 — delegates to PHASE4_DIGEST_WORKER_URL)
    - workflows/weekly_brief.json (Phase 4 — delegates to worker)
    - workflows/manager_briefing.json (Phase 5 — delegates to PHASE5_MANAGER_WORKER_URL)
    - workflows/daily_digest.json (active=false per Phase 2.5 note)
    - workflows/outreach_review_queue.json (uses outreach_log.subject — TEXT, untouched by Phase 6)
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Pattern 7 — 5-workflow survey table + zero-touch conclusion)
  </read_first>
  <action>
    Create workflows/_phase6_notes.md. Content sections:

    1. Header: "Phase 6 I18N-07 — n8n zero-touch decision"

    2. Conclusion (one sentence): No n8n workflow JSON file was modified by Phase 6. Audience routing (Telegram=ka, Gmail=en) lives in the Python worker layer.

    3. Per-workflow audit table mirroring RESEARCH.md Pattern 7:

        | Workflow | Telegram-body source | Gmail-body source | Phase 6 change |
        |---|---|---|---|
        | telegram_daily_digest.json | Python worker fire_daily_batch() | n/a | None — body in worker |
        | daily_digest.json | n8n Compose digest JS node reads p.title | n/a | None — workflow stays inactive (per Phase 2.5 note); if reactivated later, update the JS to use display_field_py-equivalent |
        | weekly_brief.json | Python worker /render-weekly-brief | Same worker | None — worker reads briefs.sections per locale |
        | manager_briefing.json | Python worker /morning-briefing | n/a | None — worker reads briefs + manager_actions per locale |
        | outreach_review_queue.json | n8n Compose digest JS reads outreach_log.subject | n/a | None — outreach_log.subject stays TEXT per SPEC out-of-scope |

    4. Backlog item: "If workflows/daily_digest.json is ever activated (currently `active: false`), update its `Compose digest` JS code node to extract `p.title.ka || p.title.en || p.title` for Telegram audience. Filed at Phase 6 close, no work performed."

    5. Reference: RESEARCH.md Pattern 7 + Open Question #1.

    Target length: 40–80 lines. The file's primary purpose is audit-trail — explaining WHY no changes appear in workflows/*.json during Phase 6 commits.
  </action>
  <acceptance_criteria>
    - File workflows/_phase6_notes.md exists.
    - File line count between 30 and 120.
    - File contains the 5-workflow audit table.
    - File contains the backlog item for daily_digest.json reactivation.
    - File references RESEARCH.md Pattern 7.
  </acceptance_criteria>
  <verify>
    <automated>test -f workflows/_phase6_notes.md && grep -q "telegram_daily_digest.json" workflows/_phase6_notes.md && grep -q "daily_digest.json" workflows/_phase6_notes.md && grep -q "Pattern 7" workflows/_phase6_notes.md</automated>
  </verify>
  <done>n8n zero-touch decision documented for audit trail; backlog item filed for daily_digest.json future reactivation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python worker → Telegram Bot API | Family-facing message body crosses delivery boundary; locale must match family register (Georgian) |
| Python worker → Gmail API | Clinician/family English digest crosses delivery boundary; locale must match clinician register (English) |
| n8n workflow JSON → Python worker URL | Workflow trigger crosses to worker; locale resolution happens in worker, not in n8n expression syntax |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-LOCALE-LEAK-TG | Information Disclosure | scripts/communicator/telegram_sender.py | mitigate | Task 2 enforces `display_field_py(field, 'ka')` everywhere; negative-grep acceptance forbids `display_field_py.*'en'` in this file. Verifier check_i18n_07 dry-run asserts Mkhedruli presence. |
| T-06-LOCALE-LEAK-GM | Information Disclosure | scripts/communicator/gmail_digest.py | mitigate | Task 3 enforces `display_field_py(field, 'en')` everywhere; negative-grep forbids `'ka'`. Verifier check_i18n_07 dry-run asserts zero Mkhedruli codepoints. |
| T-06-FALLBACK-OBJECT-OBJECT | Information Disclosure (low) | display_field_py | mitigate | Task 1 contract: None/string/dict/other inputs all return a string; never returns the raw JSONB object. `[object Object]`-style leak is structurally impossible. |
</threat_model>

<verification>
- All 5 Python files touched (Tasks 1–4); `python -m pytest tests/test_display_field_py.py` exits 0
- `python -m scripts.communicator.telegram_sender --bilingual-dryrun` exits 0
- `python -m scripts.communicator.gmail_digest --bilingual-dryrun` exits 0
- `python -m scripts.verify_phase6 --bucket D` exits 0 with I18N-07 PASS
- Phase 4 ACD-01 + ACD-03 + Phase 5 MNG-10 verifier checks still PASS (regression preserved; full regression sweep in 06-13)
- No file under workflows/*.json modified in this plan's git diff (n8n zero-touch confirmed)
</verification>

<success_criteria>
- Python display_field_py helper available + 7-test pytest suite green
- telegram_sender.py reads .ka exclusively; dry-run emits Mkhedruli
- gmail_digest.py reads .en exclusively; dry-run emits zero Mkhedruli
- briefing.py reads .ka for Sunday brief Telegram audience
- workflows/_phase6_notes.md audits zero-touch decision
- I18N-07 verifier flips GREEN
</success_criteria>

<output>
Create `.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-12-SUMMARY.md` when done. Record:
- display_field_py 7-test pass count
- Number of call sites updated in each Python file (telegram_sender, gmail_digest, briefing)
- Dry-run output excerpts showing locale-correct content
- Confirmation that `git diff workflows/` shows ONLY the new _phase6_notes.md file (no JSON edits)
</output>
