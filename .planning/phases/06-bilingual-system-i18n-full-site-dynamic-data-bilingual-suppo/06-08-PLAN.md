---
phase: 06-bilingual-system-i18n
plan: 08
type: execute
wave: 2
depends_on:
  - 06-03b
  - 06-04
  - 06-07
files_modified:
  - viewer/app/api/manager/apply/route.ts
  - viewer/app/[locale]/timeline/page.tsx
  - viewer/app/[locale]/therapies/page.tsx
  - viewer/app/[locale]/hypotheses/page.tsx
  - viewer/app/[locale]/hypotheses/[id]/page.tsx
requirements:
  - I18N-05
  - I18N-08
autonomous: true
must_haves:
  truths:
    - "viewer/app/api/manager/apply/route.ts writes JSONB shape {en, ka} to aleksandra_timeline.title, aleksandra_timeline.description, hypotheses.title, hypotheses.description, therapies.name, therapies.evidence_summary — NOT bare TEXT (RESEARCH.md Pitfall 3)"
    - "Timeline, Therapies, Hypotheses viewer pages read JSONB columns via displayField(field, locale) from @/lib/i18n"
    - "Manager apply insert path mirrors `{en: input, ka: input}` for fields the manager UI captures as single-language (manager intake is English-text-typed today; bilingual emission only applies when LLM is involved — D-02 scoping)"
    - "Mixed-locale rows render correctly: a row with only {en: 'X'} (no ka key) renders 'X' on /ka/timeline (English fallback)"
    - "No new remote fetch/axios.post/XMLHttpRequest from viewer/ to non-self origins introduced by this plan (FND-02 trust boundary lint must continue to pass)"
  artifacts:
    - path: viewer/app/api/manager/apply/route.ts
      provides: "Updated INSERT path that writes JSONB {en, ka} shape for the 6 converted columns"
      contains: "displayField"
  key_links:
    - from: viewer/app/api/manager/apply/route.ts
      to: scripts/migrations/012_i18n_jsonb.sql
      via: "shape contract: post-migration columns expect jsonb_build_object('en', x, 'ka', x)"
      pattern: "\\{\\s*en"
    - from: viewer/app/[locale]/timeline/page.tsx
      to: viewer/lib/i18n.ts
      via: "import {displayField}"
      pattern: "from\\s+['\\\"]@/lib/i18n"
---

<objective>
Update the manager API insert path (viewer/app/api/manager/apply/route.ts) so the 6 columns that migration 012 converted to JSONB are inserted as `{en: x, ka: x}` objects instead of bare TEXT strings (RESEARCH.md Pitfall 3 + Open Question #2). Update the Timeline / Therapies / Hypotheses viewer pages to read those JSONB columns through `displayField(field, locale)` from `@/lib/i18n`. Manager intake is single-language English text today; mirroring `{en: input, ka: input}` at insert time preserves the migration-012 "ka = en" pattern until the manager UI eventually grows a Georgian-input affordance (deferred per CONTEXT.md Deferred Ideas).

Purpose: Close the Wave-2 reader/writer gap that Pitfall 3 calls out. Without this, the manager apply route breaks post-migration with `invalid input syntax for type json`.
Output: Manager apply writes JSONB; Timeline/Therapies/Hypotheses pages render via displayField; mixed-locale rendering works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-SPEC.md
@.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-CONTEXT.md
@.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md
@viewer/app/api/manager/apply/route.ts
@viewer/app/[locale]/timeline/page.tsx
@viewer/app/[locale]/therapies/page.tsx
@viewer/app/[locale]/hypotheses/page.tsx
@viewer/app/[locale]/hypotheses/[id]/page.tsx
@viewer/lib/i18n.ts
@scripts/migrations/012_i18n_jsonb.sql

<interfaces>
<!-- displayField from 06-04 Task 1 -->
import {displayField, type BilingualField} from '@/lib/i18n';
displayField(row.title, locale)  // returns string; handles {en, ka} | string | null

<!-- Shape now required for INSERTs into the 6 converted columns -->
type TitleField = {en: string; ka: string};
// e.g. {en: "Hello", ka: "Hello"} for manager-typed text (mirror)
// e.g. {en: "Hello", ka: "გამარჯობა"} for LLM-emitted text (Wave-3 compose_bilingual output)

<!-- Row-typing reflection in viewer pages -->
type TimelineEvent = {
  id: string;
  event_date: string;
  event_type: string;
  title: BilingualField;          // ← was `string`; now JSONB-or-legacy
  description: BilingualField;
  institution: string | null;     // unchanged
  location: string | null;        // unchanged
  ...
};
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit every INSERT/UPDATE call site touching the 6 converted columns; enumerate the shape changes</name>
  <files>(audit only — no code changes; produces an inline summary used by Tasks 2+3)</files>
  <read_first>
    - viewer/app/api/manager/apply/route.ts (manager-side write path)
    - scripts/communicator/weekly_brief.py (Phase 5 brief composer — writes briefs.sections — bilingual landing in Wave 3 / 06-09)
    - scripts/communicator/notion_archiver.py (sanity check — does it write Supabase rows or only Notion? per Phase 4 it's Notion-only)
    - scripts/manager/briefing.py (sanity check — does it write briefs? per RESEARCH.md it does)
    - agents/communicator.py (CrewAI wrapper — read for INSERT into hypotheses/therapies)
    - scripts/seed_aleksandra_data.py (seed fixture — if it INSERTs into the 4 tables, must shape-update too)
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Pitfall 3 — Open Question #2 specifically calls out viewer/app/api/manager/apply/route.ts)
  </read_first>
  <action>
    Grep across the codebase for every INSERT / UPDATE / Supabase JS .insert() / .update() call site touching one of the 4 target tables:

        grep -rEn "(INSERT INTO (aleksandra_timeline|hypotheses|therapies|briefs)|\\.from\\(['\\\"](aleksandra_timeline|hypotheses|therapies|briefs)['\\\"]\\)\\.insert|\\.from\\(['\\\"](aleksandra_timeline|hypotheses|therapies|briefs)['\\\"]\\)\\.update)" viewer/ scripts/ agents/

    Enumerate each call site and decide its disposition:

    Classification table:
    | Call site | Disposition | Reason |
    |---|---|---|
    | viewer/app/api/manager/apply/route.ts | UPDATE NOW (Task 2) | Manager apply writes timeline/hypotheses/therapies rows post-Phase-5; must use JSONB shape |
    | scripts/communicator/weekly_brief.py briefs INSERT | DEFER to 06-09 | Bilingual emission is Wave 3's job |
    | scripts/manager/briefing.py briefs INSERT (if any) | DEFER to 06-09 | Same |
    | agents/communicator.py hypotheses/therapies INSERT | DEFER to 06-09 | Same |
    | scripts/seed_aleksandra_data.py | UPDATE NOW or DEFER (depending on whether it is in active rotation per CLAUDE.md) | If active: shape-update with {en:x, ka:x} mirror; if only-run-once-seed and 012 already migrated the historical rows, skip |
    | scripts/migrate_papers.py / migrations/ | NO CHANGE | Migrations are static |

    Record the full enumeration in the executor's working notes (will end up in this plan's SUMMARY).

    Acceptance: Task 2 covers viewer/app/api/manager/apply/route.ts; Task 3 covers the viewer READ paths; Wave 3 plans 06-09/06-10 cover the deferred Python write paths.
  </action>
  <acceptance_criteria>
    - Grep output stored in plan SUMMARY (or working notes); count of call sites ≥ 2 (manager apply route + at least one Python write).
    - Every grep hit is classified into one of: UPDATE NOW, DEFER to 06-09, NO CHANGE.
    - Manager apply route classified as UPDATE NOW.
  </acceptance_criteria>
  <verify>
    <automated>grep -rEn "\.from\(['\\\"](aleksandra_timeline|hypotheses|therapies|briefs)['\\\"]\)\.insert" viewer/ | wc -l | awk '{exit ($1 >= 1 ? 0 : 1)}'</automated>
  </verify>
  <done>Audit complete; call sites classified; Tasks 2/3 know exact targets; Wave 3 plans receive the deferred-list.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update viewer/app/api/manager/apply/route.ts to write JSONB shape for the 6 converted columns</name>
  <files>viewer/app/api/manager/apply/route.ts</files>
  <read_first>
    - viewer/app/api/manager/apply/route.ts (current — likely uses Supabase JS client `.from('aleksandra_timeline').insert({title: x, ...})`)
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Pitfall 3 — the manager apply route is the canonical example given)
    - scripts/migrations/012_i18n_jsonb.sql (column shapes — title, description, name, evidence_summary are jsonb)
  </read_first>
  <action>
    Edit viewer/app/api/manager/apply/route.ts:

    (a) For every INSERT touching the 6 converted columns, wrap the user-typed text in `{en: x, ka: x}` mirror objects:

        // BEFORE
        await sb.from('aleksandra_timeline').insert({
          title: input.title,              // bare TEXT — post-012 this is a TYPE error
          description: input.description,
          institution: input.institution,
          location: input.location,
          event_date: input.event_date,
          event_type: input.event_type,
        });

        // AFTER
        await sb.from('aleksandra_timeline').insert({
          title: {en: input.title, ka: input.title},
          description: input.description == null
            ? null
            : {en: input.description, ka: input.description},
          institution: input.institution,   // unchanged — stays TEXT per SPEC out-of-scope
          location: input.location,         // unchanged
          event_date: input.event_date,
          event_type: input.event_type,
        });

    Apply the same pattern to every INSERT into hypotheses (title, description) and therapies (name, evidence_summary) found by Task 1's audit. Columns NOT in the 6-column conversion list (event_date, event_type, status, priority, etc.) STAY their existing types.

    (b) For UPDATE statements (manager apply is also undo-able via 24h window per Phase 5), apply the same wrapping when the updated value targets one of the 6 converted columns.

    (c) Add a tiny helper at the top of the file if there are 3+ call sites that benefit:

        function asBilingual(s: string | null | undefined): {en: string; ka: string} | null {
          if (s == null) return null;
          return {en: s, ka: s};
        }

    Then call `title: asBilingual(input.title)` at each call site. If only 1–2 sites need the wrapping, inline the object — no helper needed.

    (d) TypeScript: declare any local input-shape type used for the manager apply payload to use `string` for the human-typed input (manager UI is still English-only). The TEXT→JSONB transformation happens at the boundary, not in the input type. This keeps the manager UI / batch preview cards code from changing.
  </action>
  <acceptance_criteria>
    - viewer/app/api/manager/apply/route.ts contains the substring `{en:` in the context of writes to one of the 4 target tables (count ≥ 2).
    - File no longer has any INSERT into aleksandra_timeline / hypotheses / therapies that passes bare TEXT for the 6 converted columns. Grep audit: `grep -E "title:\\s*[^{]" viewer/app/api/manager/apply/route.ts` should not match any line writing a string literal or bare variable to `title` for the 4 target tables.
    - `cd viewer && npx tsc --noEmit -p tsconfig.json` exits 0.
    - `cd viewer && npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "{en:" viewer/app/api/manager/apply/route.ts | awk '{exit ($1 >= 2 ? 0 : 1)}' && cd viewer && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Manager apply route writes JSONB shape; post-migration insert won't fail with json-syntax error.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update Timeline, Therapies, Hypotheses (list + detail) viewer pages to read JSONB via displayField</name>
  <files>viewer/app/[locale]/timeline/page.tsx, viewer/app/[locale]/therapies/page.tsx, viewer/app/[locale]/hypotheses/page.tsx, viewer/app/[locale]/hypotheses/[id]/page.tsx</files>
  <read_first>
    - viewer/app/[locale]/timeline/page.tsx (post-06-03, post-06-05 — already has locale, getTranslations; now needs displayField for JSONB columns)
    - viewer/app/[locale]/therapies/page.tsx
    - viewer/app/[locale]/hypotheses/page.tsx
    - viewer/app/[locale]/hypotheses/[id]/page.tsx
    - viewer/lib/i18n.ts (06-04 — exports displayField + BilingualField)
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Pattern 2 last block — TimelinePage with BilingualField row type + displayField call)
  </read_first>
  <action>
    For each of the 4 viewer pages:

    (a) Add `import {displayField, type BilingualField} from '@/lib/i18n';` at the top.

    (b) Update the row TypeScript types so JSONB columns are typed as `BilingualField` instead of `string`. Example for timeline:

        type TimelineEvent = {
          id: string;
          event_date: string;
          event_type: string;
          title: BilingualField;           // was: string
          description: BilingualField;     // was: string | null
          institution: string | null;      // UNCHANGED — stays TEXT
          location: string | null;         // UNCHANGED
          created_at: string;
          updated_at: string;
        };

    For therapies: `name: BilingualField; evidence_summary: BilingualField; therapy_type: string; aleksandra_status: string; evidence_in_hie: string; ...` (only the 2 converted columns become BilingualField).

    For hypotheses (list + detail): `title: BilingualField; description: BilingualField;` only.

    (c) Replace every render of the 6 converted-column values with `displayField(row.field, locale)`. Examples:

        // BEFORE
        <td>{event.title}</td>
        <p>{event.description}</p>

        // AFTER
        <td>{displayField(event.title, locale)}</td>
        <p>{displayField(event.description, locale)}</p>

    (d) For the hypotheses [id] detail page, the entire row content goes through displayField — there may be additional places (cards / detail panels) where title/description appear.

    (e) Do NOT modify columns that stay TEXT (`institution`, `location`, `event_date`, `event_type`, `status`, `priority`, `therapy_type`, `aleksandra_status`, `evidence_in_hie`, etc.) — these stay rendered as `{row.field}` directly.

    (f) The `locale` variable comes from `const {locale} = await params;` (already added by 06-03 Task 3).

    (g) After editing, run `cd viewer && npm run build` — TypeScript will surface every site that did not get updated; treat any error mentioning `BilingualField` or `displayField` as a Wave-2 reading gap and fix.
  </action>
  <acceptance_criteria>
    - Each of the 4 page.tsx files contains `import {displayField` substring.
    - Each file contains at least one `displayField(` call.
    - Combined across the 4 files, `grep -c "displayField" viewer/app/\[locale\]/{timeline,therapies,hypotheses}/page.tsx viewer/app/\[locale\]/hypotheses/\[id\]/page.tsx | awk -F: '{s+=$2} END {print s}'` ≥ 6 (matching the 6 converted columns surfaced across the 4 pages — note dashboard/today/papers/knowledge may also display these columns but only the 4 plan-listed pages strictly require it).
    - `cd viewer && npx tsc --noEmit -p tsconfig.json` exits 0.
    - `cd viewer && npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>for f in "viewer/app/[locale]/timeline/page.tsx" "viewer/app/[locale]/therapies/page.tsx" "viewer/app/[locale]/hypotheses/page.tsx" "viewer/app/[locale]/hypotheses/[id]/page.tsx"; do grep -q "displayField" "$f" || { echo "MISS $f"; exit 1; }; done; cd viewer && npm run build 2>&1 | grep -qE "compiled successfully|Compiled successfully"</automated>
  </verify>
  <done>4 pages render JSONB columns via displayField; mixed-locale rows degrade gracefully via English fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager UI → viewer/app/api/manager/apply/route.ts | manager-typed payload crosses untrusted-input boundary; service-role writes downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-INSERT-SHAPE-MISMATCH | Tampering | viewer/app/api/manager/apply/route.ts | mitigate | Task 2 wraps every insert into the 6 converted columns with `{en, ka}` mirror objects. RESEARCH.md Pitfall 3 explicitly named this as the canonical break. |
| T-06-05 (display layer) | Information Disclosure (low) | Timeline/Therapies/Hypotheses pages | mitigate | displayField helper (06-04) handles `null`, `{en: 'X'}`-only, legacy TEXT — no rendering crash possible. Verifier check_i18n_08 unit-tests these 5 cases. |
| T-06-FND-02 (carry-over) | Information Disclosure | viewer/ remote-origin fetches | mitigate | This plan touches manager apply route + 4 viewer read pages; no new fetch call sites. FND-02 lint regression continues to pass (covered by 06-13 sweep). |
</threat_model>

<verification>
- viewer/app/api/manager/apply/route.ts writes `{en, ka}` for the 6 converted columns
- Timeline/Therapies/Hypotheses pages render JSONB via displayField
- `cd viewer && npm run build` exits 0
- Manual smoke: load /en/timeline and /ka/timeline against a row with `title = {en: "Test", ka: "ტესტი"}` — verify "Test" appears on /en, "ტესტი" appears on /ka
- `python -m scripts.verify_phase6 --bucket A` re-confirms check_i18n_08 still GREEN
</verification>

<success_criteria>
- Manager apply route writes JSONB shape; no `invalid input syntax for type json` errors at runtime
- Viewer pages render bilingual JSONB columns via displayField
- TypeScript catches any missed call site at compile time (BilingualField vs string mismatch)
- I18N-05 + I18N-08 verifier checks remain GREEN (no regression from 06-07's Wave-2 GREEN)
</success_criteria>

<output>
Create `.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-08-SUMMARY.md` when done. Record:
- Audit results from Task 1: full call-site enumeration with disposition
- Number of INSERT sites updated in Task 2
- Number of displayField() calls inserted in Task 3
- Any unexpected call sites that needed wrapping (e.g., a hidden seed script)
</output>
