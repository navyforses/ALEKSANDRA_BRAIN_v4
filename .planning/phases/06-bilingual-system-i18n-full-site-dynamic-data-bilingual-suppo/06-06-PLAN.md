---
phase: 06-bilingual-system-i18n
plan: 06
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - scripts/migrations/012_i18n_jsonb.sql
  - scripts/migrations/012_rollback/aleksandra_timeline.pre012.dump
  - scripts/migrations/012_rollback/hypotheses.pre012.dump
  - scripts/migrations/012_rollback/therapies.pre012.dump
  - scripts/migrations/012_rollback/briefs.pre012.dump
  - scripts/migrations/012_rollback/aleksandra_timeline.policies.pre.txt
  - scripts/migrations/012_rollback/hypotheses.policies.pre.txt
  - scripts/migrations/012_rollback/therapies.policies.pre.txt
  - scripts/migrations/012_rollback/briefs.policies.pre.txt
  - scripts/migrations/012_runbook.md
requirements:
  - I18N-05
  - I18N-09
autonomous: true
must_haves:
  truths:
    - "scripts/migrations/012_i18n_jsonb.sql exists with the exact ALTER COLUMN ... TYPE jsonb USING jsonb_build_object('en', col, 'ka', col) syntax from RESEARCH.md Pattern 5"
    - "Migration body covers all 6 TEXT-to-JSONB conversions: aleksandra_timeline.{title,description}, hypotheses.{title,description}, therapies.{name,evidence_summary}"
    - "Migration body covers briefs.sections reshape: each section body field rewritten to {en, ka} via the recursive jsonb_build_object UPDATE from RESEARCH.md Pattern 5"
    - "Migration wraps every statement in a single BEGIN ... COMMIT transaction"
    - "scripts/migrations/012_rollback/ contains pre-migration pg_dump artifacts (4 .dump files, one per table) AND pre-migration RLS policy snapshots (4 .policies.pre.txt files)"
    - "scripts/migrations/012_runbook.md exists with the exact operator command sequence Shako runs in 06-07"
  artifacts:
    - path: scripts/migrations/012_i18n_jsonb.sql
      provides: "Migration 012 SQL — TEXT-to-JSONB + briefs.sections reshape + RLS-preservation by no-op (ALTER COLUMN TYPE does not drop policies per PostgreSQL 15 docs)"
      contains: "jsonb_build_object('en'"
      min_lines: 60
    - path: scripts/migrations/012_runbook.md
      provides: "Step-by-step operator manual for applying migration 012 against production Supabase"
      contains: "pg_dump"
    - path: scripts/migrations/012_rollback/aleksandra_timeline.pre012.dump
      provides: "pg_dump rollback artifact for aleksandra_timeline"
    - path: scripts/migrations/012_rollback/hypotheses.pre012.dump
      provides: "pg_dump rollback artifact for hypotheses"
    - path: scripts/migrations/012_rollback/therapies.pre012.dump
      provides: "pg_dump rollback artifact for therapies"
    - path: scripts/migrations/012_rollback/briefs.pre012.dump
      provides: "pg_dump rollback artifact for briefs"
  key_links:
    - from: scripts/migrations/012_i18n_jsonb.sql
      to: scripts/migrations/008_phase3_tables_and_rls.sql
      via: "RLS policies from 008 must survive — verified by .policies.pre.txt diff after apply"
      pattern: "ALTER TABLE.*ALTER COLUMN"
---

<objective>
Author scripts/migrations/012_i18n_jsonb.sql (RESEARCH.md Pattern 5 verbatim) — the migration that converts 6 TEXT columns across 4 tables (`aleksandra_timeline`, `hypotheses`, `therapies`) plus the `briefs.sections` JSONB body fields to the `{en, ka}` shape. Generate the 4 per-table pre-migration `pg_dump` rollback artifacts and snapshot the existing RLS policy blocks. Write scripts/migrations/012_runbook.md — the operator manual Shako follows in 06-07 to apply the migration. This plan does NOT apply the migration to production; it produces the artifacts that 06-07's [BLOCKING] task consumes.

Purpose: Stage migration 012 with full rollback safety. The pg_dump artifacts make the apply step in 06-07 reversible. The runbook makes the manual apply step deterministic.
Output: 1 SQL migration + 4 dump files + 4 policy snapshots + 1 runbook markdown.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-SPEC.md
@.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-CONTEXT.md
@.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md
@scripts/migrations/002_aleksandra_timeline.sql
@scripts/migrations/008_phase3_tables_and_rls.sql
@scripts/migrations/011_manager_actions_and_intake_drops.sql
@scripts/schema.sql

<interfaces>
<!-- Migration SQL surface (Pattern 5 from RESEARCH.md, verified syntax) -->
BEGIN;

ALTER TABLE aleksandra_timeline
  ALTER COLUMN title TYPE jsonb
    USING jsonb_build_object('en', title, 'ka', title),
  ALTER COLUMN description TYPE jsonb
    USING CASE WHEN description IS NULL THEN NULL
               ELSE jsonb_build_object('en', description, 'ka', description) END;

ALTER TABLE hypotheses
  ALTER COLUMN title TYPE jsonb USING jsonb_build_object('en', title, 'ka', title),
  ALTER COLUMN description TYPE jsonb USING jsonb_build_object('en', description, 'ka', description);

ALTER TABLE therapies
  ALTER COLUMN name TYPE jsonb USING jsonb_build_object('en', name, 'ka', name),
  ALTER COLUMN evidence_summary TYPE jsonb USING CASE WHEN evidence_summary IS NULL THEN NULL
                                                       ELSE jsonb_build_object('en', evidence_summary, 'ka', evidence_summary) END;

UPDATE briefs SET sections = ( /* recursive jsonb_build_object — see RESEARCH.md Pattern 5 verbatim */ );

COMMIT;

<!-- Rollback artifact layout -->
scripts/migrations/012_rollback/
  aleksandra_timeline.pre012.dump        (pg_dump --data-only --column-inserts)
  hypotheses.pre012.dump
  therapies.pre012.dump
  briefs.pre012.dump
  aleksandra_timeline.policies.pre.txt   (output of `\d aleksandra_timeline`)
  hypotheses.policies.pre.txt
  therapies.policies.pre.txt
  briefs.policies.pre.txt
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-flight live-DB introspection (RESEARCH.md Assumptions A1, A3, A4, A7 verification)</name>
  <files>scripts/migrations/012_rollback/aleksandra_timeline.policies.pre.txt, scripts/migrations/012_rollback/hypotheses.policies.pre.txt, scripts/migrations/012_rollback/therapies.policies.pre.txt, scripts/migrations/012_rollback/briefs.policies.pre.txt, scripts/migrations/012_rollback/_preflight.txt</files>
  <read_first>
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Assumptions Log A1, A3, A4, A7 — pre-migration verification tasks)
    - scripts/migrations/008_phase3_tables_and_rls.sql (RLS pattern that must survive)
  </read_first>
  <action>
    Verify against the live Supabase database (using SUPABASE_DB_URL env var, which is already used by scripts/communicator/weekly_brief.py per RESEARCH.md Environment Availability):

    (a) Confirm NO indexes touch the converted columns (RESEARCH.md A1):
        psql "$SUPABASE_DB_URL" -c "\d aleksandra_timeline" > scripts/migrations/012_rollback/aleksandra_timeline.policies.pre.txt
        psql "$SUPABASE_DB_URL" -c "\d hypotheses"          > scripts/migrations/012_rollback/hypotheses.policies.pre.txt
        psql "$SUPABASE_DB_URL" -c "\d therapies"           > scripts/migrations/012_rollback/therapies.policies.pre.txt
        psql "$SUPABASE_DB_URL" -c "\d briefs"              > scripts/migrations/012_rollback/briefs.policies.pre.txt

    Each output contains both the column list, the index list, the policy list, AND the trigger list — covering A1 + A7 in one shot.

    (b) Append a `_preflight.txt` summary that records the audit results:
        - Column-touching indexes on the 6 target columns: must be EMPTY. Grep each `.policies.pre.txt` for the column names; assert no index entry mentions them.
        - Triggers on the 4 tables referencing the converted columns: must be EMPTY beyond the `aleksandra_timeline_set_updated_at` already known to touch only `updated_at`.
        - RLS policies present on each table: record count + names per table (expected: 3 policies for aleksandra_timeline per RESEARCH.md; 2–4 per other tables per migration 008).

    (c) For A4 — confirm scripts/manager/briefing.py does NOT directly read `briefs.sections` body fields with old-string-shape assumptions. Use a literal grep:
        grep -rn "sections\[" scripts/manager/ scripts/communicator/ || true
        grep -rn "summary_lines\[" scripts/manager/ scripts/communicator/ || true
    Record the results in _preflight.txt with one line per call site found; if any call site reads a string-shape field without going through a display_field_py-style helper, file a follow-up in the runbook.

    (d) For A3 — confirm workflows/daily_digest.json is still `"active": false`. Grep:
        grep -c '"active": false' workflows/daily_digest.json
    Should be ≥ 1. Record in _preflight.txt.

    If SUPABASE_DB_URL is not set in the executor's environment, the executor MUST set it before running this task. If the live DB is unreachable, file this task as BLOCKED on environment access and surface to the human operator — DO NOT proceed to Task 2 with assumed data.
  </action>
  <acceptance_criteria>
    - All 4 .policies.pre.txt files exist under scripts/migrations/012_rollback/.
    - Each file is non-empty (contains the `\d table` output).
    - scripts/migrations/012_rollback/_preflight.txt exists.
    - _preflight.txt records: index audit (zero column-targeted indexes on the 6 converted columns), trigger audit (none referencing converted columns by name), RLS policy count per table.
    - Grep over the 4 .policies.pre.txt files for the 6 converted column names paired with "btree" / "gin" / "hash" returns 0 matches.
  </acceptance_criteria>
  <verify>
    <automated>test -s scripts/migrations/012_rollback/aleksandra_timeline.policies.pre.txt && test -s scripts/migrations/012_rollback/hypotheses.policies.pre.txt && test -s scripts/migrations/012_rollback/therapies.policies.pre.txt && test -s scripts/migrations/012_rollback/briefs.policies.pre.txt && test -s scripts/migrations/012_rollback/_preflight.txt</automated>
  </verify>
  <done>Live-DB introspection complete; assumptions A1+A3+A4+A7 confirmed or revised; pre-policy snapshots persisted.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Generate pre-migration pg_dump artifacts (4 per-table .dump files)</name>
  <files>scripts/migrations/012_rollback/aleksandra_timeline.pre012.dump, scripts/migrations/012_rollback/hypotheses.pre012.dump, scripts/migrations/012_rollback/therapies.pre012.dump, scripts/migrations/012_rollback/briefs.pre012.dump</files>
  <read_first>
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-CONTEXT.md (Claude's Discretion: per-table dumps preferred — Task 1 specifics call this out)
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Pattern 5 — Pre-migration commands block)
  </read_first>
  <action>
    For each of the 4 target tables, generate a per-table data dump using pg_dump's `--column-inserts` mode (RESEARCH.md "Don't Hand-Roll" — `--column-inserts` produces a restorable text file independent of TYPE changes):

        pg_dump "$SUPABASE_DB_URL" \
          --table=aleksandra_timeline \
          --data-only \
          --column-inserts \
          --no-owner --no-privileges \
          --file=scripts/migrations/012_rollback/aleksandra_timeline.pre012.dump

    Repeat for `hypotheses`, `therapies`, `briefs`.

    The dumps record EVERY existing row's data with explicit column names — perfect for restoration after a hypothetical TYPE-revert. Each file should be human-readable SQL (INSERT statements), not binary.

    Sanity-check each file post-dump:
    - File exists and is non-empty.
    - File starts with `--` (pg_dump SQL header).
    - File contains `INSERT INTO` lines (the row data).
    - File does NOT contain credentials or service-role keys (pg_dump never emits these, but assert just in case).
  </action>
  <acceptance_criteria>
    - All 4 .dump files exist under scripts/migrations/012_rollback/.
    - Each file is non-empty.
    - Each file begins with `--` (pg_dump header).
    - `head -1 scripts/migrations/012_rollback/aleksandra_timeline.pre012.dump` returns a `-- PostgreSQL database dump` line.
    - No file contains the string `SUPABASE_DB_URL` or any obvious secret pattern.
  </acceptance_criteria>
  <verify>
    <automated>for tbl in aleksandra_timeline hypotheses therapies briefs; do test -s "scripts/migrations/012_rollback/$tbl.pre012.dump" || exit 1; head -1 "scripts/migrations/012_rollback/$tbl.pre012.dump" | grep -q "PostgreSQL database dump" || exit 1; done</automated>
  </verify>
  <done>4 per-table dump artifacts staged; migration 012 is reversible.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author scripts/migrations/012_i18n_jsonb.sql (RESEARCH.md Pattern 5 verbatim)</name>
  <files>scripts/migrations/012_i18n_jsonb.sql</files>
  <read_first>
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Pattern 5 — entire SQL block from `-- scripts/migrations/012_i18n_jsonb.sql` through `COMMIT;`)
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-CONTEXT.md (D-04 — no GIN indexes; D-05 NA here)
    - scripts/migrations/011_manager_actions_and_intake_drops.sql (most-recent migration — file-header convention)
  </read_first>
  <action>
    Create scripts/migrations/012_i18n_jsonb.sql containing the EXACT SQL from RESEARCH.md Pattern 5 ("Migration 012 body (verified syntax against PostgreSQL 15 docs)").

    Required components in order:

    (1) File header comment (mirror migration 011 style):
        -- scripts/migrations/012_i18n_jsonb.sql
        -- Phase 6 I18N-05 + I18N-09: convert 6 TEXT columns to JSONB {en, ka}
        -- and reshape briefs.sections body fields. Preserves RLS from migration 008.
        -- Pre-migration rollback artifacts: scripts/migrations/012_rollback/*.pre012.dump
        -- Apply: psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f scripts/migrations/012_i18n_jsonb.sql

    (2) `BEGIN;` — all statements atomic.

    (3) ALTER TABLE aleksandra_timeline — convert `title` (NOT NULL) + `description` (nullable) to JSONB using `jsonb_build_object('en', col, 'ka', col)`; for nullable `description`, wrap with `CASE WHEN description IS NULL THEN NULL ELSE jsonb_build_object(...) END` to preserve NULLs.

    (4) ALTER TABLE hypotheses — convert `title` + `description` to JSONB.

    (5) ALTER TABLE therapies — convert `name` (NOT NULL) + `evidence_summary` (nullable, wrap CASE) to JSONB.

    (6) UPDATE briefs SET sections = (...) — the recursive `jsonb_build_object` expression from RESEARCH.md Pattern 5 that reshapes per-array-element body fields (`summary_lines[i]` → `{en, ka}`, `papers[i].title` → `{en, ka}`, `hypotheses[i].title` → `{en, ka}`, `therapies[i].name` → `{en, ka}`, `outreach[i].subject` → `{en, ka}`, `questions[i].question` → `{en, ka}`, `questions[i].context` → `{en, ka}`). Leave alone: `citations[]`, `citation_id`, dates, scores, statuses.

    (7) `COMMIT;`

    Do NOT add GIN indexes (CONTEXT.md D-04). Do NOT add foreign-key changes. Do NOT touch other tables (`outreach_log.subject` stays TEXT per SPEC out-of-scope).

    Copy the SQL EXACTLY from RESEARCH.md Pattern 5 — every `jsonb_build_object` clause, every `jsonb_agg` clause, every `||` concat. Manual deviation risks breaking the briefs reshape.
  </action>
  <acceptance_criteria>
    - File scripts/migrations/012_i18n_jsonb.sql exists.
    - File line count ≥ 60.
    - File contains `BEGIN;` and `COMMIT;`.
    - File contains EXACTLY 6 `ALTER COLUMN ... TYPE jsonb` clauses (3 ALTER TABLE statements × 2 columns each).
    - File contains the string `jsonb_build_object('en'` at least 6 times (one per column).
    - File contains `UPDATE briefs` exactly once.
    - File contains `jsonb_agg(` multiple times (briefs reshape — at least 6 per RESEARCH.md Pattern 5 block).
    - `grep -c "GIN" scripts/migrations/012_i18n_jsonb.sql` returns 0 (no GIN indexes per D-04).
    - `psql --dry-run` substitute: run `psql "postgres://postgres@localhost/postgres" --set ON_ERROR_STOP=1 -c "PARSE scripts/migrations/012_i18n_jsonb.sql"` — this is not a real psql command; instead use a syntax-only check via `psql --command="\\set ECHO all" -f scripts/migrations/012_i18n_jsonb.sql --dry-run` (PostgreSQL has no `--dry-run`; use `python -c "import re; sql = open('scripts/migrations/012_i18n_jsonb.sql').read(); assert sql.count('jsonb_build_object') >= 6, sql.count('jsonb_build_object')"` as the deterministic structural assertion).
  </acceptance_criteria>
  <verify>
    <automated>test -f scripts/migrations/012_i18n_jsonb.sql && python -c "sql = open('scripts/migrations/012_i18n_jsonb.sql', encoding='utf-8').read(); assert 'BEGIN;' in sql and 'COMMIT;' in sql; assert sql.count('jsonb_build_object') >= 6, f'got {sql.count(chr(34)+chr(34))}'; assert sql.count('ALTER COLUMN') >= 6; assert 'UPDATE briefs' in sql; assert 'GIN' not in sql.upper().replace('BEGIN','x'); print('OK')"</automated>
  </verify>
  <done>Migration 012 SQL authored to RESEARCH.md Pattern 5 spec; no GIN indexes; transactional.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Author scripts/migrations/012_runbook.md (operator manual for 06-07's blocking apply)</name>
  <files>scripts/migrations/012_runbook.md</files>
  <read_first>
    - scripts/migrations/012_i18n_jsonb.sql (just authored — Task 3 output)
    - scripts/migrations/012_rollback/_preflight.txt (Task 1 output — known live-DB state)
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-VALIDATION.md (Manual-Only Verifications: "Production Supabase migration 012 application — Manual SQL execution against prod")
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Pattern 5 — Pre-migration / Post-migration smoke SELECTs / Rollback blocks)
  </read_first>
  <action>
    Create scripts/migrations/012_runbook.md — a markdown document Shako follows. Structure (verbatim section names so the executor in 06-07 can grep for them):

    ## Pre-flight (already done by 06-06)
    - List the rollback artifacts in scripts/migrations/012_rollback/ (4 .dump + 4 .policies.pre.txt files).
    - Reference the result of _preflight.txt (zero index conflicts, RLS policy counts).
    - Confirm SUPABASE_DB_URL env var holds the service-role connection string.

    ## Apply command
    Single command, fail-fast on any error:

        psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f scripts/migrations/012_i18n_jsonb.sql

    ## Post-apply smoke checks (run in order)
    1. `psql "$SUPABASE_DB_URL" -c "SELECT pg_typeof(title) FROM aleksandra_timeline LIMIT 1;"` — expected: `jsonb`.
    2. Same against hypotheses.title, therapies.name, etc. — all 6 must return `jsonb`.
    3. `psql "$SUPABASE_DB_URL" -c "SELECT id, title->>'en', title->>'ka' FROM aleksandra_timeline LIMIT 5;"` — every row has identical en/ka content (I18N-09 acceptance).
    4. `psql "$SUPABASE_DB_URL" -c "SELECT polname FROM pg_policy WHERE polrelid='aleksandra_timeline'::regclass;"` — expected: 3 policies from migration 002 (aleksandra_timeline_family_read, aleksandra_timeline_service_write, aleksandra_timeline_service_update). Cross-check against scripts/migrations/012_rollback/aleksandra_timeline.policies.pre.txt — policy list MUST be identical (RLS preserved).
    5. Repeat the policy check for hypotheses, therapies, briefs.
    6. `psql "$SUPABASE_DB_URL" -c "SELECT count(*) FROM briefs WHERE sections->'summary_lines'->0->>'en' IS NOT NULL;"` — should equal total briefs count if any briefs have summary_lines populated; 0 is fine if no briefs exist yet.

    ## Rollback procedure (if smoke fails)
    Per RESEARCH.md Pattern 5:

        for tbl in aleksandra_timeline hypotheses therapies briefs; do
          psql "$SUPABASE_DB_URL" -c "BEGIN; DELETE FROM $tbl;" -f "scripts/migrations/012_rollback/$tbl.pre012.dump" -c "COMMIT;"
        done
        # Then schema TYPE revert (manual):
        psql "$SUPABASE_DB_URL" -c "ALTER TABLE aleksandra_timeline ALTER COLUMN title TYPE text USING title->>'en'; ALTER COLUMN description TYPE text USING description->>'en';"
        # (repeat for hypotheses, therapies; briefs.sections JSONB type is unchanged so no revert needed there)

    ## What 06-07 does
    Reference: "The [BLOCKING] task in plan 06-07 runs the Apply command above and the 6 smoke checks. Approval gate: Shako runs the command in a maintenance window; downstream Wave-3 plans block until verified."

    Length: 100–200 lines is plenty. Keep prose precise; no decorative prose.
  </action>
  <acceptance_criteria>
    - File scripts/migrations/012_runbook.md exists.
    - Line count between 80 and 250.
    - File contains the exact `psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f scripts/migrations/012_i18n_jsonb.sql` apply command.
    - File contains all 6 post-apply smoke checks numbered 1..6.
    - File contains a `## Rollback procedure` section.
    - File references the 4 .pre012.dump and 4 .policies.pre.txt artifacts by name.
  </acceptance_criteria>
  <verify>
    <automated>test -f scripts/migrations/012_runbook.md && grep -q "ON_ERROR_STOP=1" scripts/migrations/012_runbook.md && grep -q "## Rollback procedure" scripts/migrations/012_runbook.md && grep -q "pg_typeof" scripts/migrations/012_runbook.md</automated>
  </verify>
  <done>Operator manual ready; 06-07's blocking apply task has a deterministic procedure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → Supabase production DB | service-role credentials cross deployment boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information Disclosure (HIGH) | scripts/migrations/012_i18n_jsonb.sql + migration 008 RLS | mitigate | Task 1 captures `\d table` policy block PRE-migration; Task 4 runbook step 4 verifies the policy block POST-migration must match. RESEARCH.md confirms `ALTER COLUMN ... TYPE` does NOT drop policies on PostgreSQL 15. Combined: pre-snapshot + post-check = RLS-preserved by structural diff. |
| T-06-MIGRATION-DATALOSS | Information Disclosure | scripts/migrations/012_i18n_jsonb.sql | mitigate | Task 2 generates per-table `pg_dump --column-inserts` rollback artifacts BEFORE the migration runs. Task 3 wraps every ALTER + UPDATE in a single transaction so partial-apply is impossible. Task 4 runbook includes a rollback procedure that uses the dump artifacts. |
| T-06-PARTIAL-INDEX-CONFLICT | Tampering | ALTER COLUMN TYPE jsonb | accept | RESEARCH.md A1 verified by Task 1 — the 6 target columns have no indexes; the conversion does not trip an index-drop error. Risk: a future migration we missed could have added an index. Mitigation: Task 1 _preflight.txt records the live state at the moment of migration authoring. |
</threat_model>

<verification>
- `test -f scripts/migrations/012_i18n_jsonb.sql && test -f scripts/migrations/012_runbook.md`
- All 4 .pre012.dump and 4 .policies.pre.txt files exist under scripts/migrations/012_rollback/
- `python -m scripts.verify_phase6 --mode code-complete --bucket B` — at this Wave 2 / 06-06 stage, code-complete mode passes (SQL + dumps exist) but production mode still RED (migration not yet applied — that is 06-07's job)
- `python -c "open('scripts/migrations/012_i18n_jsonb.sql').read().count('jsonb_build_object') >= 6"`
</verification>

<success_criteria>
- Migration 012 SQL exists with RESEARCH.md Pattern 5 verbatim shape
- 4 per-table pg_dump rollback artifacts staged
- 4 pre-migration RLS/policy snapshots staged
- Operator runbook authored with deterministic apply + rollback procedure
- check_i18n_05 (code-complete mode) flips GREEN; production-mode flip happens in 06-07
</success_criteria>

<output>
Create `.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-06-SUMMARY.md` when done. Record:
- Total pg_dump size per table (sanity: should be tens of KB at current row counts)
- _preflight.txt findings: index audit + RLS count per table + workflow daily_digest.json active=false confirmed
- Any deviation from RESEARCH.md Pattern 5 SQL (none expected)
</output>
