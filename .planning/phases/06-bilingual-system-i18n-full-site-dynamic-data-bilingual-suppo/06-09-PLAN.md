---
phase: 06-bilingual-system-i18n
plan: 09
type: execute
wave: 3
# Phase 6 Wave 3 is split into 3a (06-10, 06-11 — expose redact_bilingual + locales kwarg) and 3b (this plan — consumes those APIs)
depends_on:
  - 06-07
  - 06-10
  - 06-11
files_modified:
  - scripts/communicator/bilingual.py
  - scripts/communicator/weekly_brief.py
  - scripts/manager/briefing.py
  - agents/communicator.py
requirements:
  - I18N-06
autonomous: true
must_haves:
  truths:
    - "scripts/communicator/bilingual.py exists and exports compose_bilingual(prompt, *, client, model='claude-sonnet-4-5') -> dict"
    - "compose_bilingual uses Anthropic strict tool_use (tools=[{strict: True, ...}], tool_choice={type: 'tool', name: 'compose_bilingual'}) per CONTEXT.md D-02 + RESEARCH.md Pattern 6 — schema-validated {en, ka} output"
    - "scripts/communicator/weekly_brief.py emits {en, ka} JSONB for briefs.sections fields per CONTEXT.md D-02 per-tier policy"
    - "scripts/manager/briefing.py emits {en, ka} JSONB for manager-briefing inserted rows"
    - "agents/communicator.py (CrewAI Communicator wrapper) — its hypothesis/therapy/timeline row write path emits {en, ka}; internal CrewAI agent reasoning stays English (D-02 scoping)"
    - "Communicator clinician outreach drafter (scripts/communicator/outreach_drafter.py) is NOT modified — single-recipient single-language stays per D-02 Per-tier policy"
    - "agents/communicator.py imports redact_bilingual from scripts.communicator.phi_redactor (provided by 06-10) and uses banned_phrases.check(text, locales=('ka',)) (per-locale kwarg provided by 06-11) — both Wave-3a APIs are guaranteed available because depends_on includes 06-10 + 06-11"
    - "SUMMARY_TEMPLATES_KA block is preceded by a TODO(Phase 6 execute) comment for Shako sanity-check of the 5 Georgian template strings (mirrors 06-11 Shako-review pattern as a TODO marker, not a blocking checkpoint)"
  artifacts:
    - path: scripts/communicator/bilingual.py
      provides: "compose_bilingual() helper — Anthropic strict tool_use returning {en, ka}"
      contains: "compose_bilingual"
      min_lines: 50
    - path: scripts/communicator/weekly_brief.py
      provides: "Updated brief composer — section bodies + summary_lines emitted as {en, ka}"
      contains: "{en"
  key_links:
    - from: scripts/communicator/weekly_brief.py
      to: scripts/communicator/bilingual.py
      via: "from scripts.communicator.bilingual import compose_bilingual (for LLM-translated lines) OR static-template mirror (Option A from RESEARCH.md Pattern 6)"
      pattern: "compose_bilingual|jsonb_build_object"
    - from: scripts/manager/briefing.py
      to: scripts/communicator/bilingual.py
      via: "import compose_bilingual when writing JSONB rows"
      pattern: "compose_bilingual"
---

<objective>
Author scripts/communicator/bilingual.py — the `compose_bilingual()` helper that wraps Anthropic Claude Sonnet 4.5 with `tools=[{strict: True, input_schema: {en, ka}}]` + `tool_choice={type: 'tool', name: 'compose_bilingual'}` per CONTEXT.md D-02 + RESEARCH.md Pattern 6. Update scripts/communicator/weekly_brief.py, scripts/manager/briefing.py, and agents/communicator.py to emit `{en, ka}` JSONB for family-visible newly-created rows in `briefs.sections`, `aleksandra_timeline`, `hypotheses`, `therapies`. Per CONTEXT.md D-02 per-tier policy: scripts/communicator/outreach_drafter.py is NOT touched (single-recipient single-language stays); internal CrewAI agent reasoning stays English-only.

Per RESEARCH.md Pattern 6 recommendation: use **Option A** (deterministic-prose mirror) for the weekly_brief `summary_lines` block — the 5 fixed template strings get hand-translated and mirrored `{en, ka}` at zero Anthropic cost. Use **Option B** (compose_bilingual LLM call) for the upstream Communicator agent's per-row hypothesis/therapy/timeline emissions, where the content varies per call and translation drift is real.

**Wave-3 dependency note (per checker BLOCKER 2):** this plan consumes `redact_bilingual` from 06-10 and the per-locale `check(text, locales=...)` kwarg from 06-11. Both must land first, hence `depends_on: [06-07, 06-10, 06-11]`. 06-10 and 06-11 themselves only depend on 06-07.

Purpose: Land I18N-06 — Communicator + Phase 5 composer emit `{en, ka}` for family-visible newly-created content.
Output: 1 new helper + 3 updated write paths; bilingual rows produced from the next Communicator run forward.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-SPEC.md
@.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-CONTEXT.md
@.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md
@scripts/communicator/weekly_brief.py
@scripts/manager/briefing.py
@agents/communicator.py
@scripts/communicator/phi_redactor.py
@scripts/communicator/banned_phrases.py

<interfaces>
<!-- Locked surface from CONTEXT.md D-02 + RESEARCH.md Pattern 6 -->
def compose_bilingual(prompt: str, *, client: Anthropic, model: str = "claude-sonnet-4-5") -> dict:
    """Returns {'en': str, 'ka': str}. Raises RuntimeError if model produced no tool_use."""

# Schema (strict tool_use):
BILINGUAL_TOOL = {
    "name": "compose_bilingual",
    "description": "...",
    "strict": True,
    "input_schema": {
        "type": "object",
        "properties": {
            "en": {"type": "string", "description": "..."},
            "ka": {"type": "string", "description": "..."},
        },
        "required": ["en", "ka"],
        "additionalProperties": False,
    },
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author scripts/communicator/bilingual.py (compose_bilingual helper, Anthropic strict tool_use)</name>
  <files>scripts/communicator/bilingual.py, tests/test_compose_bilingual.py</files>
  <read_first>
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Pattern 6 — the entire `compose_bilingual` Python block + cost note + strict-tool-use justification)
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-CONTEXT.md (D-02 — single Anthropic structured-output call returning {en, ka})
    - scripts/communicator/weekly_brief.py (top imports — confirm Anthropic SDK usage pattern in scripts/)
    - scripts/communicator/banned_phrases.py (will be used downstream; not directly imported by bilingual.py but referenced in tool description)
  </read_first>
  <behavior>
    - Test 1 (smoke against mock Anthropic): A mocked Anthropic client returning a tool_use block with `{"en": "Hello", "ka": "გამარჯობა"}` yields `{'en': 'Hello', 'ka': 'გამარჯობა'}`.
    - Test 2 (no tool_use block): If the mocked response contains only text blocks, `compose_bilingual` raises `RuntimeError` with message containing `no tool_use`.
    - Test 3 (schema integrity): The BILINGUAL_TOOL constant has `strict=True`, `additionalProperties=False`, `required=['en', 'ka']`.
    - Test 4 (default model): Calling without `model=...` uses `claude-sonnet-4-5`.
  </behavior>
  <action>
    Create scripts/communicator/bilingual.py with the EXACT shape from RESEARCH.md Pattern 6, with these refinements:

    (a) Tool description (the LLM-facing prompt portion) MUST include the PHI guard language from Pattern 6: `"... use 'A.J., 8-month-old infant with severe HIE' as the patient referent."` This reinforces the redactor convention even though Task 4 of plan 06-10 will redact post-emission.

    (b) Add Phase 6 budget integration: BEFORE the Anthropic call, call the existing `from scripts.ledger import check_daily_budget; check_daily_budget(estimated_cost_usd=0.01)`. This routes the call through the Phase 0 FND-04 cost gate (the daily $1.50 ceiling that protects against the runaway scenario; budget gate is the standard pattern across all Communicator LLM calls per Phase 2.5 / Phase 5).

    (c) Function signature exactly: `def compose_bilingual(prompt: str, *, client: Anthropic, model: str = "claude-sonnet-4-5") -> dict[str, str]:`. Keyword-only `client` + `model` per Pattern 6.

    (d) Implementation body: build `messages=[{"role": "user", "content": prompt}]`, call `client.messages.create(model=..., max_tokens=1024, tools=[BILINGUAL_TOOL], tool_choice={"type": "tool", "name": "compose_bilingual"}, messages=...)`. Iterate the resp.content blocks; return the first `tool_use` block's `.input` dict (must have keys 'en' and 'ka' — strict mode guarantees this; defensive read still allowed for type stability). If no tool_use found, raise RuntimeError.

    (e) After the call, log to runs ledger via the same pattern used in scripts/communicator/weekly_brief.py — `runs.token_cost` recorded for spend instrumentation (Phase 2.5 A.2 gate). Defer the exact ledger call to weekly_brief.py / briefing.py callers (compose_bilingual itself stays a pure-function helper; the caller wraps it with ledger.record_llm_call).

    (f) Author tests/test_compose_bilingual.py — a pytest module with the 4 behavior tests above. Use `unittest.mock.MagicMock` to fake the Anthropic client; structure mock returns to mimic `client.messages.create(...).content = [tool_use_block]` shape (consult anthropic SDK docs via Context7 if the message shape is non-obvious).
  </action>
  <acceptance_criteria>
    - File scripts/communicator/bilingual.py exists; line count between 50 and 120.
    - File contains the strings: `def compose_bilingual`, `"strict": True`, `"additionalProperties": False`, `tool_choice={"type": "tool", "name": "compose_bilingual"}`, `claude-sonnet-4-5`.
    - File imports `from scripts.ledger import check_daily_budget` (budget gate).
    - File `tests/test_compose_bilingual.py` exists.
    - `python -m pytest tests/test_compose_bilingual.py -v` exits 0 with 4 tests passing.
    - `python -c "from scripts.communicator.bilingual import compose_bilingual, BILINGUAL_TOOL; assert BILINGUAL_TOOL['strict']; assert BILINGUAL_TOOL['input_schema']['additionalProperties'] is False; assert BILINGUAL_TOOL['input_schema']['required'] == ['en', 'ka']; print('OK')"` prints OK.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_compose_bilingual.py -v 2>&1 | grep -E "4 passed|passed" && python -c "from scripts.communicator.bilingual import compose_bilingual, BILINGUAL_TOOL; assert BILINGUAL_TOOL['strict'] and BILINGUAL_TOOL['input_schema']['required'] == ['en', 'ka']; print('OK')"</automated>
  </verify>
  <done>compose_bilingual helper exists with strict tool_use schema; 4 unit tests pass; budget gate integrated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update scripts/communicator/weekly_brief.py — Option A (deterministic-mirror) for summary_lines; JSONB shape for inserted briefs.sections</name>
  <files>scripts/communicator/weekly_brief.py</files>
  <read_first>
    - scripts/communicator/weekly_brief.py (current — read fully; understand BriefSections to_dict, summary_lines composition lines ~359–366, the briefs INSERT call)
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Pattern 6 "Integration into weekly_brief.py" — Recommendation: Option A for summary_lines block; Option B for upstream hypotheses/therapies/aleksandra_timeline row inserts)
    - scripts/migrations/012_i18n_jsonb.sql (briefs.sections post-migration shape — per-array-element {en, ka})
  </read_first>
  <action>
    (a) **Define a small Georgian-mirror dict** at the top of weekly_brief.py for the 5 fixed template summary-line strings. Example for "{n} new relevant papers this week.":

        # TODO(Phase 6 execute): Shako sanity-check the Georgian phrasing of these 5 template strings before this plan's commit is merged.
        # 5 fixed template strings → manual Georgian translations (Option A from RESEARCH.md Pattern 6)
        SUMMARY_TEMPLATES_KA = {
            "no_new_papers": "ამ კვირას ახალი რელევანტური სტატია არ მოგვეპოვება.",
            "new_papers_count": "ამ კვირას {n} ახალი რელევანტური სტატია.",
            "no_new_hypotheses": "ამ კვირას ახალი ჰიპოთეზა არ მოგვეპოვება.",
            "new_hypotheses_count": "ამ კვირას {n} ახალი ჰიპოთეზა გადასახედი.",
            # …add the 5 actual template keys the existing weekly_brief.py uses; see current code lines ~359–366
        }

    Use the actual template strings from the existing weekly_brief.py code at lines ~359–366. The translations must follow CLAUDE.md "Georgian docs and family-facing prose" convention and must NOT trigger the imperative-verb lint (use evidence-grade descriptive Georgian, not direct instructions).

    (b) **Update BriefSections.to_dict() (or the equivalent serializer)** to emit each `summary_lines[i]` as a `{"en": en_str, "ka": ka_str}` dict by looking up the template key in SUMMARY_TEMPLATES_KA. The output JSONB shape for the briefs row's sections field becomes:

        {
          "week_start": "...",
          "summary_lines": [
            {"en": "5 new relevant papers this week.", "ka": "ამ კვირას 5 ახალი რელევანტური სტატია."},
            ...
          ],
          "papers": [...],   # already-bilingualized per-row from upstream Communicator (Task 3)
          ...
        }

    Match the per-array-element shape that migration 012 produces for historical briefs.sections rows.

    (c) **For per-row fields inside papers/hypotheses/therapies/outreach/questions arrays inside briefs.sections** — these arrays are stitched from upstream rows that ALREADY have JSONB title/name (because migration 012 ran AND task 3 below + Communicator agent emit JSONB). So weekly_brief.py here just passes through whatever upstream gave it. If a paper's title comes from `papers.title` (which stays TEXT — papers table is OUT of scope per SPEC; only the 4 named tables convert), wrap it `{en: paper.title, ka: paper.title}` per Option-A mirror.

    (d) **briefs INSERT call** — find where weekly_brief.py inserts into the briefs table and update so the inserted `sections` value is a JSON-encoded version of the new bilingual shape from (b). The existing `sections` jsonb_column is already JSONB at the DB level; only the *contents* shape changed. Use `psycopg2.extras.Json(sections_dict)` or `json.dumps(sections_dict)` per the existing pattern.

    (e) **Add a --bilingual-test dry-run flag** to scripts/communicator/weekly_brief.py's CLI argparse so verifier check_i18n_06 can call `python -m scripts.communicator.weekly_brief --dry-run --bilingual-test` and assert the produced sections dict has summary_lines[0].en and summary_lines[0].ka non-empty (per RESEARCH.md Validation I18N-06 row).

    (f) Do NOT call compose_bilingual here for the summary_lines — Option A is deterministic-prose only, zero Anthropic cost.
  </action>
  <acceptance_criteria>
    - scripts/communicator/weekly_brief.py contains the string `SUMMARY_TEMPLATES_KA` (or equivalent Georgian-translation dict).
    - The Georgian translations contain ≥3 Mkhedruli codepoints across them.
    - The SUMMARY_TEMPLATES_KA block is preceded by a TODO marker comment: `# TODO(Phase 6 execute): Shako sanity-check the Georgian phrasing of these 5 template strings before this plan's commit is merged.` This mirrors the Shako-review pattern from 06-11 but as a TODO marker rather than a checkpoint:human-verify task (these are static prose templates, not a security-critical lexicon — review benefit is real but Wave-blocking checkpoint cost is not justified per checker WARNING 7).
    - File contains `"en":` and `"ka":` substrings inside the BriefSections serializer (or equivalent).
    - File contains `--bilingual-test` argparse flag.
    - `python -m scripts.communicator.weekly_brief --dry-run --bilingual-test` exits 0 AND prints (or writes to stdout) a JSON blob containing `summary_lines[0].en` and `summary_lines[0].ka` as non-empty strings.
    - No call to compose_bilingual from this file (Option A is deterministic; Option B lives in task 3 / agents/communicator.py).
    - `python -c "from scripts.communicator import weekly_brief"` exits 0 (file imports cleanly).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "SUMMARY_TEMPLATES_KA\|summary_templates_ka" scripts/communicator/weekly_brief.py && grep -q "ka\"\\?:" scripts/communicator/weekly_brief.py && grep -q "bilingual-test\|bilingual_test" scripts/communicator/weekly_brief.py && python -c "from scripts.communicator import weekly_brief; print('OK')"</automated>
  </verify>
  <done>weekly_brief composer emits bilingual summary_lines via deterministic mirror; --bilingual-test CLI flag exposes the dry-run for verifier.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update agents/communicator.py + scripts/manager/briefing.py — Option B compose_bilingual call for per-row hypothesis/therapy/timeline inserts</name>
  <files>agents/communicator.py, scripts/manager/briefing.py</files>
  <read_first>
    - agents/communicator.py (CrewAI Communicator wrapper — find every hypotheses/therapies/aleksandra_timeline write path)
    - scripts/manager/briefing.py (Phase 5 manager briefing — read fully; understand which tables it writes)
    - scripts/communicator/bilingual.py (Task 1 output — compose_bilingual signature)
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Code Examples / "Example: Communicator inserting bilingual hypothesis row")
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-CONTEXT.md (D-02 per-tier policy)
  </read_first>
  <action>
    (a) **agents/communicator.py** — for every code path that INSERTs into hypotheses, therapies, or aleksandra_timeline with content that originated from an Anthropic call, refactor the single-language emission into a compose_bilingual call:

        # BEFORE (pseudocode)
        title_en = client.messages.create(...).content[0].text  # single English string
        cur.execute("INSERT INTO hypotheses (title, ...) VALUES (%s, ...)", (title_en, ...))

        # AFTER
        from scripts.communicator.bilingual import compose_bilingual
        pair = compose_bilingual(prompt="Draft a family-readable hypothesis title.\n\n{context}", client=client)
        # pair == {"en": "...", "ka": "..."}
        cur.execute("INSERT INTO hypotheses (title, ...) VALUES (%s::jsonb, ...)", (json.dumps(pair), ...))

    Use psycopg2's `Json` wrapper or explicit `json.dumps(pair)` with `::jsonb` cast; match the existing pattern in the file.

    Insert the PHI redactor + imperative-verb lint AROUND the compose_bilingual call EXACTLY per RESEARCH.md "Code Examples / Example: Communicator inserting bilingual hypothesis row" — that example is the canonical reference. The redactor must run on BOTH `.en` and `.ka` halves (RESEARCH.md Pitfall 5); raise RuntimeError if either half is blocked. Bilingual lint pass: run `check(pair['en'], locales=('en',))` AND `check(pair['ka'], locales=('ka',))`; raise if either fails.

    Note: scripts/communicator/banned_phrases.py and scripts/communicator/phi_redactor.py are already extended for Georgian by this plan's depends_on (06-10, 06-11) — per the Wave 3a/3b split from checker BLOCKER 2, those APIs are guaranteed to be in place when 06-09 runs. Use `redact_bilingual` (from 06-10) and `check(text, locales=('ka',))` (from 06-11) directly — they exist.

    (b) **scripts/manager/briefing.py** — for the manager-briefing assembler that inserts into briefs (or aleksandra_timeline / hypotheses / therapies if it does — verify by reading the file), apply the same pattern. The manager briefing is deterministic per RESEARCH.md (Phase 5 produces it without LLM cost by default); if its current code path is pure-Python templating with no LLM call, mirror the Option-A pattern from Task 2 (deterministic Georgian translation of the 3–5 template strings the manager briefing uses).

    (c) **scripts/communicator/outreach_drafter.py — DO NOT MODIFY.** Per CONTEXT.md D-02 per-tier policy, clinician outreach is single-recipient single-language; the existing `contacts.outreach_language` per-recipient routing covers it.

    (d) Add `runs` ledger logging around each compose_bilingual call (use the existing ledger.record_llm_call function pattern from scripts/communicator/weekly_brief.py — token cost, model, run_id). This satisfies the Phase 2.5 A.2 spend-instrumentation gate.
  </action>
  <acceptance_criteria>
    - agents/communicator.py imports `compose_bilingual` from `scripts.communicator.bilingual`.
    - agents/communicator.py contains at least one INSERT writing `::jsonb` cast (or psycopg2's Json wrapper) into hypotheses, therapies, or aleksandra_timeline.
    - agents/communicator.py contains the pattern `redact(...).text` called on both `.en` and `.ka` halves (RESEARCH.md Pitfall 5 — both-halves redaction).
    - agents/communicator.py contains an explicit `raise` on either-half block OR either-half lint fail.
    - scripts/manager/briefing.py either imports compose_bilingual OR contains a Georgian-mirror dict for its template strings (whichever matches the file's existing pattern).
    - scripts/communicator/outreach_drafter.py is UNCHANGED (git diff returns empty for that file in this plan's commit range).
    - `python -c "import agents.communicator; import scripts.manager.briefing; print('OK')"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "compose_bilingual" agents/communicator.py && grep -q "::jsonb\|psycopg2.extras.Json\|Json(" agents/communicator.py && python -c "import agents.communicator; import scripts.manager.briefing; print('OK')"</automated>
  </verify>
  <done>Communicator + manager briefing emit JSONB {en, ka}; outreach_drafter untouched per D-02; redactor + lint guards in place on both halves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Communicator agent → Anthropic API | prompt + medical content crosses external-service boundary |
| compose_bilingual output → Postgres briefs/hypotheses/therapies/aleksandra_timeline | LLM-emitted text crosses into persisted family-visible storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | compose_bilingual return shape | mitigate | Anthropic strict tool_use (`tools=[{strict: True, ...}]` + `tool_choice={type: 'tool', name: ...}`) provides grammar-constrained sampling — provably impossible to emit malformed JSON per RESEARCH.md Pattern 6. The defensive read in displayField is the safety net. |
| T-06-04-EMPTY-STRING | Tampering | compose_bilingual `.en` / `.ka` empty | mitigate | Task 3 acceptance: raise RuntimeError when either half is empty (caught by lint pass — banned_phrases.check fails an empty string). |
| T-06-01 (redactor-side) | Information Disclosure (HIGH) | agents/communicator.py | mitigate | Task 3 wires redact() on BOTH `.en` and `.ka` halves; OR-blocking — if either reports blocked, INSERT fails. The Georgian PHI patterns themselves are extended in plan 06-10. |
| T-06-COST-RUNAWAY (Phase 0 carry-over) | Denial of Service | compose_bilingual call | mitigate | Task 1 wires `check_daily_budget(estimated_cost_usd=0.01)` BEFORE the Anthropic call; existing FND-04 ceiling at $1.50/day caps runaway. |
</threat_model>

<verification>
- `python -m pytest tests/test_compose_bilingual.py -v` exits 0 (Task 1 unit tests)
- `python -m scripts.communicator.weekly_brief --dry-run --bilingual-test` exits 0 and emits JSONB with `summary_lines[0].en` + `summary_lines[0].ka`
- `python -m scripts.verify_phase6 --bucket C --gate I18N-06` PASSES
- No regression: `python -c "from scripts.communicator import weekly_brief, phi_redactor, banned_phrases; from scripts.manager import briefing; import agents.communicator"` all import cleanly
</verification>

<success_criteria>
- compose_bilingual helper exists with strict tool_use schema; budget gate integrated
- weekly_brief.py uses Option A (deterministic mirror) for summary_lines block — zero Anthropic cost
- agents/communicator.py uses Option B (compose_bilingual call) for per-row inserts — 1 Anthropic call per row instead of 2
- Manager briefing emits bilingual JSONB
- outreach_drafter.py untouched per D-02
- I18N-06 verifier check flips GREEN
</success_criteria>

<output>
Create `.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-09-SUMMARY.md` when done. Record:
- compose_bilingual line count + test count
- summary_lines Georgian translations (audit table — exactly which template strings landed which translation)
- Cost estimate per compose_bilingual call (RESEARCH.md says ~$0.001 overhead + token cost)
- Outreach_drafter confirmed untouched (git diff stat)
</output>
