---
phase: 06-bilingual-system-i18n
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/verify_phase6.py
  - tests/fixtures/phase6/phi_ka.yaml
  - tests/fixtures/phase6/bilingual_samples.json
requirements:
  - I18N-01
  - I18N-02
  - I18N-03
  - I18N-04
  - I18N-05
  - I18N-06
  - I18N-07
  - I18N-08
  - I18N-09
  - I18N-10
  - I18N-11
autonomous: true
must_haves:
  truths:
    - "scripts/verify_phase6.py exists and is runnable as a module"
    - "verifier exposes 5 capability buckets A/B/C/D/E per CONTEXT.md D-06"
    - "verifier prints a Phase 5-style table on exit and exits 0 only when all 11 I18N-* checks pass"
    - "tests/fixtures/phase6/phi_ka.yaml exists with the 10 Georgian PHI fixtures from RESEARCH.md Pattern 8"
    - "tests/fixtures/phase6/bilingual_samples.json exists with 30 sample bilingual digests for imperative-lint regression"
  artifacts:
    - path: scripts/verify_phase6.py
      provides: "Phase 6 11-check verifier with --mode code-complete + --bucket {A,B,C,D,E}"
      contains: "def check_i18n_01"
    - path: tests/fixtures/phase6/phi_ka.yaml
      provides: "10 Georgian PHI redactor fixtures (must_not_appear_in_output / must_block contracts)"
    - path: tests/fixtures/phase6/bilingual_samples.json
      provides: "30 bilingual digest samples (each with .en + .ka body) for imperative-verb lint regression"
  key_links:
    - from: scripts/verify_phase6.py
      to: scripts/verify_phase4.py
      via: "subprocess spawn — bucket E regression check"
      pattern: "verify_phase4"
    - from: scripts/verify_phase6.py
      to: scripts/verify_phase5.py
      via: "subprocess spawn — bucket E regression check"
      pattern: "verify_phase5"
---

<objective>
Author the Phase 6 verifier scaffold scripts/verify_phase6.py (skeleton — every check_i18n_NN function defined but most return RED until the corresponding Wave 1–4 plan lands the implementation), plus the two fixture files that downstream plans depend on (10 Georgian PHI phrases for I18N-10, 30 bilingual digest samples for the imperative-verb lint regression sweep). This is the Wave-0 gap closure required by 06-VALIDATION.md.

Purpose: Nyquist compliance — every downstream task has an automated verifier check it can call. Without this scaffold, executing plans 03–13 produces unverifiable artifacts.
Output: Working `python -m scripts.verify_phase6 --mode code-complete` that exits with 11 RED checks (as expected at Wave 0) but prints the Phase-5-style table and accepts --bucket A/B/C/D/E flags.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-SPEC.md
@.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-CONTEXT.md
@.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md
@.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-VALIDATION.md
@scripts/verify_phase5.py

<interfaces>
<!-- From scripts/verify_phase5.py (lines 60–100, read first) — the dataclass shape to mirror -->
@dataclass
class Check:
    code: str        # e.g. "I18N-01"
    label: str
    passed: bool
    evidence: str
    requirement: str = ""

@dataclass
class Report:
    checks: list[Check]
    def add(self, c: Check) -> None: ...
    @property
    def passed(self) -> bool: ...
    def print_table(self) -> None: ...

<!-- bucket→requirement map from CONTEXT.md D-06 -->
# A frontend: I18N-01, I18N-02, I18N-03, I18N-04, I18N-08
# B database: I18N-05, I18N-09
# C agent:    I18N-06, I18N-10
# D delivery: I18N-07
# E regress:  I18N-11
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write scripts/verify_phase6.py skeleton (11 checks + bucket dispatcher + Phase-5-style table)</name>
  <files>scripts/verify_phase6.py</files>
  <read_first>
    - scripts/verify_phase5.py (lines 1–200 — copy the dataclass + helper pattern; do NOT copy MNG-* code)
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-CONTEXT.md (D-06 — bucket map)
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Validation Architecture / Phase Requirements → Test Map — exact automated command per check)
  </read_first>
  <action>
    Create scripts/verify_phase6.py mirroring the verify_phase5.py structure:

    (1) Module docstring naming the 11 I18N-* checks + REGR.

    (2) `@dataclass Check` and `@dataclass Report` — IDENTICAL to verify_phase5.py lines 60–100.

    (3) Helper functions: `_pg_query(sql, params)`, `_module_present(dotted)`, `_file_present(rel)` — copy from verify_phase5.py.

    (4) Eleven `check_i18n_NN(report: Report) -> None` functions, one per I18N-01..I18N-11. Each function adds exactly one Check to the report. AT THIS WAVE-0 STAGE, every check returns `passed=False` with evidence `"PENDING — Wave N plan NN will land this"`. The specific Wave→check mapping (per 06-VALIDATION.md):
        - check_i18n_01 — assert next-intl@4.x in viewer/package.json; assert viewer/proxy.ts exists; assert `cd viewer && npm run build` exits 0. Wave 1 / plan 06-01.
        - check_i18n_02 — assert viewer/app/[locale]/{dashboard,timeline,papers,therapies,hypotheses,today,knowledge}/page.tsx all exist. (Production-mode check curls 14 URLs against `next start` — code-complete mode only checks file existence.) Wave 1 / plan 06-03.
        - check_i18n_03 — grep-extract every `t('Key')` / `useTranslations` call from viewer/app/[locale]/** + viewer/components/**; assert each dotted key path resolves in both viewer/messages/en.json and viewer/messages/ka.json. Wave 1 / plan 06-05.
        - check_i18n_04 — assert viewer/components/LanguageSwitcher.tsx imported by viewer/app/[locale]/layout.tsx (grep). Wave 1 / plan 06-04.
        - check_i18n_05 — production-mode: psql against $SUPABASE_DB_URL: `SELECT pg_typeof(title) FROM aleksandra_timeline LIMIT 1` returns 'jsonb' for all 4 tables × 6 columns; `SELECT polname FROM pg_policy WHERE polrelid='aleksandra_timeline'::regclass` returns the 3 migration-008 policies (and equivalents for hypotheses, therapies, briefs); code-complete mode: assert scripts/migrations/012_i18n_jsonb.sql exists AND scripts/migrations/012_rollback/ contains 4 .dump files. Wave 2 / plans 06-06, 06-07.
        - check_i18n_06 — assert scripts/communicator/bilingual.py exists OR scripts/communicator/weekly_brief.py contains string `compose_bilingual`. Production-mode runs a 1-call smoke through `compose_bilingual("test prompt")` and asserts returned dict has `'en'` and `'ka'` non-empty. Wave 3 / plan 06-09.
        - check_i18n_07 — assert scripts/communicator/telegram_sender.py reads `.ka` (grep for `\.get\(["']ka["']\)` or `\.ka`); assert scripts/communicator/gmail_digest.py reads `.en`. Wave 4 / plan 06-12.
        - check_i18n_08 — assert viewer/lib/i18n.ts exists and exports `displayField`; assert viewer/lib/__tests__/i18n.test.ts exists and `cd viewer && npx tsx --test viewer/lib/__tests__/i18n.test.ts` exits 0 (runner pinned per checker WARNING 4 — do NOT accept the node --test variant). Wave 1 / plan 06-04.
        - check_i18n_09 — production-mode psql: `SELECT count(*) FROM aleksandra_timeline WHERE title->>'en' = title->>'ka'` equals `SELECT count(*) FROM aleksandra_timeline` (and equivalents for the other 3 tables); code-complete mode: scripts/migrations/012_i18n_jsonb.sql contains string `jsonb_build_object('en'`. Wave 2 / plan 06-07.
        - check_i18n_10 — load tests/fixtures/phase6/phi_ka.yaml; for each fixture, call `from scripts.communicator.phi_redactor import redact, ConsentFlags`; assert each `must_not_appear_in_output` string is absent from `redact(input, ConsentFlags()).text` AND any `must_block: true` fixture returns `RedactionResult.blocked == True`. Plus load tests/fixtures/phase6/bilingual_samples.json; for each sample, call `from scripts.communicator.banned_phrases import check`; assert `check(sample['en'], locales=('en',)).passed` AND `check(sample['ka'], locales=('ka',)).passed`. Wave 3 / plans 06-10, 06-11.
        - check_i18n_11 — spawn `python -m scripts.verify_phase4 --mode code-complete` via subprocess.run; assert exit code 0 AND stdout contains '9/9 PASS'; spawn `python -m scripts.verify_phase5 --mode code-complete`; assert exit code 0 AND stdout contains '13/13 PASS'. Wave 4 / plan 06-13.

    (5) `main()` — argparse with `--mode {production,code-complete}` (default production) + `--bucket {A,B,C,D,E,all}` (default all) + `--gate I18N-NN`. Bucket dispatch:
        A → check_i18n_01, check_i18n_02, check_i18n_03, check_i18n_04, check_i18n_08
        B → check_i18n_05, check_i18n_09
        C → check_i18n_06, check_i18n_10
        D → check_i18n_07
        E → check_i18n_11

    (6) Exit code: 0 if report.passed else 1.

    Use UTF-8 throughout (`# -*- coding: utf-8 -*-` at top); the Georgian PHI fixture set requires correct UTF-8 handling on Windows under `.venv/Scripts/python.exe -X utf8`.
  </action>
  <acceptance_criteria>
    - File scripts/verify_phase6.py exists; line count ≥ 300.
    - `python -c "import scripts.verify_phase6; scripts.verify_phase6.main(['--mode', 'code-complete'])"` exits with code 1 (because no checks pass yet at Wave 0) AND prints a table.
    - `grep -c "def check_i18n_" scripts/verify_phase6.py` returns 11.
    - `python -m scripts.verify_phase6 --help` exits 0 and shows `--mode`, `--bucket`, `--gate` flags.
    - `python -m scripts.verify_phase6 --mode code-complete --bucket A` runs ONLY the 5 A-bucket checks (output table has 5 rows).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import scripts.verify_phase6 as v; print('OK' if sum(1 for n in dir(v) if n.startswith('check_i18n_')) == 11 else 'MISSING_CHECKS')" | grep -q "^OK$"</automated>
  </verify>
  <done>Verifier scaffold complete; all 11 checks scaffolded; 5-bucket dispatch works; expected to run all-RED until downstream plans land.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author tests/fixtures/phase6/phi_ka.yaml (10 Georgian PHI fixtures)</name>
  <files>tests/fixtures/phase6/phi_ka.yaml</files>
  <read_first>
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Pattern 8 — exact 10-fixture YAML block, lines ~823–855)
    - scripts/communicator/phi_redactor.py (existing Georgian patterns at lines 81–101 — fixtures must hit these AND new ones)
  </read_first>
  <action>
    Create tests/fixtures/phase6/phi_ka.yaml with EXACTLY the 10 fixtures from 06-RESEARCH.md Pattern 8 (the YAML block titled "Bilingual PHI fixture set (verifier C.3 input)"). The fixture schema:

        - name: <short label>
          input: <Georgian phrase containing PHI>
          must_not_appear_in_output: [<string>, <string>, ...]    # OR
          must_block: true                                        # for hard-block fixtures

    Use the exact 10 fixtures listed in RESEARCH.md (Patient full name Mkhedruli; Patient short name; Surname only; DOB Georgian-month; BMC MRN literal; Hospital name English-in-Mkhedruli; Doctor name English-in-Mkhedruli; Date dd.mm.yyyy; Patient first-name diminutive; MRI artifact reference HARD BLOCK).

    The file MUST be UTF-8 (Mkhedruli script Unicode block U+10A0..U+10FF). On Windows, save with explicit UTF-8 encoding (no BOM).

    Create the `tests/fixtures/phase6/` directory if it does not already exist.
  </action>
  <acceptance_criteria>
    - File tests/fixtures/phase6/phi_ka.yaml exists.
    - `python -c "import yaml; data = yaml.safe_load(open('tests/fixtures/phase6/phi_ka.yaml', encoding='utf-8')); print(len(data))"` prints `10`.
    - File contains the byte sequence for `ალექსანდრა` (Mkhedruli) at least 3 times.
    - File contains the byte sequence for `ჯინჭარაძე` at least 2 times.
    - File contains the literal `7616818` at least once.
    - File contains exactly one `must_block: true` line (the MRI artifact reference fixture).
    - `python -c "import yaml; data = yaml.safe_load(open('tests/fixtures/phase6/phi_ka.yaml', encoding='utf-8')); assert all('input' in f for f in data); assert all('must_not_appear_in_output' in f or 'must_block' in f for f in data); print('OK')"` prints `OK`.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import yaml; data = yaml.safe_load(open('tests/fixtures/phase6/phi_ka.yaml', encoding='utf-8')); assert len(data) == 10, f'got {len(data)}'; print('OK')"</automated>
  </verify>
  <done>10 Georgian PHI fixtures stored under tests/fixtures/phase6/phi_ka.yaml; consumed by check_i18n_10.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author tests/fixtures/phase6/bilingual_samples.json (30 bilingual digest samples for imperative-lint regression)</name>
  <files>tests/fixtures/phase6/bilingual_samples.json</files>
  <read_first>
    - .planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-RESEARCH.md (Pattern 8 — Bilingual PHI fixture context + Pattern 6 — Communicator bilingual emission shape)
    - scripts/communicator/banned_phrases.py (existing Georgian imperative lexicon at lines 78–93 — samples must EXERCISE the lint without triggering it; the goal is `count = 0` across 30 samples)
  </read_first>
  <action>
    Create tests/fixtures/phase6/bilingual_samples.json containing a JSON array of 30 sample bilingual digests. Each entry has the shape:

        {
          "id": "S01",
          "en": "<English digest body — clinical-register, non-imperative, evidence-grade language>",
          "ka": "<Georgian translation in Mkhedruli — same content, non-imperative, polite-passive>"
        }

    The 30 samples MUST:
    - Cover the 5 fixed-schema finding kinds from CGM-03 (`finding / source / evidence_strength / population_gap / clinician_question`) — at least 6 samples per kind.
    - Use English that avoids the 6 banned verbs (`should`, `must`, `consider`, `try`, `ask for`, `request`).
    - Use Georgian that avoids the 12 existing patterns in banned_phrases.py AND the 6 new D-05 entries (`უნდა X (with direct-instruction context)`, `მართებთ`, `აუცილებლად`, `აუცილებელია`, `განიხილეთ`, `გაითვალისწინეთ`, `სცადეთ`, `მოითხოვეთ`, `ითხოვეთ`, `ვურჩევთ`, `გვირჩევთ`, `რეკომენდირებულია`, `გამოჯანმრთელდება`, `ეს აღკვეთს`).
    - Use evidence-grade phrasing instead: "A 2024 case series reports..." / "Evidence-tier 2 finding..." / "კვლევა აღწერს..." / "მონაცემები ვარაუდობს...".

    The samples are DESIGN-INTENT — they must NOT cause the lint to fire, because check_i18n_10's lint sweep asserts `count = 0` violations across all 30. If samples accidentally contain a banned phrase, the verifier fails — that is intentional canary behavior.

    Write the file as UTF-8 (no BOM), JSON-formatted with 2-space indent.
  </action>
  <acceptance_criteria>
    - File tests/fixtures/phase6/bilingual_samples.json exists.
    - `python -c "import json; data = json.load(open('tests/fixtures/phase6/bilingual_samples.json', encoding='utf-8')); print(len(data))"` prints `30`.
    - Every entry has keys `id`, `en`, `ka`; every value is a non-empty string.
    - For each sample, neither `en` nor `ka` contains any of the banned strings (assert via grep on the rendered file).
    - At least one Mkhedruli codepoint (U+10A0..U+10FF) appears in every `.ka` value (no English-only entries).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import json; d = json.load(open('tests/fixtures/phase6/bilingual_samples.json', encoding='utf-8')); assert len(d) == 30; assert all('en' in s and 'ka' in s and s['en'] and s['ka'] for s in d); print('OK')"</automated>
  </verify>
  <done>30-sample bilingual digest fixture stored; consumed by check_i18n_10 imperative-lint regression sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier subprocess → verify_phase4/5 | spawned with current python env; trusted internal boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information Disclosure (low) | ka.json missing keys at runtime | mitigate | check_i18n_03 enforces every used `t('...')` key resolves in both dictionaries before phase closes. This plan establishes the verifier scaffold; 06-05 expands the dictionaries. |
| T-06-VERIFIER-COVERAGE | Tampering | scripts/verify_phase6.py | mitigate | Phase 5 verifier-style pattern (`Check`/`Report` dataclasses) is the established reference (78/78 cumulative coverage); no novel verification primitives introduced. |
</threat_model>

<verification>
- `python -m scripts.verify_phase6 --help` exits 0
- `python -m scripts.verify_phase6 --mode code-complete --bucket A` exits 1 (all checks RED at Wave 0) but prints 5-row table
- `test -f tests/fixtures/phase6/phi_ka.yaml`
- `test -f tests/fixtures/phase6/bilingual_samples.json`
- Both fixtures parse without UTF-8 / YAML / JSON errors
</verification>

<success_criteria>
- Phase 6 verifier scaffold runnable as a module
- 11 check_i18n_NN functions defined; bucket dispatcher works
- Georgian PHI fixture set (10 entries) parses
- Bilingual digest sample set (30 entries) parses; no entry trips the existing English or Georgian lint
- Downstream plans 06-03..06-13 have a verifier to call
</success_criteria>

<output>
Create `.planning/phases/06-bilingual-system-i18n-full-site-dynamic-data-bilingual-suppo/06-02-SUMMARY.md` when done. Record:
- verify_phase6.py line count
- Count of RED checks at Wave 0 (expected: 11)
- Whether Phase-5-style table renders cleanly under `-X utf8` on Windows
</output>
