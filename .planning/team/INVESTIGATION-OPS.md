# Investigation: Tier 0/1/2 Operations Remediation
**Agent:** R1 (Wave 1)
**Input:** `.planning/AUDIT_2026-05-18.md` §7
**Date:** 2026-05-18

## Summary

This investigation operationalises the 19 outstanding-work rows from audit §7 (T0-1…T0-7, T1-1…T1-5, T2-1…T2-7). Tier 0 is the only path that unblocks any user-visible production traffic: rows T0-1/T0-2 restore the Vercel viewer; T0-3/T0-4 distribute the 4 missing worker-URL env vars (`PHASE5_MANAGER_WORKER_URL`, `PERCEPTION_WORKER_URL`, `PHASE4_DIGEST_WORKER_URL`, `PHASE5_VOICE_WORKER_URL`, all pointing at `https://aleksandra-worker-production.up.railway.app` per audit §4.4) to both `.env` and Vercel project env; T0-5 swaps the Railway `n8n` service from our `Dockerfile.worker` (per repo-root `railway.json`) back to `n8nio/n8n:latest`; T0-6 is gated on parallel agent R2's CGM-01 patch; T0-7 commits the 6 modified files surfaced by `git status --short` (`.gitignore`, `scripts/setup_qdrant.py`, `scripts/verify_phase2.py`, `scripts/verify_phase2_5.py`, `scripts/chunking/embedder.py`, `scripts/chunking/retrofit_qdrant_stamps.py`).

Tier 1 represents what must happen during the Phase 4 acceptance window (closes ~2026-06-07): activating the 6 inactive workflows after n8n is alive (T1-1), one-off firing of `daily_digest` to clear the C.3 → REGR cascade (T1-2), and three passive observations on Sunday 2026-05-24 and beyond. Tier 2 is hardening — doc-truthing CLAUDE.md, fresh Railway token, alias decision, missing-tables triage, ANTHROPIC_USAGE_API_KEY, paper backlog, and stale CF Worker cleanup.

Confirmed via probes during this investigation: there is **no root `vercel.json`** (only `external-skills/open-design/vercel.json`, unrelated), so Root Directory in the Vercel UI is the authoritative knob for T0-1; the viewer reads `PHASE5_MANAGER_WORKER_URL`, `PHASE5_VOICE_WORKER_URL`, `PHASE5_WORKER_AUTH_TOKEN`, `MANAGER_USER_ID`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` at runtime (15 hits across 6 files under `viewer/app/api/manager/` and `viewer/lib/supabase.ts`); root `railway.json` sets `"builder":"DOCKERFILE", "dockerfilePath":"Dockerfile.worker"` for the entire project, which explains why the Railway `n8n` service serves `{"service":"perception_worker"}` on `/`.

## Tier 0 — Operational rows (7 items, copied from audit §7)

| ID | Action | Where | Exact step | Verify | Blast radius | ETA min | Deps | Audit cite |
|---|---|---|---|---|---|---|---|---|
| T0-1 | Fix Vercel "Root Directory" to `viewer/` | Vercel UI → Project `viewer` (id `prj_pOGHJR8Hr3ACxFr2IOzAi8S1lkhm`) → Settings → General → Root Directory | 1. Open project Settings. 2. Find "Root Directory" field (currently empty / repo-root). 3. Enter `viewer`. 4. Save. No root `vercel.json` exists at the repo root (confirmed via Glob — only `external-skills/open-design/vercel.json` is present, scoped to that subdir) so there is no override to worry about. | After T0-2 redeploy: `curl -I https://viewer-git-main-shakos-projects-82dad3f2.vercel.app/` returns 200 or 401-SSO with a valid Next.js HTML body (not the current build-error `DEPLOYMENT_NOT_FOUND` on `aleksandra-brain-v4.vercel.app`). | Mis-set value (e.g. `/viewer` with leading slash, or `viewer/app`) → next build still errors with "Couldn't find any `pages` or `app` directory". No data loss. | 2 | — | §4.6, §7 |
| T0-2 | Trigger a fresh Vercel deploy after T0-1 | Vercel UI → Project `viewer` → Deployments → most recent ERROR deploy → "⋯" → Redeploy (or `git commit --allow-empty -m "chore(deploy): bump for root-dir fix" && git push origin main`) | 1. After T0-1 save, click Redeploy on the topmost ERROR deploy in the list. 2. Watch build logs — expect to see `npm run vercel-build` succeed inside `viewer/`. 3. Once green, check Aliases tab: confirm `viewer-git-main-shakos-projects-82dad3f2.vercel.app` now points at the new deployment. Open question for D1/D2: should `aleksandra-brain-v4.vercel.app` be assigned as an alias (it does not currently exist — audit §4.6 says `DEPLOYMENT_NOT_FOUND`), since `5-warm-crown.md` Phase E references that URL? | `curl -fsS -o /dev/null -w "%{http_code}" https://viewer-git-main-shakos-projects-82dad3f2.vercel.app/brain` → 200 (or 401-SSO with HTML shell, not build-error). API probe: `GET https://api.vercel.com/v6/deployments?projectId=prj_pOGHJR8Hr3ACxFr2IOzAi8S1lkhm&limit=1` shows `state: READY`. | Build still red → viewer unreachable, Phase 5 UI cannot render, family-facing brief activity feed stays dark. | 6 | T0-1 | §4.6, §7 |
| T0-3 | Add 4 missing worker-URL env vars to `.env` | Local repo `.env` (file lives at `c:\Users\jinch\…\aleksandra brane\.env`) | Append below the existing `PHASE5_WORKER_AUTH_TOKEN=…` (currently line 141, confirmed by grep): `PHASE5_MANAGER_WORKER_URL=https://aleksandra-worker-production.up.railway.app` / `PERCEPTION_WORKER_URL=https://aleksandra-worker-production.up.railway.app` / `PHASE4_DIGEST_WORKER_URL=https://aleksandra-worker-production.up.railway.app` / `PHASE5_VOICE_WORKER_URL=https://aleksandra-worker-production.up.railway.app`. All four URLs are identical (single worker service per audit §4.4 healthz). | `grep -nE "^(PHASE5_MANAGER_WORKER_URL|PERCEPTION_WORKER_URL|PHASE4_DIGEST_WORKER_URL|PHASE5_VOICE_WORKER_URL)=" .env` returns 4 matches, each `=https://aleksandra-worker-production.up.railway.app`. | Local scripts that consult `os.environ` directly (workflows, smoke tests) silently fall back to placeholders. Not user-visible until Vercel side (T0-4) also missing. | 5 | — | §5, §4.7, §7 |
| T0-4 | Copy 5 env vars to Vercel project env | Vercel UI → Project `viewer` → Settings → Environment Variables → Add | Add 5 keys, each scoped Production (and Preview, optional): `PHASE5_MANAGER_WORKER_URL`, `PHASE5_VOICE_WORKER_URL`, `PHASE4_DIGEST_WORKER_URL`, `PERCEPTION_WORKER_URL` — all four `=https://aleksandra-worker-production.up.railway.app` — plus `PHASE5_WORKER_AUTH_TOKEN` (copy hex64 value from `.env` line 141). `MANAGER_USER_ID` (line 124) and `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` should already be present from earlier deploys — confirm. Viewer reads these at runtime in 6 files: `viewer/lib/supabase.ts:53-54`, `viewer/app/api/manager/apply/route.ts:16-48`, `viewer/app/api/manager/voice/route.ts:24-57`, `viewer/app/api/manager/email/route.ts:15-45`, `viewer/app/api/manager/audit/route.ts:22`, `viewer/app/api/manager/undo/[id]/route.ts:28-43`. | Redeploy then `curl -fsS https://viewer-git-main-…/api/manager/audit` returns 200 JSON (not 500 "MANAGER_USER_ID missing"). Or in Vercel Functions logs, no `… is not defined` errors. | Manager Apply/Voice/Email/Undo all 500 at runtime; viewer renders but actions fail. | 7 | T0-3, T0-2 | §5, §4.6, §7 |
| T0-5 | Swap Railway `n8n` service to Docker image | Railway UI → Project `lucky-ambition` (or the project owning the `n8n-production-48c7` subdomain) → service `n8n` → Settings → Source | 1. In service Settings → Source, click "Change source" → choose "Docker Image" → enter `n8nio/n8n:latest`. 2. Verify Build section no longer references `Dockerfile.worker` (root `railway.json` line 5 currently forces `"dockerfilePath":"Dockerfile.worker"` — this file applies project-wide; consider either (a) deleting/renaming `railway.json` so it stops applying to the n8n service, or (b) confirming Railway "Docker Image" source overrides `railway.json`'s `build` block). 3. Confirm persistent volume `/home/node/.n8n` is still attached (postgres data + encryption key live there). 4. Set service-level env vars `N8N_HOST`, `N8N_PROTOCOL=https`, `WEBHOOK_URL=https://n8n-production-48c7.up.railway.app/`, `GENERIC_TIMEZONE=UTC`. 5. Deploy. | `curl -sI https://n8n-production-48c7.up.railway.app/` returns HTML containing `n8n` (login page) **not** JSON `{"service":"perception_worker"}`. Also `curl -H "X-N8N-API-KEY: $N8N_API_KEY" https://n8n-production-48c7.up.railway.app/api/v1/workflows` returns 200 with a JSON `data` array of 11 workflows (currently 404 per audit §4.5). | If volume detaches or encryption key changes → all stored credentials inside n8n become unreadable; workflows would need re-import + Telegram/Gmail OAuth re-auth. Snapshot the volume before swap. | 13 | — | §4.5, §4.7, §7 |
| T0-6 | Apply R2's CGM-01 patch (Qdrant 403 → propagate `QDRANT_API_KEY`) | Wherever R2 locates the `summarize()` call site (the `papers/points/search` HTTP path in the verbatim error implicates the Communicator/retrieval module) | After R2's patch lands and is staged, run `.venv/Scripts/python.exe -X utf8 -m scripts.verify_phase3 --json`. Expected: CGM-01 → PASS; REGR still FAIL until T1-2 fires daily_digest, so overall **10/11 PASS** (not 11/11). | `verify_phase3 --json` shows `cgm_01.passed=true`. Manual: `python -c "from qdrant_client import QdrantClient; import os; q=QdrantClient(url=os.environ['QDRANT_URL'], api_key=os.environ['QDRANT_API_KEY']); print(q.search('papers', [0.0]*384, limit=1))"` returns a list (currently 403). | Communicator agent retrieval keeps returning 0 evidence → every clinician brief lacks citations → CGM-01 stays red and Phase 3 dashboard misrepresents reality (CLAUDE.md still claims 11/11 — audit §6 delta row). | 3 (after R2 patch) | R2 patch | §Phase 3 (CGM-01), §6, §7 |
| T0-7 | Commit the 6 (or 7) modified scripts | Local git working tree at repo root | 1. After T0-6, run `git add .gitignore scripts/setup_qdrant.py scripts/verify_phase2.py scripts/verify_phase2_5.py scripts/chunking/embedder.py scripts/chunking/retrofit_qdrant_stamps.py <CGM-01-patched-file>` (confirmed via `git status --short` during this investigation). 2. Commit with Conventional Commits: `fix(qdrant): propagate QDRANT_API_KEY across scripts + fix CGM-01 verifier 403`. 3. `git push origin main`. Do NOT bulk `git add .` — there are 33 untracked Phase 6 scaffold files (audit §3) that must not enter this commit. | `git status --short` shows zero `M` lines under `scripts/`; `git log -1 --stat` lists the 6 (or 7) expected files; remote `main` advances by one commit. | Push without the 7th file → CGM-01 stays red. Bulk-add → 5,073 LOC of unreviewed Phase 6 scaffold enters main. | 5 | T0-6 | §A.1, §3, §7 |

## Tier 1 — Acceptance-window rows (5 items)

| ID | Action | Where | Exact step | Verify | Blast radius | ETA min | Deps | Audit cite |
|---|---|---|---|---|---|---|---|---|
| T1-1 | Activate 6 inactive n8n workflows | n8n UI at `https://n8n-production-48c7.up.railway.app/` (post-T0-5) → Workflows list | For each of `perception_6h`, `daily_digest`, `daily_spend_report`, `chunking_trigger`, `extraction_trigger`, `urgent_alerts`: open the workflow → toggle the Active switch → Save. Confirm each saved switch persists across reload (per audit §4.7 file-flag `active=False`). | n8n REST: `curl -H "X-N8N-API-KEY: $N8N_API_KEY" https://n8n-production-48c7.up.railway.app/api/v1/workflows | jq '.data[] | {name, active}'` shows all 11 with `active=true` (currently 5 active by file, 0 active in reality). | Forgetting one (e.g. `urgent_alerts`) → silent miss on a critical alert tier. | 6 | T0-5 | §4.7, §7 |
| T1-2 | Manually fire `daily_digest` once | n8n UI → workflow `daily_digest` → "Execute Workflow" button | After T1-1, click Execute. Wait for the Telegram message to arrive + a new row in Supabase `runs` table with `kind LIKE '%daily_digest%'` within last 5 min. This single fire clears the Phase 2.5 C.3 cascade (audit §Phase 2.5) which then unblocks REGR in Phase 3/4/5 verifiers. | `verify_phase2_5 --json` C.3 → `recent_fire >= 1`; subsequent reruns of `verify_phase3 / verify_phase4 / verify_phase5` show REGR → PASS (so totals become 11/11, 9/9, 13/13 once T0-6 also lands). | Workflow has a hidden bug → first real fire errors silently in n8n logs. | 2 | T1-1 | §Phase 2.5 C.3, §7 |
| T1-3 | Sunday 2026-05-24 first Weekly Brief check | Passive monitoring | Sunday 2026-05-24 around 13:00 UTC + 30 min: query `select count(*) from briefs;` in Supabase (currently 0 per audit §4.1) → expect ≥1; check Gmail Drafts for the weekly brief draft; check Vercel viewer activity feed for the new brief entry. | `briefs` count ≥ 1; Gmail draft visible; activity feed shows entry. If 0 after 13:30 UTC, check n8n execution log for `weekly_brief`. | First family-visible deliverable misses if cron silently fails. | Wait (passive) | T1-1, T0-4 | §4.1 briefs=0, §7 |
| T1-4 | Sunday Manager Briefing fires | Passive monitoring | Sunday 2026-05-24 13:00 UTC: confirm Telegram message arrives from manager-briefing worker call (`POST /morning-briefing` to `aleksandra-worker-production.up.railway.app`); check `runs` table for matching row. | New row in `runs` with `kind` matching manager_briefing and `created_at` within 13:00–13:05 UTC; Telegram delivery confirmed by family. | Manager briefing silently misses week 1 → Shako loses the activation signal. | Wait (passive) | T1-1, T0-3 | §4.7 manager_briefing active, §7 |
| T1-5 | Daily outreach_log growth monitoring | Local check / Supabase SQL | Daily for the next 14 days: `select count(*), max(created_at) from outreach_log;` — currently 1 row, max=2026-05-16-ish per audit §4.1. Expect growth as `outreach_review_queue.json` (cron `0 18 * * *`, active=True per file) starts firing post-T0-5. | Count climbs from 1; `max(created_at)` advances daily. | Cron fires but writes never persist (RLS or service-role mis-config) → 0 growth despite green logs. | Daily 1 min | T0-5 | §4.1 outreach_log=1, §4.7, §7 |

## Tier 2 — Hardening rows (7 items)

| ID | Action | Where | Exact step | Verify | Blast radius | ETA min | Deps | Audit cite |
|---|---|---|---|---|---|---|---|---|
| T2-1 | Truth up `CLAUDE.md` "მიმდინარე ეტაპი" Phase 3 claim | Local repo `CLAUDE.md` | Either (a) replace "11/11 CGM gates + REGR ყველა GREEN" with "10/11 CGM gates (CGM-01 pending Qdrant key propagation) + REGR pending C.3 fire" — OR — (b) skip if T0-6 lands and verifier reruns 11/11 same session. Also bump entity counts (568 → 641) and add the Vercel-broken / n8n-broken caveats. Commit as `docs: truth up CLAUDE.md after 2026-05-18 audit`. | New CLAUDE.md content aligns with audit §6 deltas; `git diff CLAUDE.md` reviewable in PR. | None (doc-only). | 8 | (T0-6 optional) | §6 delta rows, §7 |
| T2-2 | Refresh `RAILWAY_API_TOKEN` | Railway UI → Account Settings → Tokens | 1. Generate new account token (not project token — project tokens return `Project Token not found` per audit §4.4). 2. Update `.env` line 69 in place. 3. Probe: `curl -H "Authorization: Bearer $RAILWAY_API_TOKEN" -H "Content-Type: application/json" -d '{"query":"query { me { name email } }"}' https://backboard.railway.app/graphql/v2` → returns name + email. | GraphQL `me` query succeeds; service list query also succeeds. | Token leak in handoff → rotate immediately. | 4 | — | §4.4, §7 |
| T2-3 | Resolve `aleksandra-brain-v4.vercel.app` alias question | Vercel UI → Project `viewer` → Settings → Domains | Decision needed (open question for D1/D2): either (a) add alias `aleksandra-brain-v4.vercel.app` to point at production deployment, OR (b) sweep all docs and runbooks (`docs/PHASE_5_OPERATOR_RUNBOOK.md`, `.claude/plans/5-warm-crown.md` Phase E, etc.) to use the actual alias `viewer-git-main-shakos-projects-82dad3f2.vercel.app`. | `curl -I https://aleksandra-brain-v4.vercel.app/` returns 200/401-SSO (not `DEPLOYMENT_NOT_FOUND`), OR `git grep "aleksandra-brain-v4"` returns 0 hits. | Docs continue to reference a fictitious URL → onboarding friction. | 12 | T0-2 | §4.6, §7 |
| T2-4 | Resolve missing-tables question | Supabase SQL editor + repo grep | For each of `llm_call`, `citations`, `digest_to_run_link`, `telegram_history`, `email_log`, `firecrawl_calls`: (a) `git grep -nE "(from_|\.table\(|FROM )<name>"` in repo to find usage; (b) decide create-migration vs remove-dead-code; (c) for those kept, draft migration `012_missing_tables.sql`. | Either each table exists in Supabase or no code references remain. `verify_phase*` reruns clean. | Hidden runtime errors on cron paths that try to insert. | 30 | — | §4.1 missing tables, §7 |
| T2-5 | Reconnect `ANTHROPIC_USAGE_API_KEY` | Anthropic Console → Admin Keys → new key | 1. Generate real admin key (current `.env` line 77 is placeholder `sk-ant-admin-`). 2. Update `.env` and n8n credential store. 3. After T0-5, re-test `daily-budget-gate.json` — should now successfully fetch real spend (was 0 because n8n dead + key fake). | n8n `daily-budget-gate` execution log shows real spend value; Telegram alert thresholds become trustworthy. | False sense of budget headroom → cap blow-through. | 9 | T0-5 | §5 line 77, §7 |
| T2-6 | Reprocess ~200 ledger papers blocked by Anthropic 400 content-filter | `scripts/chunking/embedder.py` + scripts/extract pipeline (the 5 newly-modified scripts target this path) | After T0-7 commit, rerun extraction over ledger rows where `entities IS NULL`. Expect entity count to climb from 641 toward ~1000+. Spend cap target ~$0.30 (check `runs.cost_usd` sum). | Neo4j: `MATCH (n) RETURN count(n)` ≥ 900 (currently 792 per audit §4.3); ledger rows with `entities IS NOT NULL` increases by ~200. | LLM spend overrun if content-filter cascade pushes retries. Cap at $1 just in case. | 25 | T0-7 | §4.3 792 nodes, §7 |
| T2-7 | Delete stale Cloudflare Worker `aleksandrabrane4` | Cloudflare Dashboard → Workers & Pages → `aleksandrabrane4` → Manage → Delete | Single-click delete in CF dashboard. No code paths consume it (carry-over item, not probed in this audit). | `curl -fsS https://aleksandrabrane4.<acct>.workers.dev/` returns NXDOMAIN / 404 hostname; CF dashboard no longer lists worker. | None. | 3 | — | §7 (carry-over) |

## Open questions for Wave 2 (D1 / D2)

- **Vercel alias decision (T2-3):** add `aleksandra-brain-v4.vercel.app` as a real alias, or sweep all runbook references to the actual stable alias (`viewer-git-main-shakos-projects-82dad3f2.vercel.app` or `viewer-sigma-two.vercel.app`)? Audit §4.6 confirms the v4 alias does not exist; `5-warm-crown.md` Phase E and `docs/PHASE_5_OPERATOR_RUNBOOK.md` reference it.
- **railway.json scope vs Docker Image source (T0-5):** root `railway.json` declares `"builder":"DOCKERFILE", "dockerfilePath":"Dockerfile.worker"` project-wide; will switching the `n8n` service "Source" to a Docker Image in the Railway UI override this, or do we need to either (a) move `railway.json` into a per-service path, or (b) delete it and use service-level Build configs? Best-effort answer: Railway service-level source overrides repo `railway.json`, but worth confirming before the swap to avoid bricking the worker service.
- **Volume snapshot before n8n swap (T0-5):** does the existing Railway `n8n` service have a persistent volume at `/home/node/.n8n` from before it was rebuilt as the worker? If yes, encryption key + credentials may still be intact. If no (volume was wiped when Dockerfile.worker took over), all 11 workflows need re-import + Telegram/Gmail OAuth re-auth.
- **Missing tables triage (T2-4):** are `llm_call` / `citations` / `digest_to_run_link` / `telegram_history` / `email_log` / `firecrawl_calls` aspirational (need migration) or vestigial (need code removal)? D1/D2 to scan code references and decide migration vs deletion.
- **`MANAGER_USER_ID` value in Vercel (T0-4):** `.env` line 124 has it as `shako-jincharadze` — a string, not a uuid. Confirm that the Supabase `manager_actions.user_id` column accepts a free-form string, or whether a real uuid is needed before Phase 5 production traffic is meaningful.
- **CGM-01 patch site (T0-6):** R2 must identify the exact `summarize()` call site. The audit fingerprint is the `papers/points/search` POST returning 403 — likely the same Qdrant-client construction pattern that was already fixed in `scripts/setup_qdrant.py`, `scripts/chunking/embedder.py`, `scripts/chunking/retrofit_qdrant_stamps.py`, `scripts/verify_phase2.py`, `scripts/verify_phase2_5.py`, but applied to the Communicator retrieval path.
