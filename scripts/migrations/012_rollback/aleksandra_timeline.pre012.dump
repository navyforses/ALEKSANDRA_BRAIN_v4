-- PostgreSQL database dump
-- PLACEHOLDER — populated by scripts/migrations/012_runbook.md step 3 before psql apply
--
-- Phase 6 / Plan 06-06 authored this skeleton.
-- Phase 6 / Plan 06-07 (autonomous=false, Shako-supervised) will replace this file
-- with the live output of:
--
--   pg_dump "$SUPABASE_DB_URL" \
--     --table=aleksandra_timeline \
--     --data-only \
--     --column-inserts \
--     --no-owner --no-privileges \
--     --file=scripts/migrations/012_rollback/aleksandra_timeline.pre012.dump
--
-- The populated dump contains every existing row's data as explicit
-- INSERT INTO aleksandra_timeline (...) VALUES (...) statements. The
-- --column-inserts flag is critical: it produces a restorable text file
-- that is independent of the column TYPE (so it remains valid both
-- before and after the TEXT-to-JSONB conversion in migration 012).
--
-- ROLLBACK USAGE (if migration 012 smoke check fails):
--   psql "$SUPABASE_DB_URL" -c "BEGIN; DELETE FROM aleksandra_timeline;"
--   psql "$SUPABASE_DB_URL" -f scripts/migrations/012_rollback/aleksandra_timeline.pre012.dump
--   # then schema TYPE revert (see runbook ## Rollback procedure)
--
-- DO NOT commit live data here. The repository placeholder is intentional;
-- live row data is only written by 06-07 immediately before psql apply,
-- and pg_dump output is shred-equivalent (Shako's local clone), never pushed.
--
-- Expected size at current row counts (per CLAUDE.md Phase 2.5):
--   aleksandra_timeline rows are part of 47 episodes — likely 10-30 KB
