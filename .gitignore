# ═══════════════════════════════════════════════════════════
# ALEKSANDRA_BRAIN — Git ignore
# ═══════════════════════════════════════════════════════════

# Secrets
.env
.env.local
.env.*.local
*.pem
*.key
.secrets/
.secrets/*

# Medical imaging (NEVER commit patient data)
*.nii
*.nii.gz
*.dcm
*.dicom
*.mha
*.mhd
*.nrrd
*.gii
*.surf
*.mgz
*.glb
*.gltf
patient_data/
mri_data/

# Provisioned fonts — downloaded on demand + checksum-verified by
# brain/common/ka_font.py; never vendored (see brain/assets/fonts/README.md)
brain/assets/fonts/*.ttf
brain/assets/fonts/*.otf
brain/assets/fonts/*.part

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
.venv-*/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Node / Next.js
node_modules/
.next/
out/
.turbo/
.vercel/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Claude Code / memory
.claude-mem/
.claude/
.agents/
.code-review-graph/
CLAUDE.local.md

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# OS
Thumbs.db
desktop.ini

# Databases (local volumes)
neo4j_data/
qdrant_data/
postgres_data/

# Embeddings cache
.fastembed_cache/
embeddings_cache/

# Test outputs
.coverage
htmlcov/
.tox/
coverage.xml
*.cover
briefs/*.pdf

# Runtime run logs
scripts/repurposing/run_logs/
.observer/

# Verifier run logs (timestamped JSON snapshots — regenerated per verifier run)
v7_architecture/foundation_logs/verify_phase_*_*.json
v7_architecture/foundation_logs/*.log

# graphify-out skill build artifacts
graphify-out/

# Codex skill build artifacts
.codex/

# Local imports / contact exports (may contain personal emails)
data/notion_contacts.csv

# External skill repos (cloned reference material, not source-of-truth)
external-skills/

# Per-deploy raw-paste blocks (secrets — never commit)
.planning/RAILWAY_ENV_BLOCK.txt
.planning/VERCEL_ENV_BLOCK.txt

# Neo4j Aura download files contain plaintext passwords
Neo4j-*-Created-*.txt

# Large Phase 6 swarm fixture point-clouds — Git LFS / regenerate-on-demand pending decision (R3 flag)
tests/fixtures/damaged_brain_points.json
tests/fixtures/healthy_brain_points.json
tests/fixtures/test_brain_128_swarm_result.json

# Migration pre-DDL DB snapshots (generated safety nets; tables are
# reconstructable from evidence_ledger + R2 — see scripts/migrations/*_runbook.md)
scripts/migrations/028_trials_backup.json
